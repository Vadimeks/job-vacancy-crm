## [2026-05-28] — Бэкенд: Паляпшэнне сінхранізацыі OTTO і справаздачнасці

### ⚙️ Backend

- **`sheets.service.js`**: Укаранёна правіла "мінімум 3 ячэйкі" для актывацыі радкоў OTTO.
- **`sheets.service.js`**: Адключана праверка STOP-маркераў для OTTO, каб пазбегнуць памылковага закрыцця праз каментары.
- **`sheets.service.js`**: Палепшана логіка вызначэння назвы вакансіі: тэхнічныя ID больш не выкарыстоўваюцца як загалоўкі.
- **`sheets.service.js`**: У справаздачы Inbox цяпер выводзяцца MongoDB `_id` для новых і закрытых вакансій.

## [2026-05-28] — Бэкенд: Выпраўленне фільтрацыі вакансій

### ⚙️ Backend

- **`vacancies.js`**: Укаранёна строгая фільтрацыя па жыллю на баку сервера. Цяпер вакансіі "Без житла" не адлюстроўваюцца пры актыўным фільтры "З жыллём".
- **`vacancies.js`**: Аптымізавана логіка выбару рэгіёна. Пры выбары "Польшча" аўтаматычна паказваюцца ўсе польскія ваяводствы.

## [2026-05-28] — Бэкенд: Нармалізацыя рэгіёнаў і выпраўленне фільтрацыі

### ⚙️ Backend

- **`ai.service.js`**: Экспартаваны эталонныя спісы `POLISH_VOIVODESHIPS` і `VOIVODESHIP_MAP`. Выпраўлены рэгістр назваў ваяводстваў для поўнай адпаведнасці фронтэнду.
- **`vacancies.js`**: Поўнасцю перапісана логіка фільтра "Польшча". Цяпер выкарыстоўваецца строгі пошук па спісе ваяводстваў (`$in`), што вырашае праблему з пустатой палёў і замежнымі вакансіямі.
- **`vacancies.js`**: У маршруце `filters-data` укаранёна валідацыя. Цяпер у выпадаючы спіс на фронтэндзе трапляюць толькі валідныя назвы рэгіёнаў, што выключае з'яўленне дублікатаў тыпу "Підляське воєводство".
- **`vacancies.js`**: Палепшаны скрыпт `cleanup-locations`. Дададзена інтэлектуальнае мапінгу ўкраінскіх назваў ваяводстваў на лацінскія эталоны (напр. "Підляське" -> "Podlaskie").

## [2026-05-28] — Бэкенд: Поўная нармалізацыя геаграфічных дадзеных

### ⚙️ Backend

- **`ai.service.js`**: Пашыраны `VOIVODESHIP_MAP` (дададзены ўсе варыянты напісання на трох мовах і розныя кадыроўкі сімвалаў).
- **`vacancies.js`**: Укаранёна фільтрацыя на аснове рэгулярных выразаў (`RegExp 'i'`), што дазваляе знаходзіць вакансіі незалежна ад рэгістра ў базе.
- **`vacancies.js`**: Маршрут `filters-data` пераведзены на "белы спіс" эталонных ваяводстваў. Смеццевыя запісы больш не трапляюць у інтэрфейс.
- **`vacancies.js`**: Скрыпт ачысткі зроблены агрэсіўным: аўтаматычна выпраўляе рэгістр і запаўняе пустыя палі рэгіёна значэннем "Польща".

### [28.05.2026] — Backend: Паляпшэнне геаграфічнай фільтрацыі v2.3.1

- **vacancies.js**: Перапрацавана логіка маршруту `GET /`. Цяпер выбар рэгіёна "Польшча" ўключае ўсе вакансіі з `country: "Polska"`, што вырашае праблему адлюстравання агульных вакансій.
- **vacancies.js**: Укаранёны "частковы пошук" (partial match) для ваяводстваў. Цяпер вакансіі, якія працуюць на некалькі рэгіёнаў адначасова (напр. "Wielkopolskie, Dolnośląskie"), карэктна адлюстроўваюцца пры выбары любога з гэтых рэгіёнаў.
- **vacancies.js**: Маршрут `filters-data` цяпер інтэлектуальна збірае спіс даступных рэгіёнаў, ігнаруючы дублікаты і заўсёды прапаноўваючы пункт "Польшча" для польскіх кантрактаў.

### [28.05.2026] — Backend: Падтрымка мульты-рэгіянальных вакансій v2.3.2

- **vacancies.js**: Скрыпт `cleanup-locations` перапрацаваны для падтрымкі спісаў ваяводстваў. Цяпер кожны рэгіён у радок нармалізуецца асобна.
- **ai.service.js**: Абноўлены `SYSTEM_INSTRUCTION`. AI цяпер абучаны вызначаць і пералічваць некалькі ваяводстваў праз коску, калі вакансія ахоплівае розныя гарады.
- **ai.service.js**: У функцыю `processSingle` дададзена лагічная апрацоўка масіваў ваяводстваў, што гарантуе чысціню даных пры парсінгу.

## [2026-05-29] — Рамонт сінхранізацыі і паляпшэнне справаздачнасці v2.4

### ⚙️ Backend & Sync

- **sheets.service.js**: Укаранёна логіка **"Рэанімацыі вакансій"**. Цяпер, калі вакансія была закрыта ў базе, але зноў з'явілася як актыўная ў Google Sheets, яна аўтаматычна пераводзіцца ў статус `active` з абнаўленнем даных.
- **sheets.service.js**: Палепшана функцыя `getRowStatus`. Дададзены новыя маркеры ("СТОП", "ИЩЕМ", "opiekunka") і ўведзена ўніверсальнае правіла мінімум 3-х запоўненых ячэйкі для адсеку шуму.
- **sheets.service.js**: Укаранёны механізм **"Break on Cooldown"**. Калі AI выдае памылку лімітаў (429), сінхранізацыя ўсіх наступных табліц спыняецца для захавання стабільнасці сервера.
- **sheets.service.js**: Справаздачы ў Inbox цяпер выкарыстоўваюць публічныя коды вакансій `[VAC-XXXX]` замест тэхнічных MongoDB ID, што палягчае пошук для рэкрутэраў.
- **Vacancy.js**: Дададзены камбінаваныя індэксы па `sourceHash` і `status` для паскарэння працэсу дэдуплікацыі і "рэанімацыі" пры сканаванні вялікіх табліц.
- **index.js**: Сінхранізацыя табліц пераведзена на строга паслядоўны рэжым (sequential) з паўзамі паміж табліцамі для прадухілення Rate Limit памылак.

## [2026-05-29] — Рамонт сінхранізацыі і паляпшэнне справаздачнасці v2.5

### ⚙️ Backend & Sync

- **sheets.service.js**: Укаранёна логіка **"Рэанімацыі вакансій"**. Цяпер, калі вакансія была закрыта ў базе, але зноў з'явілася як актыўная ў Google Sheets, яна аўтаматычна пераводзіцца ў статус `active` з абнаўленнем тэксту.
- **sheets.service.js**: Палепшана функцыя `getRowStatus`. Дададзены новыя маркеры ("СТОП", "ИЩЕМ", "opiekunka") і ўведзена строгае правіла для лічбавых маркераў (напр. "0"), каб пазбегнуць ілжывага закрыцця вакансій з ID.
- **sheets.service.js**: Палепшана функцыя `buildRowText`. Укаранёны фільтр **"няма літар — значыць ID"**, які дазваляе ігнараваць тэхнічныя палі пры выбары назвы вакансіі для справаздачы.
- **sheets.service.js**: Справаздачы ў Inbox цяпер максімальна інфарматыўныя: яны ўключаюць код вакансіі `[VAC-XXXX]`, назву і **нумар радка ў табліцы** `(Row: N)`.
- **sheets.service.js**: Выпраўлена крытычная памылка дублявання кода і парушэння балансу дужак, якая прыводзіла да прыпынку сервера.
- **sheets.service.js**: Укаранёна логіка адпраўкі ў Inbox для катэгорый `UPDATE` і `RECRUITER_INFO`, калі дадзеных недастаткова для стварэння поўнай вакансіі (правіла 400 сімвалаў).

### 🤖 AI Service

- **ai.service.js**: Укаранёны **JSON-шчыт** у функцыі `formatTelegramPost`. Цяпер сістэма аўтаматычна блакуе адпраўку паведамленняў у Telegram, калі AI памылкова вярнуў сыры JSON замест прыгожага тэксту.
- **gemini.service.js**: Аптымізаваны `SYSTEM_PROMPT` для лепшай класіфікацыі паведамленняў, якія прыходзяць з

## [2026-05-30] — Разумная сінхранізацыя v2.6: Semantic Hash і падтрымка Telegraph

### ⚙️ Backend & Sync

- **Semantic Hashing**: Укаранёна новая логіка ідэнтыфікацыі вакансій праз хэш (Агенцыя + Назва + Горад). Гэта дазваляе абнаўляць існуючыя вакансіі пры змене тэксту/стаўкі замест стварэння дублікатаў.
- **Google Sheets Fix**: Выпраўлена апрацоўка назваў лістоў з кірыліцай і прабеламі праз дынамічнае выкарыстанне адзіночных двукоссяў у `ranges`.
- **Photo Filtering**: Палепшана функцыя `buildRowText` — цяпер сістэма аўтаматычна ігнаруе спасылкі на фотаздымкі (па ключавых словах у URL), што прадухіляе памылкі HTTP 400.
- **WORK&HUMAN**: Поўная інтэграцыя новай агенцыі (загалоўкі, стоп-маркеры, AI-класіфікацыя).

### 🤖 AI & Enrichment

- **Telegraph Support**: Дададзена аўтаматычнае "разгортванне" спасылак `telegra.ph`. Сістэма цяпер скрапіць тэкст апісання вакансіі прама з Telegraph для аналізу AI.
- **Silent Drive Errors**: Функцыя загрузкі Google Docs цяпер ціха ігнаруе файлы, якія не з'яўляюцца тэкставымі (выявы, PDF), не забіваючы логі памылкамі.
- **addAndSync.js**: Выпраўлена памылка хардакоду агенцыі пры стварэнні новых крыніц для тэсту.

## [2026-05-31] — Бэкенд: Выпраўленне "каннібалізму" вакансій v2.7

### ⚙️ Backend

- **`Vacancy.js`**: Дададзена поле `sheetName` і абноўлены індэкс для ізаляванага аўта-закрыцця вакансій у межах аднаго ліста.
- **`ai.service.js`**: Функцыя `parseVacancyWithAI` абноўлена для падтрымкі поля `sheetName`.
- **`vacancies.js`**: Функцыя `processVacancyMessage` і маршрут `/auto` абноўлены для апрацоўкі поля `sheetName`.
- **`sheets.service.js`**: Укаранёна ізаляцыя аўта-закрыцця па полі `sheetName`. Цяпер вакансіі аднаго ліста не могуць закрываць вакансіі іншага ліста той жа агенцыі.

## [2026-05-31] — Разумная сінхранізацыя v2.8: Логіка UPDATE і палепшаны хэш

### ⚙️ Backend & Sync

- **`vacancies.js`**: Функцыя `processVacancyMessage` і маршрут `/auto` цяпер падтрымліваюць абнаўленне існуючых вакансій па ID. Гэта цалкам спыняе стварэнне дублікатаў.
- **`sheets.service.js`**: Укаранёны "Супер-унікальны хэш" (Агенцыя + Назва + Горад + Праект). Гэта дазваляе сістэме адрозніваць розныя праекты з аднолькавымі агульнымі назвамі (напр. "Активная") у адным горадзе.
- **`sheets.service.js`**: Функцыя сінхранізацыі цяпер здабывае `crmName` і перадае ID існуючай вакансіі ў працэсар для карэктнага абнаўлення дадзеных.

## [2026-05-31] — Разумная сінхранізацыя v3.0: Семантычныя якары і ізаляцыя

### ⚙️ Backend & Sync

- **`sheets.service.js`**: Укаранёна сістэма **"Семантычных якараў"** (`ANCHOR_MAP`). Цяпер для кожнай агенцыі вызначаны ключавыя палі (напр., Назва + CRM + Горад), якія фармуюць унікальны ID вакансіі. Гэта дазваляе вакансіям "пераязджаць" па табліцы без стварэння дублікатаў.
- **`sheets.service.js`**: Поўная ізаляцыя лістаў. У хэш дададзена поле `sheetName`, што спыніла "каннібалізм" вакансій унутры адной агенцыі (напр., Інтрасэрвіс Польша больш не закрывае Опекунок).
- **`sheets.service.js`**: Строгая дэтэкцыя статусаў. Прыярытэт аддадзены першаму слупку. Дададзена падтрымка спецыфічных маркераў: `❌` (Bisar), `FALSE` (Vekos), колер фону (Ralen). Выпадковыя словы "Так" у апісанні больш не ўплываюць на статус.
- **`vacancies.js`**: Функцыя `processVacancyMessage` выпраўлена і аптымізавана. Цяпер яна карэктна абнаўляе існуючыя запісы па `existingId`, захоўваючы гісторыю і код `VAC-XXXX`.
- **`Vacancy.js`**: У схему дададзена поле `sheetName` і абноўлены індэксы для паскарэння ізаляванай сінхранізацыі.

### 📊 Reporting

## [2026-06-01]

- Справаздачы ў Inbox цяпер падзяляюць статыстыку на **Новыя** (`✨`) і **Абноўленыя** (`🔄`), што дае рэкрутэрам дакладнае разуменне змен у табліцах.
- Выпраўлена дэтэкцыя статусу вакансій у Google Sheets (Bug A).
- Дададзены мапінг слупкоў статусу для BISAR, VEKOS, WORK&HUMAN, MRÓWKI і INTRASERVICE.
- Палепшана ўніверсальная праверка стоп-маркераў (❌, STOP, FALSE).
- Выпраўлена памылка сплітынгу ў processVacancyMessage (Bug B).
- Прыбраны заўчасныя return, што дазволіла ствараць некалькі вакансій з аднаго паведамлення/радка.
- Усе фрагменты цяпер захоўваюцца ў базу і адпраўляюцца ў Telegram.
- Выпраўлены Bug C у ai.service.js.
- Дададзена логіка аднаўлення (fallback) для Telegram-пастоў, калі AI вяртае JSON замест тэксту.
- Узмоцнены промпт для фарматавання пастоў.

## [2026-06-02] - v3.1 - Regex & Status Fixes

### Added

- Гнуткі мапінг статусаў для BISAR і VEKOS (ігнараванне ❌ і "false").
- Палепшаны Regex для Telegraph: падтрымка URL-кадыроўкі і ачыстка ад тэхнічных дужак `]`, `)`.

### Fixed

- Памылка 404 пры скрапінгу Telegraph праз абразанне спасылак.
- Дубліраванне вакансій пры паўторнай сінхранізацыі (Semantic Anchoring verified).

## [2026-06-02] - v3.2 - AI Model Optimization & Payload Handling

### Added

- Інтэлектуальны выбар мадэлі AI у залежнасці ад памеру тэксту (`maxChars` у `AI_CHAIN`).
- Аўтаматычны пропуск мадэляў з малым кантэкстным акном (напр. Llama 8b) для вялікіх запытаў (>5000 сімвалаў).
- Дадатковая ачыстка URL Telegraph ад тэхнічных дужак праз `.replace()` для гарантыі паспяховага скрапінгу.

### Fixed

- Памылка `413 Request too large` пры апрацоўцы вельмі доўгіх вакансій (напрыклад, ад MRÓWKI).
- Выключана рызыка "завісання" канвеера пры спробе адправіць занадта вялікі палэйлад у малую мадэль.

### Optimization

- Пацверджана і праверана логіка пропуску скрапінгу вонкавых спасылак для радкоў са статусам `STOP` (эканомія трафіку і часу).

## [2026-06-02] — Сінхранізацыя: Выпраўленне статусаў RALEN і семантычных якараў

### ⚙️ Backend & Sync

- **`sheets.service.js`**: Палепшана дэтэкцыя колеру для агенцыі RALEN. Дададзена падтрымка `themeColor` і `backgroundColorStyle`, што дазваляе бачыць зялёныя і аранжавыя радкі, нават калі яны выкарыстоўваюць тэмавае фарматаванне Google Sheets.
- **`sheets.service.js`**: Укаранёна ачыстка загалоўкаў ад пераносаў радкоў (`\n`) і лішніх прабелаў перад параўнаннем. Гэта выправіла баг, калі ключавыя слупкі (напр. "Название вакансии") не знаходзіліся праз фарматаванне табліцы.
- **`sheets.service.js`**: Выпраўлена праблема "каннібалізму" вакансій RALEN. Семантычны якар пашыраны полем "Ставка", што дазваляе адрозніваць розныя вакансіі ў адным горадзе з аднолькавай назвай.
- **`sheets.service.js`**: Дададзена нумарацыя радкоў у дэбаг-логі (`[Color Debug]`, `[Status Debug]`) для зручнага пошуку праблемных дадзеных у табліцах.
- **`sheets.service.js`**: Функцыя `getRowStatus` цяпер прымае індэкс радка для дакладнай справаздачнасці ў кансолі.

### [2026-06-02] — Фікс вызначэння зялёных радкоў RALEN (Google Sheets)

**Праблема:** Радкі 14–34 табліцы RALEN з зялёным фонам
вызначаліся як неактыўныя (STOP) і прапускаліся пры сінхранізацыі.

**Дыягностыка:**

- Дэбаг-логі паказалі `R:1.000 G:1.000 B:1.000` для зялёных радкоў
- Скрыпт `debugConditional.js` выявіў: умоўнага фарматавання няма;
  зялёны колер запісаны як `{"green":1}` без палёў `red` і `blue`
- Прычына: код выкарыстоўваў `bg.red ?? 1` — адсутны канал
  атрымліваў `1` (белы), хаця правільна павінен быць `0`

**Выпраўленне (`sheets.service.js`):**

- Фолбэк для адсутных RGB-каналаў зменены з `?? 1` на `?? 0`
  для абодвух крыніц: `backgroundColor` і `backgroundColorStyle.rgbColor`
- Цяпер `{"green":1}` правільна чытаецца як R:0 G:1 B:0 → ACTIVE

**Вынік:** 21 зялёны радок цяпер карэктна апрацоўваецца.

- Выпраўлена праблема з 404 памылкамі пры скрапінгу калонкі "link na strone" у табліцах INTRASERVICE.
- Дададзены спіс ігнаравання для вонкавага скрапінгу ў `sheets.service.js`.
- Скрапер цяпер вяртае `null` пры памылках доступу, не забруджваючы тэкст вакансіі.
- Палепшана распазнаванне спасылак Google Drive у `gemini.service.js`.

### [2026-06-03]

- Выпраўлена крытычная памылка ў `sheets.service.js`: цяпер сінхранізацыя цалкам спыняецца пры атрыманні памылкі ад AI (Cooldown).
- Гэта прадухіляе памылковае закрыццё актыўных вакансій, якое адбывалася з-за пропуску радкоў у цыкле.
- Палепшана апрацоўка сігналу `STOP_ALL` у глабальным цыкле сінхранізацыі.

## [2025-06-04] - Паляпшэнне сінхранізацыі Google Sheets

### Backend

- **Файл:** `backend/services/sheets.service.js`
- **Змены:** Абноўлена логіка вызначэння статусу радка ў функцыі `getRowStatus`.
- **Апісанне:** Уведзена строгая праверка для агенцыі **MRÓWKI**: калі слупок статусу ідэнтыфікаваны, але ячэйка пустая, радок аўтаматычна атрымлівае статус `STOP`.
- **Чаму гэта важна:** Гэта выпраўляе памылку апрацоўкі аб'яднаных ячэек у Google Sheets. Раней, калі некалькі радкоў былі аб'яднаны пад адным маркерам "стоп", API вяртала значэнне толькі для першага радка, а астатнія памылкова лічыліся актыўнымі. Цяпер увесь блок будзе карэктна ігнаравацца або закрывацца.
- **Статус:** Гатова да тэставання.

## [2025-06-04] — Бэкенд: Укараненне сістэмы адсочвання крыніц (v3.5)

### ⚙️ Backend

- **`Vacancy.js`**: Дададзена поле `sourceType` (enum: `spreadsheet`, `viber`, `telegram`, `site`, `trello`, `airtable`, `manual`) з індэксацыяй для фільтрацыі.
- **`vacancies.js`**: Функцыя `processVacancyMessage` мадэрнізавана для прыёму і захавання тыпу крыніцы.
- **`sheets.service.js`**: Укаранёна аўтаматычная пазнака `sourceType: "spreadsheet"` для ўсіх вакансій, якія прыходзяць з Google Sheets.
- **`inbox.js`**: Наладжаны мапінг крыніц для паведамленняў з мэсэнджараў: `telegram` (для юзербота) і `viber` (для астатніх).
- **Вынік**: Зараз кожная вакансія мае "пашпарт крыніцы", што дазваляе дакладна кантраляваць паток даных і рыхтаваць глебу для дынамічных іконак на фронтэндзе.

## [2025-06-04] — Бэкенд: Семантычная дэдуплікацыя і пашыраная фільтрацыя (v3.7)

### ⚙️ Backend

- **`vacancies.js` (Semantic Sync)**: Укаранёна логіка "4 параметраў" (Агенцыя + Лакацыя + Брэнд + Загаловак) для аўтаматычнага абнаўлення вакансій з мэсэнджараў. Цяпер паведамленні з Viber/Telegram не ствараюць дублікаты, а "асвяжаюць" існуючыя запісы (абнаўляецца `updatedAt` і `sourceType`).
- **`vacancies.js` (Region Fix)**: Выпраўлены баг фільтрацыі ваяводстваў. Выкарыстанне строгага Regex `(^|,)\s*...` дазволіла ізаляваць "Śląskie" ад "Dolnośląskie".
- **`vacancies.js` (Date Filters)**: У маршрут GET `/` дададзена падтрымка параметраў `startDate` і `endDate`. Рэалізавана карэктная апрацоўка часавых межаў (да 23:59:59 для канчатковай даты).

## [2026-06-05] — Бэкенд: Стабілізацыя сінхранізацыі і гібрыдная дэдуплікацыя (v3.9)

### ⚙️ Backend

- **`sheets.service.js` (Hash Stability)**: Мадыфікавана логіка генерацыі `rowHash`. Уведзена поўная нармалізацыя "Якара" (anchorText) праз прывядзенне да ніжняга рэгістру і выдаленне ўсіх прабелаў. Гэта забяспечвае стабільнасць ідэнтыфікатара вакансіі пры выпадковых зменах фарматавання ў Google Sheets.
- **`vacancies.js` (Hybrid Search)**: Функцыя `processVacancyMessage` мадэрнізавана для падтрымкі двухслаёвага пошуку дублікатаў. Калі супадзенне па хэшы адсутнічае, сістэма выконвае семантычную праверку па 4 параметрах (Агенцыя + Лакацыя + Брэнд + Загаловак). Гэта дазваляе табліцам "пазнаваць" і абнаўляць старыя запісы, якія не мелі хэша.
- **`fix-duplicates.js` (Utility)**: Распрацаваны і ўкаранёны службовы скрыпт для інтэлектуальнай ачысткі базы ад дублікатаў і масавай карэкцыі поля `sourceType` для вакансій вядомых агенцый, што вярнула дакладнасць Smart-лічыльнікам на фронтэндзе.

## [2026-06-05] — Бэкенд: Аўтаматызацыя дат, паляпшэнне фільтрацыі і стабілізацыя хэшаў (v4.0)

### ⚙️ Backend

- **`Vacancy.js` (Timestamps)**: У схему Mongoose дададзена опцыя `{ timestamps: true }`. Гэта забяспечвае аўтаматычнае стварэнне і абнаўленне палёў `createdAt` і `updatedAt`, што крытычна для карэктнай працы часавых фільтраў. Ручное поле `createdAt` выдалена для пазбягання канфліктаў.
- **`vacancies.js` (Hybrid Date Filter)**: Маршрут `GET /` мадэрнізаваны для гібрыднай праверкі дат. Цяпер фільтр `startDate/endDate` шукае супадзенні адначасова ў палях `createdAt` і `updatedAt` праз аператар `$or`. Гэта выправіла баг "пустога спіса", калі фронтэнд не мог знайсці вакансіі па даце абнаўлення.
- **`vacancies.js` (Source Filter)**: Дададзена серверная падтрымка фільтрацыі па полі `sourceType`. Цяпер параметры `viber,telegram,spreadsheet,manual` карэктна апрацоўваюцца базай дадзеных.
- **`sheets.service.js` (Sheet Name Normalization)**: Пры генерацыі `rowHash` назва ліста (`sheetName`) цяпер праходзіць нармалізацыю (прывядзенне да ніжняга рэгістру). Гэта ліквідавала шчыліну для стварэння дублікатаў пры змене рэгістра назвы ўкладкі ў Google Sheets (напрыклад, розніца паміж "Вакансії" і "ВАКАНСІЇ").

## [2026-06-05] — Бэкенд: "Мяккая" дэдуплікацыя і ігнараванне рэгістра (v4.3)

### ⚙️ Backend

- **`vacancies.js` (Case-Insensitive Search)**: Логіка семантычнага пошуку дублікатаў пераведзена на выкарыстанне рэгулярных выразаў з флагам `i`. Цяпер сістэма паспяхова знаходзіць і абнаўляе існуючыя вакансіі, нават калі AI змяніў рэгістр літар у загалоўку ці лакацыі (напрыклад, "Готель" vs "готель"). Гэта забяспечвае сувязь паміж старымі "ручнымі" запісамі і новымі радкамі з табліц.
- **`fix-duplicates.js` (Smart Cleanup)**: Скрыпт ачысткі мадэрнізаваны для падтрымкі рэгістранезалежнага пошуку. Гэта дазволіла эфектыўна "схлопваць" старыя запісы з новымі дублікатамі, якія ўзніклі з-за розніцы ў напісанні, забяспечваючы чысціню базы дадзеных.

## [2026-06-06] — Бэкенд: Ізаляцыя крыніц, ачыстка інбокса і абарона сінхранізацыі (v4.5)

### ⚙️ Backend

- **`vacancies.js`**: Семантычны fallback-пошук дублікатаў цяпер фільтруе па полі `sourceType`. Вакансіі з розных крыніц (telegram, viber, spreadsheet) больш не "пазнаюць" адна адну і не перазапісваюцца. Кожная крыніца абнаўляе толькі свае ўласныя запісы.

- **`sheets.service.js`**: Спрошчана справаздачнасць у Inbox. Асобныя UPDATE-паведамленні цяпер ствараюцца толькі для кароткіх радкоў табліцы (< 400 сімвалаў). Доўгія радкі (> 400 сімвалаў), якія AI класіфікаваў як UPDATE, фіксуюцца толькі ў агульнай справаздачы агенцыі — без дадатковага смецця ў Inbox.

- **`index.js`**: Умова страхавання сінхранізацыі (`runSyncWithInsurance`) ужмоцнена. Замест праверкі "ці была сінхранізацыя сёння з пачатку сутак" — праверка "ці была яна апошнія 6 гадзін". Гэта прадухіляе паўторныя запускі пры некалькіх дэплоях Render у адзін дзень.

## [2026-06-06] — Бэкенд: Дэдуплікацыя, ачыстка і стабілізацыя (v4.6)

### ⚙️ Backend

- **`sheets.service.js`**: Выпраўлены дублікат Telegram-посту пры спліце фрагментаў.
  Цяпер другі і наступныя фрагменты аднаго радка табліцы абнаўляюць першую
  створаную вакансію (праз `firstSavedId`), а не ствараюць новыя. У справаздачу
  і Telegram ідзе толькі адзін запіс на радок.

- **`sheets.service.js`**: Службовыя радкі табліц (каардынатары, кантакты) больш
  не трапляюць у Inbox як UPDATE. Дадана праверка на наяўнасць прыкмет вакансіі
  (зарплата або назва пасады) перад стварэннем паведамлення ў Inbox.

- **`sheets.service.js`**: `catch`-блок `syncSheetVacancies` цяпер вяртае
  `"STOP_ALL"` пры AI-памылках (`AI_COOLDOWN`, `ALL_AI_MODELS_FAILED`).
  Раней exception вяртаў `undefined` і `syncAllSheets` працягваў цыкл
  нават пры недаступным AI.

- **`vacancies.js`**: Выпраўлена сартаванне ў `generateVacancyCode`.
  Замест `vacancyCode: -1` (радковае сартаванне, "VAC-0099" > "VAC-0100")
  цяпер выкарыстоўваецца `createdAt: -1`. Выключана магчымасць скачка
  нумара назад пры пераходзе праз круглыя лічбы.

- **`vacancies.js`**: Семантычны fallback-пошук дублікатаў фільтруе па `sourceType`.
  Вакансіі з розных крыніц (telegram, viber, spreadsheet) больш не перазапісваюць
  адна адну. Кожная крыніца абнаўляе толькі свае ўласныя запісы.

- **`gemini.service.js`**: Ключ кэша павялічаны з 250 да 400 сімвалаў.
  Зніжана верагоднасць хібнага супадзення для радкоў з аднолькавым пачаткам.

- **`SheetSource.js`**: Выдалена мёртвае поле `processedHashes`. Дэдуплікацыя
  цалкам перанесена на `Vacancy.sourceHash`.

- **`ai.service.js`**: З чорнага спісу `cleanData` выдалены значэнні `"99"` і `99`.
  Яны маглі з'яўляцца ў рэальных дадзеных (колькасць месцаў, нумары ў назвах).
- Аптымізавана апрацоўка Inbox: кароткія апдэйты (UPDATE), інфармацыйныя радкі (INFO) і закрыцці вакансій (STOP) цяпер групуюцца ў адно паведамленне на кожную табліцу.
- Дададзена візуальнае афармленне групавых паведамленняў з іконкамі і нумарамі радкоў.
- Уведзена абмежаванне на даўжыню групавога паведамлення (4000 сімвалаў).

### v5.1 (07.06.2026) — Аптымізацыя Trello і Агрэгацыя Inbox

- **Дэдуплікацыя і эканомія токенаў**: Укаранёна праверка `originalText` перад запускам AI-ланцужка. Калі змест карткі Trello не змяніўся, сінхранізацыя прапускае яе, што цалкам ліквідуе залішнія выклікі Vertex AI/Groq і паскарае апрацоўку.
- **Trello Дайджэст**: Рэалізавана агрэгацыя інфармацыйных картак (спісы "ВАЖНО", "ОСОБИСТЕ ПРОХАННЯ") у адно групавое паведамленне для Inbox. Цяпер замест дзясяткаў асобных запісаў рэкрутэр атрымлівае адзін структураваны "Гарачы дайджэст" па кожнай дошцы.
- **Паляпшэнне справаздачнасці**: Дададзены масіў `details` у `trello.service.js` для дэталёвага логавання новых (`✨`) і абноўленых (`🔄`) вакансій у кансолі і гісторыі сінхранізацыі.
- **Стабільнасць**: Выпраўлена праблема паўторнага парсінгу ўжо існуючых вакансій пры кожным запуску `addAndSync.js`.

## [2026-06-07] — Trello: Выпраўленне дублікатаў пры сінхранізацыі (v5.2)

### ⚙️ Backend

- **`trello.service.js`**: Выпраўлена крытычная памылка дублявання вакансій пры паўторным скане дошак. Цяпер `existingVacancy._id` перадаецца ў `processVacancyMessage` (было `null`), што гарантуе абнаўленне існуючай вакансіі замест стварэння новай — ідэнтычна логіцы табліц.
- **`trello.service.js`**: Дададзены лічыльнік `ignored` у аб'ект `stats`. Раней выклікаў `NaN` у справаздачы пры ігнараванні нязменных картак.
- **`trello.service.js`**: Дададзена зменная `firstSavedId` для падтрымкі сплітавання картак (на выпадак некалькіх фрагментаў з адной карткі) — па аналогіі з табліцамі.
- **`trello.service.js`**: Пашыраны справаздачны лог сінхранізацыі — цяпер уключае лічыльнікі `updated` і `ignored`.

## [2026-06-07] — Sheets: Падтрымка аб'яднаных ячэек (v5.3)

### ⚙️ Backend

- **`sheets.service.js`**: Дададзена функцыя `resolveMergedCells`. Цяпер пры
  сінхранізацыі табліц з аб'яднанымі ячэйкамі (напр. BISAR, дзе лакацыя
  займае 11 радкоў) значэнне першага радка аб'яднання аўтаматычна
  распаўсюджваецца на ўсе радкі блока. Гэта выправіла памылкову класіфікацыю
  пустых радкоў як ACTIVE і апрацоўку радкоў-каардынатараў як вакансій.
- **`sheets.service.js`**: Здабыванне `merges` з адказу Google Sheets API
  (`response.data.sheets[0].merges`) дададзена адразу пасля загрузкі `rowData`.
  Дадатковых запытаў да API не патрабуецца — дадзеныя ўжо прысутнічаюць у
  існуючым адказе.

## [2026-06-08] — Sheets: Выпраўленне апрацоўкі BISAR і прыхаваных радкоў (v5.4)

### ⚙️ Backend

- **`sheets.service.js`**: Для агенцыі BISAR пустая ячэйка ў слупку статусу
  цяпер лічыцца STOP (аналагічна MRÓWKI). Гэта выключае апрацоўку радкоў
  без пазначанага статусу (напр. радкі 33, 63, 87).
- **`sheets.service.js`**: Слупок "Координатори" цяпер ігнаруецца пры
  пабудове тэксту вакансіі для BISAR. Гэта спыніла стварэнне дублікатаў
  для блокаў дзе адна вакансія мае некалькіх каардынатараў у асобных радках
  (напр. радкі 19-22 RZESZÓW).
- **`sheets.service.js`**: Дададзена праверка `hiddenByParent` у цыкл
  апрацоўкі радкоў. Цяпер радкі схаваныя праз групоўку (collapse) таксама
  прапускаюцца — выправіла апрацоўку прыхаваных радкоў MRÓWKI.

### 🔧 Выпраўленне рэгулярных выразаў і паляпшэнне лагавання інбокса

**Message Filters (utils/messageFilters.js):**

- **Выдаленне агрэсіўных рэгулярак**: Выдалены шкодныя рэгулярныя выразы з масіва `RECRUITER_CHAT_NOISE` (`/актуальна\s+вакансія/i`, `/чи\s+є\s+(ще|місця|вільні)/i`, `/є\s+актуальні\s+дати/i`), якія памылкова адсякалі жывыя вакансіі на стадыі папярэдняй фільтрацыі.
- **Новы дэбаг-хелпер**: Дададзена функцыя `getMatchingIgnoreRegex(text)`, якая вяртае тэкст канкрэтнай рэгуляркі, якая спрацаваў на паведамленні, для дакладнага кантролю за кошыкам.

**Inbox Pipeline (routes/inbox.js):**

- **Дэбаг-лагі пры ігнараванні**: Абнавіўся блок апрацоўкі шуму ў функцыі `runInboxPipeline`. Цяпер пры адхіленні паведамлення па Regex у кансоль і ў поле `archiveReason` базы дадзеных запісваецца дакладны радок і назва рэгулярнага выраза.

### Backend

- Выпраўлена праблема "фальшывых" апдэйтаў у `processVacancyMessage`: цяпер `updatedAt` не змяняецца, калі тэкст вакансіі ідэнтычны існуючаму.
- Уведзена строгая фільтрацыя па датах у маршруце `GET /vacancies`: час фіксуецца на 00:00:00 - 23:59:59, пошук ідзе толькі па полі `updatedAt`.
## [2026-06-12] — Этап 1 і 2: Дыягностыка, стабільнасць AI і лекаванне JSON

### Дададзена
- Поле `isLowQuality` у мадэль `Vacancy` для адсочвання вакансій з адрамантаваным JSON.
- Дэталёвае лагіраванне `[AI Request]` у `ai.service.js` (мадэль, правайдэр, памер тэксту).
- Вывад канкрэтнай прычыны і рэгекса пры адхіленні паведамлення ў шум (`inbox.js`).

### Зменена
- Рэфактарынг `shouldIgnoreMessage` у `messageFilters.js`: цяпер вяртае аб'ект з падрабязнай прычынай ігнаравання.
- Мадэрнізацыя `repairJson` у `ai.service.js`: укаранёна разумная логіка закрыцця лапак і балансіроўкі дужак (Brute Force Repair).
- Функцыя `executeAIRequest` пераведзена на фармат адказу `{ data, isLowQuality }`.
- Усе функцыі ў `ai.service.js` (`mergeWithTemplate`, `identifyTemplate`, `parseVacancyWithAI`, `updateVacancyWithAI`, `compareVacanciesWithAI`) адаптаваны пад новы фармат адказу AI.
- У парсер вакансій дададзена дынамічная інструкцыя па ігнараванні дат прыезду, старэйшых за бягучую дату (фільтрацыя застарэлых дадзеных).

### Выпраўлена
- Памылка `deleteWebhook` пры запуску бота на Render, якая магла выклікаць крах сервера.
- Патэнцыйны `Chain Crash` пры апрацоўцы бітых JSON-адказаў ад Lite-мадэляў.
- Прыбрана дубляванне запытаў да AI у функцыі `mergeWithTemplate`.
## [2026-06-12] — Этап 1, 2 і 3: Стабільнасць, Дыягностыка і Telegram "Pro"

### Дададзена
- Поле `isLowQuality` у мадэль `Vacancy` для маркіроўкі вакансій з "лекаваным" JSON.
- Падтрымка падвойнага фарматавання пастоў: Поўная + Кароткая версіі адным цыклам.
- Метададзеныя `🆔 ID | 🏢 Агенцыя | 🏭 Брэнд` у пачатку кожнага паведамлення.
- Аўтаматычнае даданне кантакту рэкрутэра `@InnaNovaWork` у канец пастоў.
- Дэталёвае лагіраванне AI-запытаў (мадэль, памер тэксту) і прычын адхілення шуму (назва рэгекса).

### Зменена
- Мадэрнізавана функцыя `repairJson`: укаранёны "Brute Force" рамонт (аўтаматычнае закрыццё лапак і дужак).
- Функцыя `executeAIRequest` цяпер вяртае аб'ект `{ data, isLowQuality }`.
- Промпт парсера цяпер ігнаруе даты прыезду, старэйшыя за бягучы дзень (2026 год).
- Функцыя `sendToTelegram` навучана разбіваць адказ AI па маркеру `=== SPLIT ===` і адпраўляць два асобныя паведамленні.

### Выпраўлена
- Крах сервера пры запуску на Render праз ігнараванне памылак `deleteWebhook`.
- Памылкі апрацоўкі бітых JSON-адказаў ад Lite-мадэляў.
- Выключана дубляванне запытаў да AI у функцыі `mergeWithTemplate`.
## [2026-06-12] — Этап 3: Фіналізацыя Telegram Pro і стабільнасць AI

### Дададзена
- Падвойнае фарматаванне пастоў (Full + Short) праз маркер `=== SPLIT ===`.
- Метададзеныя `🆔 ID | 🏢 Агенцыя | 🏭 Брэнд` у пачатку кожнага паведамлення.
- Аўтаматычнае даданне кантакту рэкрутэра `@InnaNovaWork` у канец пастоў.
- Дынамічная дата ў промпце для ігнаравання застарэлых дат прыезду.

### Зменена
- Функцыя `formatTelegramPost` цалкам перапісана: захаваны Privacy Shield і JSON-Шчыт v3.0, дададзена логіка падвойнага паста.
- Функцыя `sendToTelegram` цяпер апрацоўвае масіў пастоў і адпраўляе іх асобнымі паведамленнямі з паўзай.

### Выпраўлена
- Памылка `(text || "").trim is not a function` праз няправільную апрацоўку аб'екта ад AI.
- Уцечка прыватных дадзеных (бонусы рэкрутэраў) у AI праз выпраўленне парадку апрацоўкі ў `formatTelegramPost`.
- Дубляванне запытаў да AI у функцыі `mergeWithTemplate`.
## [2026-06-16] — Бэкенд: Фільтрацыя па зменах і ўніфікацыя кантактаў

### ⚙️ Backend
- **`Vacancy.js`**: Дададзена булева поле `schedule.onlyDayShifts` для маркіроўкі вакансій без начных змен.
- **`vacancies.js`**: Маршрут `GET /` абноўлены для падтрымкі фільтрацыі па `onlyDayShifts`.
- **`ai.service.js`**: 
  - У `SYSTEM_INSTRUCTION` дададзена лагічная праверка графіка: `onlyDayShifts` ставіцца ў `true` толькі пры гарантаванай адсутнасці начных змен (22:00–06:00).
  - У `FORMAT_PROMPT` замацавана жорсткая маска кантактаў для абодвух тыпаў пастоў (Full/Short).
  - Выпраўлены радок транспарту ў кароткім посце, каб пазбегнуць знікнення дадзеных.
  ## [18.06.2026]

### Backend & AI Logic
- **ai.service.js**: 
    - Абноўлены `FORMAT_PROMPT` для Telegram. 
    - У кароткую версію паста (Short Post) дададзены блок "Графік" (`schedule.description` і `hoursRange`).
    - Строга ўніфікаваны футар з кантактамі: `📲 Контактуйте: @InnaNovaWork +48 780 770 745 Інна`.
- **vacancies.js**: 
    - Укаранёна логіка **AI-Powered Semantic Match (v4.7)** для пошуку дублікатаў.
    - Цяпер пры апрацоўцы паведамленняў з чатаў (дзе няма `sourceHash`) сістэма шукае патэнцыйных "кандыдатаў" па палях `agencyName`, `location` і `brand`.
    - Для фінальнага вердыкту выкарыстоўваецца функцыя `compareVacanciesWithAI`, што дазваляе адрозніваць розныя пасады на адным прадпрыемстве і ігнараваць тэкставыя варыяцыі адной і той жа вакансіі.

### Bug Fixes
- Выпраўлена праблема "французскай пасткі", калі кароткія паўторныя анонсы стваралі новыя ID вакансій замест абнаўлення існуючых.
### Backend: Geocoding & Data Integrity
- **Models**: 
    - Абноўлена схема `Vacancy.js`: дададзена поле `locationCoords` для захавання шыраты і даўгаты.
    - Створана мадэль `LocationCache.js` для кэшавання вынікаў геакадзіравання (TTL 30 дзён).
- **Services**:
    - Створаны `location.service.js`: інтэграцыя з OpenStreetMap (Nominatim API) для ператварэння назваў гарадоў у каардынаты.
- **Routes**:
    - Рэфактарынг `/system/cleanup-locations` у `vacancies.js`:
        - Палепшана нармалізацыя назваў гарадоў (падтрымка складаных назваў з двух слоў і дэфісаў).
        - Укаранёна аўтаматычнае геакадзіраванне падчас ачысткі базы.
    - Інтэгравана атрыманне каардынат пры стварэнні вакансій (праз AI і ўручную).
- **Scripts**:
    - Падрыхтаваны скрыпт `seedCoords.js` для масавай міграцыі каардынат існуючых запісаў.
    ## [20.06.2026]

### Backend: Data Structure & AI Logic
- **Vacancy.js**: У схему вакансіі ў аб'ект `accommodation` дададзена булева поле `isFree` (па змаўчанні `false`). Гэта дазваляе захоўваць прыкмету бясплатнага жытла ў структураваным выглядзе.
- **ai.service.js**:
    - Абноўлены `SYSTEM_INSTRUCTION`: AI цяпер атрымаў строгае правіла вызначаць бясплатнасць жытла па ключавых словах ("безкоштовне", "0 zł", "за рахунок роботодавця") і запісваць вынік у поле `isFree`.
    - У функцыі `processSingle` дададзены мапінг гэтага поля для карэктнай апрацоўкі вынікаў парсінгу.
- **vacancies.js**:
    - Роўт атрымання вакансій (`GET /`) аптымізаваны: цяпер фільтрацыя па бескаштоўнаму жытлу адбываецца праз хуткі запыт `accommodation.isFree: true` замест цяжкага пошуку па тэксце.
    - Роўт ачысткі (`/system/cleanup-locations`) пашыраны: дададзена логіка аўтаматычнага вызначэння бясплатнасці для ўсіх існуючых вакансій у базе на аснове аналізу палёў `type` і `details`.
    ## [20.06.2026]

### Bug Fixes & Finalization
- **vacancies.js**: 
    - Выпраўлена крытычная памылка вобласці бачнасці (ReferenceError), якая выклікала крах дэплою. 
    - Логіка аўтаматычнага вызначэння бясплатнага жытла (`isFree`) канчаткова інтэгравана ў цыкл ачысткі базы.
- **ai.service.js**: 
    - Падцверджана і зафіксавана карэктная праца функцыі `normalizeLocation` без сінтаксічных дубляў.
- **Data Integrity**: 
    - Падрыхтавана глеба для паспяховага запуску ачысткі 500+ вакансій з аўтаматычнай прастаўкай каардынат і булевага поля `isFree`.
    ### 20.06.2026 — Удасканаленне логікі бясплатнага жытла (isFree)

- **Backend (ai.service.js):** Поўнасцю перапрацавана функцыя `isHousingFree`. Цяпер яна працуе больш інтэлектуальна:
  - Дададзена дэтэкцыя камунальных плацяжоў (маркер "мунальн").
  - Укаранёна праверка на ўтрыманні з зарплаты ("із зарплати", "вираховується", "самостійно").
  - Дададзены пошук перыядычных выдаткаў (напрыклад, сумы за месяц ці за суткі).
  - Рэалізавана "разумная" апрацоўка заставаў: калі ў тэксце ёсць сума, але яна пазначана як вяртаемая застава (дэпазіт), жытло застаецца ў статусе `isFree: true`.
- **Database:** Перазапушчаны скрыпт `seedIsFree.js`. У выніку праверкі 536 запісаў было выпраўлена 17 вакансій (пераведзены з true ў false), што пацвердзіла эфектыўнасць новай логікі.
### 22.06.2026 — Інтэграцыя з Airtable (Manpower, Progres, Job Impulse)

- **Database:**
  - У мадэль `Vacancy.js` дададзена поле `airtableId` для сінхранізацыі запісаў.
  - Створана мадэль `AirtableSource.js` для захоўвання налад кожнай базы (ID, дазволеныя калонкі, правілы актуальнасці).
- **Backend:**
  - Распрацаваны `airtable.service.js`, які аўтаматычна вызначае Kanban-калонку запісу і прымяняе бізнес-логіку агенцый.
  - Рэалізавана правіла "Актуальность: ДА" для Job Impulse і аўтаматычнае закрыццё вакансій Manpower з калонкі "не активні тимчасово".
  - Дададзены механізм аўта-закрыцця вакансій, якія былі выдалены з Airtable.
- **Sync:** Запушчана першая сінхранізацыя Grupa Progres, паспяхова апрацаваны першыя запісы.
### 22.06.2026 — Уніфікацыя AI-пайплайна для Airtable і Trello

- **AI Pipeline:**
  - Укаранёны этап **Stage 1 (Gemini)** для `airtable.service.js` і `trello.service.js`. Цяпер вакансіі з гэтых крыніц праходзяць інтэлектуальную ачыстку і пераклад перад парсінгам.
  - Выдалены жорстка прапісаныя беларускія пазнакі (`ЗАГАЛОВАК:`, `АПІСАННЕ:`, `ДЭТАЛІ:`), якія засмечвалі `rawText`.
  - У поле `originalText` цяпер захоўваецца чысты "сыры дамп" палёў для дакладнай дэдуплікацыі.

- **Airtable Service Fixes:**
  - **Case-Insensitive Matching:** Выпраўлены пошук Kanban-калонак (цяпер "Вроцлав" і "вроцлав" супадаюць).
  - **Job Impulse Logic:** Дададзены гнуткі пошук поля актуальнасці (падтрымка "Актуальность проекта") для карэктнай прастаўкі статусу `closed`.
  - **Split Support:** Выпраўлена праблема "Пропуск абнаўлення" пры разбіцці адной карткі на некалькі вакансій (дададзены індэкс фрагмента ў `sourceHash`).

- **Trello Service:**
  - Поўная сінхранізацыя логікі з Airtable і Sheets: выкарыстанне `analyzeAndCompareWithGemini` для атрымання чыстага ўкраінскага тэксту.
  ### 22.06.2026 — Укараненне «Разумнага буфера» і аўтаматызацыя сінхранізацыі

- **Database & Models:**
  - У мадэль `Vacancy.js` дададзены новы статус `pending_ai`. Гэта дазваляе захоўваць вакансіі, якія патрабуюць паўторнай апрацоўкі больш магутнай мадэллю.

- **AI Service & Processing:**
  - **Smart Buffer:** У `vacancies.js` укаранёна логіка «Разумнага буфера». Калі вакансія апрацоўваецца Lite-мадэллю (напр. пры лімітах Google), яна атрымлівае статус `pending_ai` і не адпраўляецца ў Telegram.
  - **Model Tracking:** AI-сэрвіс цяпер вяртае назву выкарыстанай мадэлі (`modelUsed`), што дазваляе кантраляваць якасць парсінгу.
  - **Force Full Mode:** Дададзена магчымасць прымусовага выкарыстання поўных мадэляў для апрацоўкі чаргі.

- **Queue Management:**
  - Створана функцыя `retryPendingVacancies`, якая аўтаматычна спрабуе «даціснуць» вакансіі з чаргі праз Flash-мадэль.
  - У `addAndSync.js` наладжаны паслядоўны запуск: Ачыстка чаргі -> Sheets -> Trello -> Airtable.
  - Увесь цыкл сінхранізацыі пастаўлены на аўтаматычны паўтор кожныя 4 гадзіны.

- **Airtable Fixes:**
  - Выпраўлена памылка `i is not defined` у цыкле фрагментаў.
  - Унікалізаваны `sourceHash` для выпадкаў, калі адна картка Airtable разбіваецца на некалькі вакансій.
  ### 23.06.2026 — Фіналізацыя глабальнай сінхранізацыі і «Разумнага буфера»

- **Automation & Orchestration:**
  - Файл `index.js` перапрацаваны ў адзіны цэнтр кіравання. Цяпер кожныя 4 гадзіны запускаецца поўны цыкл: **Ачыстка чаргі -> Google Sheets -> Trello -> Airtable**.
  - Парог «страхоўкі» (`INSURANCE`) зніжаны да 3.5 гадзін, што дазваляе сістэме стабільна працаваць у 4-гадзінным рытме нават пасля перазагрузак сервера.

- **Data Quality & Filtering:**
  - У `airtable.service.js` і `trello.service.js` укаранёны жорсткія фільтры даўжыні:
    - `< 200 сімвалаў`: ігнаруецца (смецце).
    - `200–400 сімвалаў`: адпраўляецца ў Inbox як `UPDATE` (кароткія анонсы).
    - `> 400 сімвалаў`: поўная апрацоўка праз AI.
  - Гэта цалкам вырашыла праблему стварэння «пустых» вакансій (напр. VAC-1363).

- **AI Stability:**
  - У `ai.service.js` дададзены флаг `fullModelOnly`. Цяпер вакансіі з чаргі (`pending_ai`) ігнаруюць Lite-мадэлі і чакаюць вызвалення Flash/Llama.
  - Паўзы паміж запытамі павялічаны да **6 секунд**, што забяспечвае стабільную працу ў межах лімітаў Vertex AI (RPM).
  - Укаранёны «Стоп-кран»: калі AI сыходзіць у Cooldown, сінхранізацыя бягучай крыніцы спыняецца адразу, не ствараючы памылак у базе.

- **Bug Fixes:**
  - Выпраўлена крытычная памылка дубліравання: пры паўторнай апрацоўцы чаргі сістэма цяпер бачыць запісы са статусам `pending_ai` і абнаўляе іх, а не стварае новыя VAC-коды.
  - Уніфікаваны пошук калонак у Airtable (цяпер ігнаруецца рэгістр літар).
  ### 23.06.2026 — Укараненне гібрыднага Airtable-скрапера і поўная аўтаматызацыя

- **Airtable Integration (Hybrid Mode):**
  - Распрацаваны `airtableScraper.service.js`, які імітуе паводзіны браўзера для атрымання даных з публічных (Shared) баз. Гэта дазволіла абысці памылку 401 для агенцый MANPOWER і JOB IMPULSE.
  - У `airtable.service.js` укаранёна гібрыдная логіка: PROGRES працуе праз афіцыйны API, астатнія — праз скрапер.
  - Дададзены Share ID у мадэль `AirtableSource.js` і налады `seedAirtable.js`.

- **Data Quality & Smart Filtering:**
  - Укаранёны трохузроўневы фільтр даўжыні для Airtable і Trello:
    - `< 200 сімвалаў`: поўнае ігнараванне (тэхнічны шум).
    - `200–400 сімвалаў`: адпраўка ў Inbox як `UPDATE` (кароткія анонсы).
    - `> 400 сімвалаў`: поўны AI-пайплайн.
  - Гэта цалкам спыніла стварэнне «пустых» вакансій.

- **AI Stability & Queue:**
  - Зафіксавана логіка «Разумнага буфера»: вакансіі ад Lite-мадэляў атрымліваюць статус `pending_ai` і чакаюць рэтраю праз Flash-мадэль.
  - Укаранёны «Стоп-кран»: сінхранізацыя спыняецца пры дасягненні лімітаў AI (Cooldown), каб не пладзіць памылкі.
  - Паўзы паміж запытамі павялічаны да 6 секунд для стабільнасці RPM.

- **Orchestration:**
  - `index.js` наладжаны на паслядоўны запуск усіх крыніц кожныя 4 гадзіны.
  - Выпраўлена памылка дубліравання вакансій пры паўторнай апрацоўцы чаргі.
  ### 23.06.2026 — Поўная стабілізацыя бэкенда і AI-пайплайна

- **AI Queue & Buffer:**
  - Пацверджана праца логікі `pending_ai`: вакансіі ад Lite-мадэляў аўтаматычна трапляюць у чаргу і не спамяць у Telegram.
  - Паспяхова пратэставаны механізм `retryPendingVacancies`: сістэма аўтаматычна «даціскае» вакансіі з чаргі праз Full-мадэлі (Flash/Llama) пры наступным цыкле.
  - Укаранёны жорсткі прыярытэт мадэляў для рэтраяў (ігнараванне Lite).

- **Airtable Scraper:**
  - Выпраўлены эндпоінт унутранага API Airtable (`read` замест `readSharedViewData`), што вырашыла праблему 404 пры скрапінгу публічных баз.

- **Automation:**
  - Глабальны цыкл сінхранізацыі (кожныя 4 гадзіны) працуе стабільна, паслядоўна апрацоўваючы ўсе крыніцы: **Retry -> Sheets -> Trello -> Airtable**.
  ### 24.06.2026 — Укараненне логікі «Resume» і фіналізацыя цыкла сінхранізацыі

- **Sync Continuity (Resume Logic):**
  - У `sheets.service.js`, `airtable.service.js` і `trello.service.js` укаранёна логіка разумнага пропуска. Цяпер сістэма прапускае запіс толькі калі тэкст супадае І статус ужо `active`. 
  - Калі вакансія знаходзіцца ў статусе `pending_ai`, яна больш не ігнаруецца асноўнымі сканерамі, што гарантуе яе даапрацоўку Full-мадэллю пры першай магчымасці.

- **Pipeline Unification:**
  - Усе крыніцы дадзеных прыведзены да адзінага стандарту апрацоўкі праз `processVacancyMessage`.
  - Зафіксавана паслядоўнасць глабальнага цыкла (кожныя 4 гадзіны): **Retry Queue -> Google Sheets -> Trello -> Airtable**.

- **Bug Fixes:**
  - Выпраўлены URL унутранага API Airtable для скрапера (фікс памылкі 404).
  - Выпраўлена памылка вызначэння мадэлі для масіваў вакансій у `vacancies.js`.
  ## [2026-06-24] - Аптымізацыя парсінгу Airtable, укараненне шчыта Markdown V2 і сінтаксічныя фіксы

### Дададзена
- **Куленепрабівальны экранары у ai.service.js (v4.0)**: Распрацавана і ўкаранёна лакальная функцыя `escapeMarkdownV2` унутры `formatTelegramPost`. Яна жалезна забяспечвае аўтаматычнае экраніраванне спецсімвалаў, якія патрабуе Telegram (`-`, `+`, `=`, `!`, `.`, `(`, `)`, `{`, `}`, `[`, `]`, `>`, `#`, `|`, `~`), абараняючы бот ад падзення ў рэжым адпраўкі звычайнага тэксту.
- **Разумная праверка парнасці фарматавання**: Дададзены аўтаматычны аналіз на няпарнасць зорачак (`*`) і ніжніх падкрэсліванняў (`_`). Пры выяўленні памылак ад AI мадэлі, сімвалы экрануюцца (`\*` і `\_`), што цалкам выключае памылкі `Bad Request: can't parse entities`.
- **Lookbehind-рэгулярка для сsplit-маркера**: Рэгулярны выраз напісаны з выкарыстаннем адмоўнага прагляду назад/наперад (`(?<!=== )`), што дазваляе экраніраваць знакі роўнасці ў тэксце, але пакідае цэлым тэхнічны маркер падзелу пастоў `=== SPLIT ===`.

### Выпраўлена
- **Запуск AI-пайплайна для Job Impulse і Solano**: Паспяхова запушчана і адладжана асінхронная сінхранізацыя паміж Airtable скраперам і базай дадзеных. Канал пачаў у рэальным часе атрымліваць структураваныя пасты.
- **Блок try-catch ў formatTelegramPost**: Адноўлены згублены пачатак блока `try {` пасля зліцця розных версій коду, што выклікала памылку парсінгу мадэлі ў асяроддзі TypeScript/JavaScript на Render.
- **Лішняя дужка**: Прыбрана залішняя фігурная дужка `}` у канцы функцыі, якая ламала сінтаксіс (SyntaxError).
- **Зменная выніку AI**: Выпраўлена памылка звароту да неіснуючай зменнай `response.data`. Дадзеныя паспяхова пераведзены на чытанне з актуальнай зменнай `result` (`result?.data || result`).
- **Бэкэнд-шчыт для аварыйнага JSON**: Абноўлены JSON-fallback шчыт — цяпер нават калі AI вяртае сыры JSON, абедзве версіі поprimary (`fullFallback` і `shortFallback`) праходзяць праз функцыю бяспечнага экраніравання перад адпраўкай.
- **Сід і Скрапер дадзеных**: Выпраўлены і аптымізаваны працэдуры ініцыялізацыі дадзеных (Seed) і скрапінгу для прадухілення дубліравання запісаў пры паралельным сканаванні розных агенцый.
### [2026-06-25] Circular Sync & Telegram Editor Backend
- **Мадэль Vacancy**: Дададзены палі для Тэлеграм-пастоў (`telegramFull`, `telegramShort`), статусу публікацыі і прычыны закрыцця.
- **Circular Sync**: Створана мадэль `SyncState`. `index.js` перапісаны на прыярытэтную чаргу (Inbox > Pending > Circle).
- **HR-Рэдактар**: У `vacancies.js` цалкам адключана аўтаматычная адпраўка ў Тэлеграм. Дададзены эндпоінты `/generate-preview` і `/publish`.
- **Стабілізацыя Airtable**:
  - Укаранёны "Safety Switch": сінхранізацыя спыняецца пры памылцы AI, не даходзячы да закрыцця вакансій.
  - Дададзена захаванне індэкса (Circular Sync) для Airtable.
  - Выпраўлены скрапер Manpower праз прымусовыя ID табліц.
  ### [2026-06-25] Поўная стабілізацыя бэкенда і Circular Sync
- **Circular Sync (Кола)**: 
  - Укаранёна захаванне пазіцыі (`SyncState`) для Google Sheets, Trello і Airtable.
  - Сістэма запамінае радок/картку пры памылцы AI і працягвае з таго ж месца.
- **Прыярытэты чаргі**: 
  - У `index.js` наладжана паўза сінхранізацыі, калі ў Інбоксе ёсць неапрацаваныя чаты.
- **HR-Рэдактар (Backend)**:
  - У `vacancies.js` цалкам адключана аўтаматычная адпраўка ў Telegram для ўсіх тыпаў стварэння вакансій.
  - Дададзены эндпоінты `POST /generate-preview` і `POST /publish`.
  - У мадэль `Vacancy` дададзены палі `telegramFull`, `telegramShort`, `isPublished`, `postOutdated`.
- **AI Resilience**:
  - У `ai.service.js` дададзена ахова ад HTML-памылак Vertex AI (403/404).
  - Абноўлены спіс мадэляў (Gemini 2.5 Flash).
- **Крыніцы**:
  - Выпраўлены скрапер Manpower (прымусовыя ID табліц).
  - Выпраўлена памылка `cardCounter` у Trello.
  ## fix(ai): resolve Vertex AI 403 by supporting Render Secret Files path

**Праблема:** Vertex AI вяртаў 403 Forbidden на Render, бо `google-creds.json`
захоўваецца ў `/etc/secrets/google-creds.json` (Render Secret Files),
а код шукаў яго ў `process.cwd()`.

**Змены:**
- `ai_service.js` — `GoogleAuth.keyFile` цяпер выкарыстоўвае `/etc/secrets/google-creds.json`
  калі файл існуе, інакш fallback на `process.cwd()` (лакальная распрацоўка)
- `gemini.service.js` — тое самае для Google Drive auth

**Вынік:** Vertex AI мадэлі (gemini-2.5-flash, gemini-2.5-flash-lite)
зноў даступны на Render. Groq больш не нясе ўсю нагрузку адзін.
## fix(sync): replace hard inbox lock with soft global.isChatProcessing flag

**Праблема:** Сінхранізацыя ніколі не запускалася бо жорсткая праверка
кол-васці паведамленняў у інбоксе заўсёды вяртала `return`.

**Змены:**
- `inbox.js` — дададзены `global.isChatProcessing` флаг які
  выстаўляецца `true` падчас апрацоўкі канвеера і `false` у `finally`
- `index.js` — жорсткі блок з `pendingInbox > 0` і `return` замененены
  на мяккае чаканне праз `setInterval` пакуль `global.isChatProcessing === false`

**Вынік:** Сінхранізацыя стартуе заўсёды. Калі канвеер чатаў заняты —
сінхранізацыя чакае яго завяршэння і толькі потым працягваецца.
### [2026-06-26] Фікс скрапера Manpower (Airtable)
- **Праблема**: Скрапер вяртаў "0 радкоў" для Manpower, бо не мог знайсці табліцу ў складанай структуры JSON (Application Read).
- **Змены ў `airtableScraper.service.js`**:
  - Укаранёны прымусовы выбар `tableId` (`tblTyT7NtUNZ1n2ek`) і `viewId` для спасылак Manpower.
  - Дададзены параметры `mayOnlyIncludeRowAndCellDataForIncludedViews` і `canClientSupportPreviewMode` для абыходу абароны Airtable.
  - Перапісана логіка пошуку аб'екта табліцы: цяпер сістэма шукае патрэбны ID ва ўсіх магчымых галінах JSON (`sharedApplication`, `application`, `data`).
- **Вынік**: Скрапер павінен паспяхова бачыць і здабываць радкі з публічнай дошкі Manpower.
## fix(sync): add isComplete flag and force-restart on incomplete circle or pending_ai

**Праблема:** Кола фіксавалася як "завершана" нягледзячы на збоі AI (403/cooldown).
Пры перазапуску сервера cooldown блакаваў запуск нават калі кола не было скончана
або заставаліся вакансіі са статусам `pending_ai`.

**Змены:**
- `SyncState.js` — дададзена поле `isComplete: Boolean` (default: false)
- `index.js` — `runSyncWithInsurance` атрымаў параметр `forceRun = false`
- `index.js` — у пачатку кола `isComplete` ставіцца `false`
- `index.js` — пасля поўнага завяршэння `isComplete` ставіцца `true`
- `index.js` — пры старце: калі cooldown актыўны АЛЕ `isComplete === false`
  або ёсць `pending_ai` вакансіі — сінхранізацыя запускаецца прымусова

**Вынік:** Сервер пасля перазапуску аўтаматычна дабірае незавершанае кола
і апрацоўвае `pending_ai` вакансіі без чакання 3.5 гадзін cooldown.
## feat(ai): replace Vertex AI with Gemini AI Studio free tier + keep Groq fallback

**Прычына:** Vertex AI накапіў запазычанасць $58.30 (білінг з 9 чэрвеня).
Google Cloud заблакаваў API доступ — усе мадэлі вярталі 403 Forbidden.

**Змены:**
- `ai_service.js` — Vertex AI мадэлі закаментаваны ў `AI_CHAIN`
- `ai_service.js` — дададзены новы provider `gemini_studio` з мадэллю
  `gemini-2.0-flash` (бясплатны ключ праз Google AI Studio)
- `ai_service.js` — дададзены апрацоўшчык `gemini_studio` у `executeAIRequest`
  з таймаутам 60с і апрацоўкай памылак 429/500/403
- Render env — `GEMINI_API_KEY` заменены на прыватны ключ з aistudio.google.com

**Новы парадак AI_CHAIN:**
1. `gemini-2.0-flash` (Gemini AI Studio, бясплатна)
2. `llama-3.3-70b-versatile` (Groq, 100k токенаў/дзень)
3. `llama-3.1-8b-instant` (Groq, fallback для кароткіх тэкстаў)

**Вынік:** Сістэма працуе без Vertex AI. Vertex можна ўключыць назад
раскаментаваўшы радкі ў `AI_CHAIN` пасля аплаты білінгу.