# Project Activity Log

# 📋 PROJECT SUMMARY — Job Vacancy CRM (HR Dispatcher Bot)

> Актуальны стан на: Красавік 2026
> Мова зносін з AI-агентам: **Беларуская** 🇧🇾
> Repo: `github.com/Vadimeks/job-vacancy-crm`

---

## 🧱 СТЭК

| Слой     | Тэхналогія                                   | Дэплой                  |
| -------- | -------------------------------------------- | ----------------------- |
| Backend  | Node.js + Express + MongoDB Atlas (Mongoose) | Railway                 |
| Frontend | React + Vite + Tailwind CSS                  | Vercel                  |
| AI       | Groq SDK (`llama-3.3-70b-versatile`)         | —                       |
| Bot      | Telegraf                                     | Telegram канал          |
| Userbot  | GramJS                                       | чытае чаты агенцый 24/7 |

**URLs:**

- Frontend: `https://job-vacancy-crm.vercel.app`
- Backend: `https://job-vacancy-crm-production.up.railway.app`

---

## 🗂 СТРУКТУРА ПРАЕКТА (файлы, з якімі працавалі)

```
backend/
  routes/
    vacancies.js          — REST API маршруты, галоўная логіка апрацоўкі
  services/
    ai.service.js         — увесь AI: парсінг, фарматаванне, шаблоны, мердж
    telegram.service.js   — адпраўка ў TG, апавяшчэнні рэкрутэру
    matching.service.js   — матчынг кандыдатаў (НЕ закраналі)
  models/
    Vacancy.js            — Mongoose-схема v2.0
    Template.js           — мадэль шаблона (патрэбна абнаўленне пад v2.0)
  seeds/
    seedTemplates.js      — засеўка шаблонаў у базу (патрэбна абнаўленне)
    manpower.js           — 46 шаблонаў ✅
    ralen.js              — 15 шаблонаў ✅
    solano.js             — 3 шаблоны ✅

frontend/src/
  pages/
    Vacancies.jsx         — спіс вакансій + рэжым "З шаблона"
    Templates.jsx         — спіс шаблонаў + пошук
    Candidates.jsx        — НЕ закраналі
  components/
    VacancyViewModal.jsx  — прагляд вакансіі (перапісаны пад v2.0)
    EditVacancyModal.jsx  — рэдагаванне вакансіі (перапісаны пад v2.0)
    TemplateViewModal.jsx — прагляд шаблона (перапісаны пад v2.0)
    EditTemplateModal.jsx — рэдагаванне шаблона (перапісаны пад v2.0)
    VacancyFilters.jsx    — фільтры (абноўлены пад v2.0)
    AddTemplateModal.jsx  — НЕ праглядалі, магчыма старая структура ⚠️
  api.js                  — дадаў createVacancyFromTemplate
```

---

## 🔄 АРХІТЭКТУРА — ФЛОЎ АПРАЦОЎКІ ВАКАНСІІ

```
Тэкст з Telegram (GramJS userbot)
    ↓
POST /api/vacancies/auto
    ↓
isInformative() / isSimpleMessage()
    ├── Просты тэкст (<80 сімв. або няма ключавых палёў)
    │       → notifyRecruiterAboutShortMessage()
    │       → TG паведамленне рэкрутэру
    │         (без кнопак на localhost, з кнопкамі на продакшне)
    │       → вакансія НЕ ствараецца
    └── Інфарматыўны тэкст
            ↓
        parseVacancyWithAI() → структура v2.0
            ↓
        constructVacancyDisplayName()
        → назва для адмінкі: "АГЕНЦЫЯ — Суць працы — Горад"
            ↓
        formatTelegramPost() → Markdown
        (БЕЗ agencyName і templateName у посце!)
            ↓
        Захаванне ў MongoDB + адпраўка ў TG канал
            ↓
        Фонавы пошук: identifyTemplate()
            ├── Брэнд у тэксце → адразу ў AI (~3к токенаў, раней ~14к)
            ├── Знойдзены → запісаць ТОЛЬКІ templateId
            │   ⚠️ НЕ мяняць палі вакансіі!
            └── Не знойдзены → createTemplateFromVacancy() → новы шаблон
```

> ⚠️ **Крытычна важна:** Фонавы блок НЕ мяняе палі вакансіі — толькі запісвае `templateId`.
> Была праблема: VAC-0082 і VAC-0084 атрымлівалі дадзеныя з шаблона замест рэальнага тэксту — выпраўлена.

---

## 📐 СТРУКТУРА ДАДЗЕНЫХ v2.0 (Vacancy)

```js
{
  // Ідэнтыфікацыя
  agencyName,           // назва агенцыі
  templateName,         // назва шаблона/прадпрыемства
  vacancydescription,   // публічны загаловак (для кандыдатаў)
  vacancyCode,          // "VAC-0082"
  category,             // сфера работы
  keywords: [],
  contractType,
  status,               // active / archived / draft

  // Лакацыя
  location,             // горад (НЕ поўны адрас!) ← вядомая праблема
  locationDescription,
  checkInCity,
  voivodeship,

  // Зарплата
  salary: {
    baseNetto, studentNetto, hoursRange,
    payoutDates, bonusDetails, salaryNotes
  },

  // Графік
  schedule: {
    description, shiftsCount, hoursPerShift,
    workDaysWeek, breakDuration
  },

  // Жыллё
  accommodation: { type, forCouples, costRaw, details },

  // Транспарт
  transport: { provided, costRaw, details },

  // Патрабаванні
  requirements: {
    gender: [],         // масіў: ["М", "Ж"]
    ageMax,
    nationalities: [],
    standardDocs: [],
    polishLanguageLevel,
    physicalLoad
  },

  // Умовы
  conditions: {
    workwearFree,       // boolean
    foodType, foodDetails,
    specificNuances: [],
    specificConditionsDetails
  },

  // Выдаткі (блок у TG посце ТОЛЬКІ калі hasStartExpenses=true)
  startExpenses: { hasStartExpenses, details },

  // Адказнасць (блок у TG посце ТОЛЬКІ калі hasLiability=true)
  earlyTerminationLiability: { hasLiability, details },

  employerCompensations: { hasCompensations, details },

  // Для рэкрутэра (НЕ паказваецца кандыдатам)
  forRecruiter: {
    internalNotes,
    hideAgencyNameForCandidate,
    hideEnterpriseNameForCandidate
  },

  // Агульнае
  description, additionalNotes,
  arrivalDate, count,
  rawText, telegramPost
}
```

---

## ✅ ШТО БЫЛО ЗРОБЛЕНА (па ўсіх чатах)

### Backend

| Файл                  | Змены                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------ |
| `telegram.service.js` | `parse_mode`: HTML → Markdown                                                              |
| `telegram.service.js` | `notifyRecruiterAboutShortMessage`: `isLocalhost` праверка → розныя кнопкі                 |
| `ai.service.js`       | `FORMAT_PROMPT`: блок "Витрати" толькі пры `hasStartExpenses=true` або `hasLiability=true` |
| `ai.service.js`       | `identifyTemplate`: лакальны пошук выдалены, адразу ў AI. Токены: ~14к → ~3к               |
| `vacancies.js`        | Новы эндпоінт `POST /api/vacancies/from-template/:templateId`                              |
| `vacancies.js`        | `isSimpleMessage()` — фільтр нефарматыўных тэкстаў                                         |
| `vacancies.js`        | Выдалены аўта-мердж палёў з шаблонам у `processVacancyMessage`                             |
| `.env`                | Дадана `FRONTEND_URL=http://localhost:5173`                                                |

### Frontend

| Файл                    | Змены                                                                                                        |
| ----------------------- | ------------------------------------------------------------------------------------------------------------ |
| `VacancyViewModal.jsx`  | Поўны перапіс пад v2.0; кнопкі кандыдата закаменціраваны                                                     |
| `EditVacancyModal.jsx`  | Поўны перапіс пад v2.0; чэкбоксы для boolean палёў                                                           |
| `TemplateViewModal.jsx` | Поўны перапіс пад v2.0 + новы кампанент прагляду                                                             |
| `EditTemplateModal.jsx` | Поўны перапіс пад v2.0                                                                                       |
| `Vacancies.jsx`         | Картка: `v.title` → `v.templateName \|\| v.vacancydescription`; `applyFilters()` пад v2.0; рэжым "З шаблона" |
| `VacancyFilters.jsx`    | Мовы: нідэрл. → ням./англ.; давоз: "Ёсць/Няма"; нацыянальнасці: +Грузія, Казахстан, Азербайджан, Іншыя       |
| `Templates.jsx`         | Пошук па назве/фірме/горадзе/ключавых словах + кнопка 👁                                                     |
| `api.js`                | Дадаў `createVacancyFromTemplate`                                                                            |

### Шаблоны агенцый (seed-файлы)

| Файл          | Шаблонаў | Заўвага                |
| ------------- | -------- | ---------------------- |
| `manpower.js` | 46       | з 109, дублі аб'яднаны |
| `ralen.js`    | 15       | з 17                   |
| `solano.js`   | 3        | з 4                    |

**Агенцыі ў базе зараз:** PERSONNEL SERVICE (~50), EWL (~35), MANPOWER (46), BISAR, APOLO (~12), OTTO (~23), RALEN (15), SOLANO (3) = **~184–200+ шаблонаў**

---

## 🔴 БЯГУЧЫЯ ПРАБЛЕМЫ

### 1. Вакансіі перасталі апрацоўвацца ← ТЭРМІНОВА

**Сімптом:** У Telegram і базу нічога не прыходзіць.
**Дзе глядзець:** Лог Railway + `GROQ_API_KEY` у env.

### 2. `location` запісвае поўны адрас замест горада

**Сімптом:** `"Przemysłowa 12, 76-200 Głobino"` замест `"Słupsk"`
**Дзе выправіць:** `SYSTEM_INSTRUCTION` парсера ў `ai.service.js`

### 3. Паламаная вакансія `*[templateName]*` у TG канале

**Статус:** Крыніца не высветлена.

---

## 🟡 ТЭХДОЎГ (адкладзена)

### Backend

- [ ] `Template.js` — абнавіць мадэль пад v2.0
- [ ] `seedTemplates.js` — абнавіць пад v2.0
- [ ] AI промпты (`mergeWithTemplate`, `formatTelegramPost`, `parseVacancyWithAI`) — праверыць і абнавіць
- [ ] Мердж `from-template` — браць толькі ЗМЕНЕНЫЯ палі з новага тэксту (шаблон у базе не кранаць)
- [ ] `vacancydescription` фолбэк — `templateName + першы сказ description` (зараз можа вяртаць "Нова вакансія")

### Frontend

- [ ] `AddTemplateModal.jsx` — не праглядаўся, верагодна старая структура ⚠️
- [ ] Фільтр агенцый — цягнуць з базы дынамічна
- [ ] Фільтр "Еду" (Адзін/Пара/Сям'я) — логіка не вызначана
- [ ] Фільтр "Сфера" — уніфікаваць як AI запісвае `category`
- [ ] Выбар агенцыі пры рэдагаванні — dropdown замест free text
- [ ] `/templates?text=` і `/vacancies?updateText=` — апрацоўка query-параметраў з TG кнопак

### Кандыдаты (пазней)

- [ ] Кнопкі "Хачу працаваць" / "Дазнацца дэталі" — закаменціраваны ў `VacancyViewModal`
- [ ] Фронтэнд для кандыдатаў — без палёў `forRecruiter`
- [ ] `Candidates.jsx` і `matching.service.js` — не закраналіся

### Астатнія агенцыі (seed-файлы)

- [ ] PERSONNEL SERVICE, EWL, BISAR, APOLO, OTTO — генерацыя / рэфактарынг пад v2.0

---

## 🎯 ПРЫЯРЫТЭТЫ (з чаго пачынаць)

| #   | Задача                                      | Прыярытэт    |
| --- | ------------------------------------------- | ------------ |
| 1   | Дыягностыка: чаму вакансіі не апрацоўваюцца | 🔴 Тэрмінова |
| 2   | Выправіць `location` — горад замест адрасу  | 🔴           |
| 3   | Абнавіць `Template.js` пад v2.0             | 🟡           |
| 4   | Абнавіць `seedTemplates.js`                 | 🟡           |
| 5   | Абнавіць AI промпты                         | 🟡           |
| 6   | Праверыць `AddTemplateModal.jsx`            | 🟡           |
| 7   | Выправіць мердж `from-template`             | 🟡           |
| 8   | Фільтры: агенцыі з базы, сфера, еду         | 🟢           |
| 9   | Дадаць астатнія агенцыі ў seed-файлы        | 🟢           |
| 10  | Фронтэнд для кандыдатаў                     | 🟢 Пазней    |

---

## 🤝 ПРАВІЛЫ СУПРАЦЫ (для AI-агента — чытаць абавязкова)

1. **Мова:** Адказваць **толькі па-беларуску**
2. **Адзін крок за раз** — зрабілі → уладальнік пацвердзіў ("GO" або апісаў выніку) → наступны крок
3. **Заўсёды пытаць актуальны файл** перад зменамі — уладальнік мяняе код паміж сесіямі
4. **Не вынаходзіць кантэкст** — калі бачыш паляпшэнне, паведамі і чакай рашэння
5. **Не аптымізаваць самому** — мяняць строга тое, пра што дамовіліся
6. **Спачатку апісанне задачы + план** — потым код
7. **Папярэджваць** калі ідэя можа зламаць сістэму — прапанаваць альтэрнатыву
8. **Тлумачыць** кожны крок як для пачаткоўца
9. **Цэлы файл або лагічны блок** — не фрагменты; файлы да 300 радкоў цалкам
10. **Камітавацца** перад кожнай новай задачай

# Job Vacancy CRM - Guide

## Commands

- Dev: `npm run dev`
- Build: `npm run build`
- Python Tests: `D:/Projects/skills/venv/Scripts/python.exe ...`

## Tech Stack

- Frontend: React, Vite, Tailwind CSS
- Backend: Node.js, Express, MongoDB

## Testing Rules

- Use Playwright from the Skills repository.
- Always wait for `networkidle`.

## [14.04.2026] — Сесія: Пераход на архітэктуру v2.0

- **Зроблена:**
  - Распрацавана новая інструкцыя парсера (`vacancy_parser_v2.md`) з пашыранай схемай JS-аб'екта.
  - Сфармулявана новая логіка матчынгу: шаблон як даведнік, а не крыніца праўды.
  - Вызначаны прыярытэты па рамонце бэкенда.
  - Прынята рашэнне адмовіцца ад фільтрацыі "кароткіх паведамленняў" — парсім усё.

- **Бягучы стан:**
  - Вакансіі часова не даходзяць да базы/Telegram (патрэбна дыягностыка Railway).
  - Праблема з `location` (піша адрас замест горада).

- **Наступны крок:** Тэхнічны аўдыт сувязі GramJS -> Backend -> Groq.

## [15.04.2026] — Сесія: Аналітыка вакансій і фарміраванне Enums

### Выканана:

- **Аўтаматызацыя падрыхтоўкі даных**: Створаны Python-скрыпт `convert_docx_to_md.py` для пакетнай канвертацыі апісанняў вакансій з фармату `.docx` у `.md` (апрацавана 13 файлаў ад розных агенцый).
- **Глыбокі аналіз рынку**: Праз AI Studio (Gemini) праведзены поўны парсінг апрацаваных вакансій.
- **Фарміраванне даведнікаў (Enums)**:
  - Сфармаваны двухузроўневы спіс **сфер дзейнасці** (Логістыка, Харчовая, Аўта, Вытворчасць, Тэкстыль, Экалогія).
  - Складзена табліца **лакацый** (больш за 80 гарадоў з прывязкай да ваяводстваў).
  - Створаны рэестр **прадпрыемстваў і брэндаў** з прывязкай да гарадоў (LPP, Amazon, Valeo, Bosch і інш.).
- **Дакументацыя**: Створаны файл `frontend/instructions/vac-descr.md` (зводны тэкст) і абноўлены правілы структуравання даных для AI-агента.

### Наступныя крокі:

- Інтэграваць атрыманыя спісы ў кампанент `VacancyFilters.jsx` на фронтэндзе.
- Выкарыстоўваць дадзеныя лакацый і прадпрыемстваў для валідацыі ў `ai.service.js`.

## [15.04.2026] — Сесія: Паляпшэнне парсера v2.0 і ўніфікацыя катэгорый

- **Зроблена:**
  - Абноўлена `SYSTEM_INSTRUCTION` у `ai.service.js`: дададзены жорсткі спіс з 8 украінскамоўных катэгорый.
  - Укаранёны новы фармат `vacancydescription`: "Суть роботи (Підкатегорія) — Місто".
  - Уключана правіла `MAX DETAIL EXTRACTION`: парсер цяпер выцягвае дробныя дэталі (кошт харчавання, назвы аўтобусных маршрутаў, дэталізаваныя прэміі).
  - Паспяхова пратэставаная вакансія "Склад Gądki": парсер выцягнуў 100% інфармацыі, уключаючы графік руху транспарту і цэны ў сталоўцы.
- **Вынік:** Парсінг стаў глыбейшым, катэгорыі ўніфікаваныя пад будучыя фільтры фронтэнда.

## [2026-04-16] — Global Template & Database Refactoring

### Што было зроблена:

- **Уніфікацыя катэгорый:** Усе 279 шаблонаў пераведзены на новую сістэму з 8 строгіх катэгорый на ўкраінскай мове.
- **Стандартызацыя загалоўкаў:** - Пале `vacancydescription` зараз фармуецца па шаблоне: `Сутнасць (Падкатэгорыя) — Горад`.
  - Прыбраны назвы агенцтваў і лішні кантэкст з апісанняў.
- **Рэфактарынг назваў шаблонаў:** Поле `templateName` стала больш інфарматыўным для рэкрутэра (Брэнд + Горад + Сутнасць).
- **Стэрылізацыя базы дадзеных:**
  - Поўнасцю выдалены старыя вакансіі (калекцыя `Vacancies`).
  - Выдалены ўстарэлыя ўнікальныя індэксы MongoDB.
  - Калекцыя `Templates` перазапісана з нуля на аснове абноўленых `.js` файлаў.
- **Аўтаматызацыя:** Напісаны і паспяхова выкарыстаны скрыпты `patchTemplates.js` (для масавага рэдагавання файлаў) і `seedTemplates.js` (для чыстага імпарту ў БД).

### Вынік:

База шаблонаў утрымлівае **279** актуальных аб'ектаў, гатовых да працы з новымі фільтрамі на фронтэндзе.

## [2026-04-17] — State Synchronization & Testing Phase

### 📋 Актуальны стан праекта (Status Quo):

- **Database:** Стэрылізавана. Калекцыя `Vacancies` пустая, `Templates` утрымлівае 279 эталонных аб'ектаў.
- **Standards:** Усе шаблоны выкарыстоўваюць адзіную сістэму з 8 катэгорый і фармат загалоўкаў `Сутнасць (Падкатэгорыя) — Горад`.
- **Cleanup:** Старыя задачы па рэфактарынгу шаблонаў і выпраўленні дублікатаў лічацца **завершанымі**. Далейшыя працы будуць грунтавацца на гэтай чыстай базе.

### 🎯 Задачы на сёння:

1. Тэставанне скразнога шляху вакансіі: **Parsing -> Matching -> Saving**.
2. Падключэнне тэставай групы Viber праз Webhook.
3. Абнаўленне фронтэнд-фільтраў пад новыя катэгорыі.

## [2026-04-17] — New Architecture: Vacancy-Template Linking

### Змена логікі:

- **No Merging:** Адмова ад зліцця вакансіі з шаблонам пры парсінгу. Вакансія заўсёды захоўвае 100% свежага апісання.
- **Matching as Reference:** Шаблон цяпер выкарыстоўваецца як даведка (Reference). Калі супадзенне 100% — спасылка ў мадалцы. Калі <100%, але тая ж агенцыя/брэнд — блок "Падобныя вакансіі".
- **Self-Growing DB:** Калі матчынг не адбыўся — вакансія аўтаматычна становіцца новым шаблонам.
- **Age Policy:** Адмова ад значэння "60" па змаўчанні. Пераход на статус "Няма інфармацыі / Без абмежаванняў".

### Фронтэнд:

- Поўная лакалізацыя на ўкраінскую мову.
- Пераход на выпадаючыя спісы (dropdowns) для ўсіх шматварыянтных фільтраў.

## [2026-04-17] — Standards Consolidation & Recruitment Logic

### Дасягненні:

- **Master Data:** Сфармаваны эталонны спіс прадпрыемстваў, лакацый і дакументаў на аснове аналізу 279 шаблонаў.
- **Recruitment Tools:** Распрацаваны ўніфікаваны чэк-ліст кваліфікацыі кандыдата (украінская мова), які будзе інтэграваны ў карткі вакансій.
- **Architectural Pivot:** Зафіксавана новая логіка "Vacancy-Template Reference" замест Merging. Гэта гарантуе 100% дакладнасць апісання пры захаванні доступу да базы ведаў.
- **Age Policy Update:** Прынята рашэнне аб адыходзе ад статычнага значэння ўзросту (60) на карысць дынамічных статусаў ("Без обмежень", "Інформація відсутня").

### Тэхнічныя змены:

- Падрыхтаваны пераклады інтэрфейсу фільтраў на ўкраінскую мову.
- Сфармаваны спіс тэгаў для фільтра "Документи".

## [2026-04-17] — Backend Models Stabilization & AI Alignment

### ✅ Дасягненні (Backend):

- **Сінхранізацыя мадэляў:** Мадэлі `Vacancy.js`, `Template.js` і `Candidate.js` цалкам прыведзены да адзінага стандарту палёў.
- **Логіка Reference:** У мадэль `Vacancy.js` дададзена поле `relatedTemplates` (масіў ObjectIds). Гэта дазваляе звязваць вакансію з некалькімі падобнымі шаблонамі без зліцця тэкстаў (адпаведна PRD v2.0).
- **Age Policy (99):** У мадэлях і `ai.service.js` значэнне па змаўчанні для `ageMax` зменена з 60 на **99** (сістэмны маркер для "Без обмежень").
- **Кандыдацкая база:** - Мадэль `Candidate.js` пашырана палямі з чэк-ліста кваліфікацыі (зрок, тэмпературны рэжым, мануальныя навыкі).
  - Уніфікаваны блок `documents` у кандыдата для хуткага матчынгу з вакансіямі (`activeDocs` тэгі).
- **Data Integrity:** Поле `shiftsCount` замацавана як `Number` для захавання матэматычнай фільтрацыі, з логікай запісу максімальнай колькасці змен праз парсер.

### 🛠 Тэхнічныя праўкі:

- **ai.service.js:** Абноўлена функцыя `cleanData`, прыбрана прымусовая замена пустых значэнняў узросту на 60.
- **Indexes:** Падрыхтаваны поўнатэкставыя індэксы для шаблонаў для паляпшэння якасці пошуку AI-агентам.

## [2026-04-17] — Стандартызацыя даных v2.0 і паляпшэнне якасці парсінгу

### 🧠 Backend & AI Service

- **Мадэль Vacancy.js:** Дададзена поле `brand` (String). Цяпер назва прадпрыемства/завода захоўваецца асобна ад катэгорыі, што паляпшае пошук і аналітыку.
- **Абнаўленне AI Parser (ai.service.js):**
  - **Zero Loss Policy:** Уведзены жорсткія патрабаванні да захавання дэталяў (брэнды BMW/Tesla, этапы рэкрутацыі BHP/сумоўе).
  - **Bullet-point Formatting:** AI цяпер абавязаны выкарыстоўваць `;` для падзелу абавязкаў, што дазваляе фронтэнду выводзіць іх спісам.
  - **Geo-Standardization:** Аўтаматычны пераклад гарадоў на польскую мову (напр. "Варшава" -> "Warszawa") для бездакорнай працы фільтраў.
  - **Language Mapping:** AI мапіць апісальныя ўзроўні мовы на міжнародныя коды (A1, A2, B1, B2).
  - **Category Sync:** Укаранёны строгі спіс з 8 асноўных катэгорый на ўкраінскай мове.

### 🎨 Frontend & UI/UX

- **Рэдызайн VacancyViewModal.jsx:**
  - Структура цалкам перапрацавана пад логіку Telegram-поста.
  - Замена іконак на тэкставыя пазнакі ("Місто:", "Ставка:", "Набір:") для паляпшэння чытальнасці.
  - Дададзены візуальныя тэгі для катэгорыі і брэнда ў хэдэр мадалкі.
- **Новы EditVacancyModal.jsx:**
  - Створана форма рэдагавання з лагічным падзелам на секцыі (Аплата, Лакацыя, Жытло).
  - Рэалізавана функцыя глыбокага абнаўлення аб'ектаў (`handleChange` для ўкладзеных палёў).
- **Фільтрацыя (Vacancies.jsx):**
  - Функцыя `applyFilters` перапісана з пошуку падрадкоў на строгае параўнанне, што выправіла памылкі адлюстравання.
- **Master Data:** Створаны файл `constants/masterData.js`, які сінхранізуе значэнні фільтраў з выхадам AI на 100%.

### 🛠 Разбор тэхнічных памылак

- **Памылка 406 AUTH_KEY_DUPLICATED (GramJS):**
  - **Прычына:** Спроба адначасовага выкарыстання адной Telegram-сесіі ў розных месцах (Local vs Railway) або дубляванне працэсаў Node.js.
  - **Рашэнне:** Поўнае спыненне ўсіх працэсаў `node`, ачыстка файлаў сесіі і пры неабходнасці перавыпуск `STRING_SESSION`. Усталяваны прыярытэт: адна сесія — адзін актыўны IP.

# 📝 Справаздача: Аптымізацыя Telegram Userbot

**Дата:** 18 красавіка 2026 г.  
**Лог №:** 001

## 1. Выкананыя задачы (Changelog)

| Катэгорыя        | Дзеянне                                                                                      | Статус |
| :--------------- | :------------------------------------------------------------------------------------------- | :----- |
| **Аўтарызацыя**  | Вырашана памылка `406: AUTH_KEY_DUPLICATED` шляхам генерацыі новай сесіі.                    | ✅     |
| **Канфігурацыя** | Перанесены ўсе зменныя (`API_ID`, `API_HASH`) у `.env`.                                      | ✅     |
| **Стабільнасць** | Імплементаваны `Heartbeat` (кожныя 10 хв) праз `client.getMe()`.                             | ✅     |
| **Код**          | Выдалены дублікаты і выпраўлены парадак выканання (ініцыялізацыя -> аўтарызацыя -> сэрвісы). | ✅     |
| **Дэплой**       | Паспяховы рэдэплой на Render. Бот актыўны і падключаны да MongoDB.                           | ✅     |

---

## 2. Паточная архітэктура

- **Сэрвіс:** Node.js (Express) + GramJS.
- **Статус:** Анлайн (Render Free Tier).
- **Абарона ад сну:**
  1.  **Cron-job (Знешні):** HTTP-пінг кожныя 10 хвілін (забяспечвае актыўнасць працэсу).
  2.  **Heartbeat (Унутраны):** `setInterval` кожныя 10 хвілін (забяспечвае актыўнасць TCP-злучэння Telegram).

---

## 3. Інструкцыя па абслугоўванні (Troubleshooting)

Калі бот перастаў адказваць:

1.  **Крок 1 (Праверка):** Праверце логі на Render.
    - Калі бачыце `Deploying` — бот проста перазагружаецца, пачакайце.
    - Калі `429 Too Many Requests` — пачакайце, не чапайце код.
2.  **Крок 2 (Рэстарт):** Націсніце `Manual Deploy -> Clear Build Cache & Deploy` у Render Dashboard.
3.  **Крок 3 (Аўтарызацыя - крайняя мера):** Калі ў логах `Unauthorized` або `AUTH_KEY...` — запусціце `node generate_session.js` на сваім кампутары, атрымайце новы радок і абнавіце зменную `TELEGRAM_SESSION` у Render.

---

# Changelog

## [0.2.0] - 2026-04-18

### Fixed

- **Parser:** Resolved `baseNetto` assignment bug in `parseVacancyWithAI` where salary data was being lost or incorrectly mapped to a non-existent property.
- **Parser:** Fixed data loss issues by restoring full response structure using the spread operator (`...cleaned`) instead of rigid field mapping, ensuring all AI-generated fields are preserved.
- **Schema:** Added safe access checks (fallbacks like `|| {}`) for nested objects (`salary`, `schedule`, `accommodation`, etc.) to prevent crashes when the AI omits specific blocks.

### Refactored

- **Parser Logic:** Streamlined the `parseVacancyWithAI` function. Simplified the return structure to prioritize AI-provided data integrity while maintaining essential validation and formatting logic.
- **Salary Logic:** Implemented a robust fallback mechanism for `baseNetto` that automatically attempts to extract the rate from `salaryNotes` if the primary field is missing or marked as "не вказано".

## [0.2.1] - 2026-04-19

### Changed

- **Parser/Schema:** Enforced strict validation for the `requirements.polishLanguageLevel` field. The AI is now restricted to specific standardized codes ("Не вимагається", "A1", "A2", "B1", "B2", "C1").
- **Data Consistency:** Updated system prompt instructions to eliminate descriptive language (e.g., "рівень розуміння") in favor of fixed codes. This ensures that the data is ready for frontend filtering while maintaining a clear format for Telegram posts.

# 📝 Project Activity Log — Session: AI Parser & Formatter Refinement

> **Дата:** 19 красавіка 2026 г.
> **Аб'ект:** `backend/services/ai.service.js`
> **Вынік:** Пераход на стандарт парсінгу v2.0 (Update: Anti-Hallucination) і паляпшэнне Telegram-фарматавальніка.

---

## 🎯 Мэта сесіі

Выправіць памылкі інтэрпрэтацыі дадзеных AI-агентам (галюцынацыі), палепшыць геаграфічную дакладнасць і стварыць прафесійнае фарматаванне для Telegram-постаў.

---

## ✅ Тэхнічныя змены

### 1. AI Parser (`parseVacancyWithAI`)

- **Геаграфія (Strict Latin Rule):**
  - Уведзена жорсткае патрабаванне выкарыстоўваць толькі лацінскія сімвалы (A-Z) для польскіх гарадоў.
  - Забаронена дадаваць назву краіны "Polska" або "Poland" у поле `location`.
  - Дададзена кодавая страхоўка: `parsed.location.replace(/Polska,?\s*/gi, "")`.
- **Анты-галюцынацыя (Brand & Role Guard):**
  - Забаронена "прыдумляць" тып тавару (адзенне/абутак), калі гэтага няма ў тэксце. Пры адсутнасці дадзеных выкарыстоўваецца нейтральны загаловак: `Склад товарів (Логістика)`.
  - Забаронена ствараць вакансію "Водій", калі ў тэксце прысутнічае толькі расклад аўтобусаў.
- **Дагавор (Contract Integrity):**
  - Вернута логіка `contractType: null`, калі тып дагавора не згаданы ў арыгінале (AI больш не гадае).
- **Дубляванне (Data Hygiene):**
  - Уведзена правіла `STRICT NO-DUPLICATION` — інфармацыя, якая ўжо ёсць у структурных палях (зарплата, жыллё, давоз, адзенне), больш не трапляе ў `description` або `additionalNotes`.
- **Паўнавартаснасць:**
  - Удакладнена правіла для `additionalNotes`: сюды ідзе ўсё, што не ўлезла ў структуру (відэа, маршруты, этапы рэкрутацыі).

### 2. Telegram Formatter (`FORMAT_PROMPT`)

- **Выпраўленне структуры:** Выдалены інструкцыі па парсінгу, якія памылкова трапілі ў блок адлюстравання.
- **Блок зарплаты:** Дададзены асобныя пункты `• Бонуси` і `• Нотатки` для лепшай чытэльнасці.
- **Блок пражывання:** Уведзены падзел на `• Вартість` і `• Деталі`.
- **Умовы працы:** Палепшана адлюстраванне нюансаў харчавання і рабочага адзення.

---

## 📊 Вынікі тэстаў (Кейс Polkowice / CCC)

| Параметр         | VAC-0023 (Да выпраўленняў)             | VAC-0024 (Пасля выпраўленняў)            |
| :--------------- | :------------------------------------- | :--------------------------------------- |
| **Загаловак**    | Склад одягу (Логістика) — галюцынацыя  | **Склад товарів (Логістика)** — дакладна |
| **Лакацыя**      | Pольковіце (памылка транслітарацыі)    | **Polkowice** (чыстая лацінка)           |
| **Дагавор**      | Umowa zlecenie (прыдумана)             | **null** (адпавядае тэксту)              |
| **Фарматаванне** | Суцэльны тэкст у блоках                | **Структураваны спіс з булітамі**        |
| **Дублі**        | Паўторы пра давоз і адзенне ў нататках | **Чысты блок дадатковай інфармацыі**     |

---

## 🛠 Наступныя крокі

1.  **Message Stitching:** Распрацоўка механізму затрымкі (2-3 сек) для аб'яднання разбітых Telegram-паведамленняў у адзін кантэкст перад адпраўкай у AI.
2.  **Frontend Sync:** Праверка адлюстравання поля `contractType` у мадалках, калі яно прыходзіць як `null`.

---

**Log end.**

# Тэхнічны лог праекта: Інтэграцыя Viber

**Дата:** 20 красавіка 2026
**Статус:** У працэсе (Апошняя міля інтэграцыі Android-шлюза)

## 1. Кароткі агляд

Мэта праекта — наладзіць аўтаматычную перадачу ўваходных паведамленняў Viber у CRM-сістэму. Пасля няўдалых спроб прамога чытання лакальнай базы дадзеных (Viber PC/Windows), праект быў перанакіраваны на выкарыстанне Android-прылады ў якасці шлюза праз MacroDroid.

---

## 2. Храналагічны пратакол

| Этап       | Дзеянне                                          | Вынік                                                      |
| :--------- | :----------------------------------------------- | :--------------------------------------------------------- |
| **Этап 1** | Спроба прамога капіравання `viber.db`            | **Няўдача:** Файлы заблакаваныя, `file is not a database`. |
| **Этап 2** | Падключэнне праз `sql.Open` (DSN `_immutable=1`) | **Няўдача:** База зашыфравана (SQLCipher).                 |
| **Этап 3** | Дыягностыка структуры файлаў                     | **Вынік:** Паведамленні ў SQLCipher, доступ нерэнтабельны. |
| **Этап 4** | Пераход на Android + MacroDroid                  | **Поспех:** Апавяшчэнні паспяхова ловяцца сістэмай.        |
| **Этап 5** | Інтэграцыя з CRM (Бэкенд/Фронт)                  | **У працэсе:** Наладка фармату `x-www-form-urlencoded`.    |

---

## 3. Тэхнічная інструкцыя: Наладка перадачы дадзеных

Пасля аналізу праблем з парсінгам `JSON` у MacroDroid, мы пераходзім на больш стабільны фармат `application/x-www-form-urlencoded`.

### А. Наладка MacroDroid (HTTP Request)

## [20.04.2026] — Сесія: Інтэграцыя Viber і сістэма "Пясочніца"

### Выканана:

- **Інтэграцыя Viber (Мабільны мост)**:
  - Выяўлена шыфраванне лакальнай базы `viber.db` (SQLCipher), што зрабіла прамое чытанне немагчымым.
  - Рэалізаваны абыходны шлях праз **Android + MacroDroid**: тэлефон перахоплівае апавяшчэнні і адпраўляе іх на бэкенд праз HTTP POST.
  - Наладжана перадача даных у фармаце `application/x-www-form-urlencoded` для стабільнай апрацоўкі спецсімвалаў і эмодзі.

- **Бэкенд (Sandbox & API)**:
  - Створана мадэль `UnprocessedMessage` для захавання ўсіх уваходных паведамленняў "як ёсць".
  - Напісаны роўт `/api/inbox/push`, які здабывае тэкст паведамлення нават пры парушанай структуры запыту.
  - Укаранёна аўтаматычная класіфікацыя паведамленняў на **Вакансіі**, **Абнаўленні** і **Чат** па ключавых словах.

- **Фронтэнд (UI/UX)**:
  - Створана старонка **"Уваходныя"** для ручной апрацоўкі паведамленняў.
  - Рэалізавана логіка **"Стварыць вакансію"**: тэкст з Пясочніцы аўтаматычна пераносіцца ў форму AI-парсінга.
  - Дададзены лічыльнік непрачытаных паведамленняў у галоўнае меню (`Layout`).

### ⚠️ Бягучыя праблемы:

- **Дублікаты**: Пры паўторных апавяшчэннях у Android ствараюцца аднолькавыя запісы ў базе.
- **UI**: Вялікія карткі паведамленняў ствараюць занадта доўгую стужку (патрэбен Compact View).
- **Кіраванне**: Адсутнасць масавых аперацый (выдаліць усё / выбранае).

### Наступныя крокі:

- Укараніць дэдуплікацыю на бэкендзе (праверка па тэксце і часе < 10 хв).
- Стварыць `Bulk API` для масавага выдалення паведамленняў.
- Перавесці спіс уваходных у таблічны (кампактны) выгляд.

## [21.04.2026] — Сесія: Аптымізацыя Пясочніцы (Inbox) і Вайтліст чатаў

**Статус:** База ачышчана, наладжаны маппінг агенцый.

### Выканана:

- **Рэалізацыя Agency Mapping**: Створаны механізм ідэнтыфікацыі агенцый па назве чата. Цяпер паведамленні з Viber аўтаматычна атрымліваюць пазнаку `agencyName` (напр. "OTTO", "Global", "PERSONNEL SERVICE").
- **Строгая фільтрацыя (Whitelisting)**: Укаранёны спіс дазволеных чатаў. Паведамленні з іншых крыніц ігнаруюцца бэкендам (`status: not_whitelisted`), што эканоміць рэсурсы базы.
- **Глыбокі Noise-фільтр**: Напісаны Regex-патэрны для выдалення смецця:
  - Рэакцыі (эмодзі на паведамленні).
  - Сістэмныя паведамленні ("прыєднався до групи").
  - Пытанні пра кандыдатаў ("можу подати жінку").
  - Стандартныя фразы ("Дякую", "Доброго дня").
- **Масавыя аперацыі**: Дададзены API-эндпойнты:
  - `POST /api/inbox/cleanup` — для ачысткі базы ад назапашанага раней смецця.
  - `DELETE /api/inbox/bulk` — для масавага выдалення апрацаваных паведамленняў.
- **Абнаўленне мадэлі**: У `UnprocessedMessage` дададзена поле `agencyName` і палепшаны індэксы для дэдуплікацыі.

### ⚠️ Выяўленыя праблемы (патрабуюць вырашэння):

- **Telegram Crash**: Сэрвіс Telegram выдае памылку `RawMessage is not defined` пры спробе пераслаць паведамленне ў пясочніцу. Трэба праверыць імпарты ў `services/telegramListener.js` (або адпаведным файле).
- **Недакладнасць маппінгу**: Некаторыя чаты (напр. з эмодзі `🤝 Mrówki`) не распазнаюцца з-за розніцы ў прабелах або спецсімвалах.
- **Дэдуплікацыя**: Трэба павялічыць акно праверкі дублікатаў або зрабіць яго больш строгім па тэксце.

### План на наступную сесію:

1. Выправіць памылку `RawMessage is not defined` у Telegram-сервісе.
2. Аптымізаваць `CHAT_AGENCY_MAP`, выкарыстоўваючы больш простыя ключы для `includes()`.
3. Наладзіць фронтэнд (Inbox.jsx) для кампактнага адлюстравання паведамленняў з фільтрацыяй па агенцыях.

## [0.2.2] - 2026-04-21

### Added

- **Hard Filter System (`utils/messageFilters.js`)**: Створаны цэнтралізаваны сэрвіс фільтрацыі. Гэта "нулявы ўзровень" абароны, які выкарыстоўвае Regex для імгненнага адсячэння сістэмных паведамленняў (Viber/TG) і іншага шуму да таго, як паведамленне трапіць да AI.
- **Pipeline Integration**: У `routes/inbox.js` інтэграваны выклік `shouldIgnoreMessage()`. Цяпер сістэма прымусова правярае кожнае паведамленне на адпаведнасць патэрнам перад адпраўкай запыту ў Groq.
- **Regex-база**: Рэалізаваны пашыраны спіс `NOISE_PATTERNS` (на аснове `analyze.md`), які ўлічвае спецыфіку Viber-апавяшчэнняў і тэставых паведамленняў.
- **Centralized Config**: `CHAT_AGENCY_MAP` вынесены ў `messageFilters.js`, што выключае дубляванне логікі і спрашчае падтрымку спісу агенцый.

### Refactored

- **Routing Logic (`inbox.js`)**: Оптымізаваны шлях апрацоўкі паведамленняў. Сэрвіс імпартуе фільтры і адсякае смецце на ўваходзе, што значна эканоміць токены AI.
- **UI/UX (`Inbox.jsx`)**: Аптымізаваны кампанент прагляду ўваходных паведамленняў для больш хуткага апрацоўкі спісу.

### Fixed

- **Rate Limit Mitigation**: Дзякуючы ўкараненню Hard Filter, колькасць непатрэбных запытаў да AI скарочана (чакаецца зніжэнне спажывання токенаў на 70%+).

## [0.2.4] - 2026-04-23

### Added

- **API Interface:** Дададзены `aiUpdateVacancy` у `api.js` (PATCH /vacancies/:id/ai-update).
- **Error Handling (Backend):** - У `vacancies.js` дададзена "мяккая" апрацоўка памылак AI (429 Rate Limit), сістэма больш не выдае `500` пры перавышэнні лімітаў Groq.
  - Меры па захаванні цэласнасці дадзеных пры няўдалых спробах адпраўкі ў Telegram.

### Fixed

- **Deduplication:** У `backend/routes/inbox.js` інтэгравана лагіка `normalizeText` і `textHash`. Сістэма ігнаруе дублікаты (з улікам эмодзі і рэгістра) на працягу 1 гадзіны.
- **Frontend (Inbox):**
  - Выдалена катэгорыя "Chat" з фронтэнду (фільтрацыя ў `Inbox.jsx`).
  - Выпраўлена праца кнопкі "Абнавіць": дададзены `loading` state і карэктны сінхронны фетч дадзеных.
- **Frontend (Navigation):** - У `Vacancies.jsx` дададзены `useLocation` для захопу `initialText` з state. Мадалка стварэння вакансіі цяпер карэктна адкрываецца пасля пераходу з Інбокса.

### Logic

- **Filtering:** Уведзена жорсткая фільтрацыя шуму на ўзроўні роўтаў `inbox.js`.

## [0.2.5] - 2026-04-23

### Added

- **MacroDroid Bridge:** Рэалізацыя "No-Code" моста для перасылкі паведамленняў з Telegram і Viber на бэкенд праз HTTP POST.
- **Architecture Shift:** Пераход ад `GramJS` userbot да лакальнай аўтаматызацыі на прыладзе (MacroDroid) для забеспячэння стабільнасці.

### Changed

- **Deployment Strategy:** Юзербот (`userbot.js`) пераведзены ў пасіўны рэжым (дэпрэкейтыд), каб пазбегнуць рызыкі блакавання акаўнтаў праз API.
- **Data Flow:** Уніфікацыя ўваходных патокаў даных; бэкенд працягвае працаваць праз існуючыя эндпоінты `/push` (Viber) і `/push-userbot` (Telegram).

### Fixed

- **Risk Mitigation:** Вырашана праблема патэнцыйных банаў Telegram API праз адмову ад выкарыстання доўгажывучых userbot-сесій на карысць лакальнага чытання апавяшчэнняў.

## [0.2.6] - 2026-04-24

### Added

- **Groq Fallback:** Імплементавана логіка пераключэння мадэляў: пры памылцы 429 (ліміт `llama-3.3-70b`) сістэма аўтаматычна пераходзіць на хуткую `llama-3.1-8b`.
- **Frontend Filters:** Дададзены інтэрактыўныя фільтры ў `Vacancies.jsx` ("Давоз да працы", "Хто едзе").
- **UI Diagnostics:** У `VacancyViewModal` дададзены блок "Арыгінальны тэкст паведамлення" для зручнай праверкі працы AI.
- **Constants:** Файл `constants/masterData.js` папоўнены блокамі `TRANSPORT_OPTIONS` і `TRAVEL_GROUPS`.

### Fixed

- **Backend Stability:** Поўнае выдаленне `GramJS` і юзербота. Сервер ачышчаны ад састарэлага коду.
- **JSON Safety:** У `index.js` дададзены мідлвар для апрацоўкі `SyntaxError`. Бітыя JSON-пакеты больш не выклікаюць падзенне сервера.
- **Inbox UI:** Выдалена катэгорыя "Chat" з інтэрфейсу і статыстыкі. Рэалізавана функцыя масавага выдалення паведамленняў.
- **MacroDroid Integration:** Пераход макрасаў на фармат `application/x-www-form-urlencoded`, што канчаткова вырашыла праблему `Bad control character` пры перадачы тэксту.

### Logic

- **Manual Mode:** Уведзена глабальная канстанта `AUTO_PROCESS_VACANCIES = false`. Аўтаматычны парсінг адключаны; вакансіі збіраюцца ў Пясочніцы для ручнога кантролю.

## [0.2.7] - 2026-04-24

### Added

- **MacroDroid Integration:** Поўны пераход на фармат `application/x-www-form-urlencoded` для стабільнай перадачы тэксту без памылак JSON.
- **Groq Fallback:** Рэалізавана аўтаматычнае пераключэнне на мадэль `llama-3.1-8b` пры памылцы 429 (ліміт `70b`).
- **UI Enhancements:** У `VacancyViewModal` дададзены блок прагляду арыгінальнага тэксту паведамлення (Raw Text).
- **Debug:** Дададзены дэталёвыя логі для ўваходных MacroDroid запытаў.

### Fixed

- **Backend Architecture:** Поўнае выдаленне `GramJS` (юзербота), пераход на незалежную ад сесіі архітэктуру.
- **Stability:** Укаранёны `SyntaxError` мідлвар для прадухілення падзення сервера пры некарэктных сімвалах.
- **Inbox:** Створаны адзіны эндпоінт `/api/inbox/push` для ўніфікацыі ўсіх крыніц (Viber/Telegram).
- **Frontend:**
  - Выпраўлены канфлікт `useEffect` у `Vacancies.jsx`, які ламаў перадачу даных.
  - Абноўлена логіка фільтраў ("Давоз", "Хто едзе", "Агенцыя", "Брэнд").
  - Ачышчаны інтэрфейс: выдалена катэгорыя "Чат", пачынена кнопка масавага выдалення паведамленняў.

### Next Session (TODO)

- [ ] **Pending Messages:** Выпраўленне "вісячых" паведамленняў (перадача `messageId` для абнаўлення статусу `processed: true`).
- [ ] **Filters:** Фікс фільтра "Нацыянальнасць" (апрацоўка пустых масіваў/ключавых слоў).
- [ ] **Whitelist:** Наладка вайтліста для тэставых чатаў (напр. "Мур Іра").
- [ ] **Maintenance:** Чыстка MongoDB ад старых дублікатаў вакансій.

## [Snapshot] - Project File Structure (2026-04-25)

Актуальная структура праекта для хуткай навігацыі і кантролю файлавай сістэмы.

```text
.
./backend
./backend/analyze.md
./backend/chats.md
./backend/CLAUDE.md
./backend/data
./backend/data/patch
./backend/data/templates
./backend/generate_session.js
./backend/import-archive.js
./backend/index.js
./backend/models
./backend/models/Candidate.js
./backend/models/Template.js
./backend/models/UnprocessedMessage.js
./backend/models/Vacancy.js
./backend/package-lock.json
./backend/package.json
./backend/patchTemplates.js
./backend/result.json
./backend/routes
./backend/routes/apply.js
./backend/routes/candidates.js
./backend/routes/inbox.js
./backend/routes/templates.js
./backend/routes/vacancies.js
./backend/scripts
./backend/scripts/seedTemplates.js
./backend/services
./backend/services/ai.service.js
./backend/services/classifier.service.js
./backend/services/gemini.service.js
./backend/services/matching.service.js
./backend/services/telegram.service.js
./backend/swaggerConfig.js
./backend/userbot.js
./backend/utils
./backend/utils/messageFilters.js
./frontend
./frontend/CLAUDE.md
./frontend/diagnose_crm.py
./frontend/eslint.config.js
./frontend/index.html
./frontend/instructions
./frontend/instructions/job_categories_and_locations.md
./frontend/instructions/LOG.md
./frontend/instructions/PLAN.md
./frontend/instructions/PRD.md
./frontend/instructions/scripts
./frontend/instructions/SYSTEM_INSTRUCTIONS.md
./frontend/instructions/vac-descr.md
./frontend/instructions/vacancy_parser_v2.md
./frontend/package-lock.json
./frontend/package.json
./frontend/postcss.config.js
./frontend/public
./frontend/public/favicon.svg
./frontend/public/icons.svg
./frontend/README.md
./frontend/src
./frontend/src/App.css
./frontend/src/App.jsx
./frontend/src/assets
./frontend/src/components
./frontend/src/constants
./frontend/src/index.css
./frontend/src/main.jsx
./frontend/src/pages
./frontend/src/services
./frontend/tailwind.config.js
./frontend/vercel.json
./frontend/vite.config.js
./skills_tools
```

- **Structure-command**: find . -maxdepth 3 -not -path '/.' -not -path 'node_modules' -not -path 'dist' -not -path 'build'

## [0.2.8] - 2026-04-25

### Added

- **Whitelist (Layer 1 Filtering):** Імплементавана жорсткая фільтрацыя па давераных чатах (SG, STAFF POWER, Solano, OTTO і інш.) для адсеку 90% спаму.
- **Loop Protection:** Дададзена ігнараванне чата "NOVA WORK AGENCY" для прадухілення бясконцых цыклаў рэкурсіўнага парсінгу.
- **Gemini Fallback:** Наладжаны аўтаматычны ланцужок пераключэння мадэляў (Flash -> Flash-lite -> 2.0 -> 1.5). Сістэма захоўвае жывучасць пры памылках 503.

### Improved / Refactored

- **Architecture:** Поўны пераход на `MacroDroid` (адмова ад `GramJS`), што забяспечвае стабільнасць прыёму паведамленняў.
- **Deduplication:** Павялічана акно праверкі дублікатаў з 1 да 24 гадзін. `normalizeText` цяпер цалкам ачышчае тэкст ад эмодзі і сімвалаў, пакідаючы толькі літары і лічбы.
- **Inbox:** Уніфікацыя эндпоінта для Viber і Telegram (адзіны роўт для апрацоўкі), што спрасціла логіку падтрымкі.

### System Status

- **Backend:** Працуе на Render. `AUTO_PROCESS_VACANCIES = false` (рэжым "Пясочніца" для назірання за фільтрамі).
- **Filtering:** Працуе трохузроўневая абарона (Whitelist -> Regex -> AI-класіфікацыя).

### Next Steps (Roadmap)

- [ ] **Field Testing:** Маніторынг логаў Render і праверка траплянняў у вайтліст.
- [ ] **Data Cleanup:** Выпраўленне статусу `processed: true` пасля стварэння вакансіі (каб паведамленні знікалі з Пясочніцы).
- [ ] **UI/UX:** Аптымізацыя формы стварэння вакансіі (auto-fill з `translatedText` ад Gemini).
- [ ] **Auto-pilot:** Уключэнне `AUTO_PROCESS_VACANCIES = true` пасля паспяховага тэставання.
- [ ] **Frontend:** Даданне дынамічных фільтраў па агенцыях/брэндах.

## [2026-04-25] — Сінхранізацыя Інбокса і паляпшэнне фільтрацыі

### ✅ Што зроблена:

- **Сінхранізацыя Inbox-Vacancy:** Укаранёны механізм аўтаматычнай ачысткі Інбокса. Пры стварэнні вакансіі (аўта, з шаблона або ручной) зыходнае паведамленне і ўсе яго дублікаты па хэшы пазначаюцца як `processed: true`.
- **Разумная дэдуплікацыя:** Акно праверкі дубляў павялічана да **24 гадзін**. Функцыя нармалізацыі тэксту цяпер выдаляе ўсе сімвалы, акрамя літар і лічбаў, што выключае пропуск дубляў праз розныя эмодзі або прабелы.
- **Loop Protection:** Дададзены статус `IGNORE_SELF` для чата `NOVA WORK AGENCY`, каб бот не парсіў свае ўласныя пасты.
- **Фікс OTTO:** Выпраўлена памылка распазнавання чата OTTO (дададзена падтрымка кірылічных літар у назве).
- **Строгі AI:** Абноўлены промпт Gemini для адсячэння маркетынгавага шуму ("шмат вакансій", "дасылайце людзей") без канкрэтных дэталяў працы.

### 🛠 Тэхнічныя змены:

- **Backend:** У `vacancies.js` дададзена функцыя `markInboxMessageAsProcessed`.
- **Backend:** У `messageFilters.js` абноўлены `CHAT_AGENCY_MAP` (дададзены FREELANCE і кірылічны OTTO).
- **Frontend:** У `api.js` абноўлены метады стварэння вакансій для перадачы `messageId`.
- **Frontend:** У `Vacancies.jsx` інтэгравана перадача `sourceMessageId` праз state роўтара.

## [2026-04-25] — Стэрылізацыя базы і ўніфікацыя агенцый

### ✅ Што зроблена:

- **Чыстка БД:** Створаны і паспяхова запушчаны скрыпт `backend/scripts/clearVacancies.js`. База вакансій поўнасцю ачышчана (выдалена 64 аб'екты) для пачатку працы з чыстага ліста.
- **Стандартызацыя CHAT_AGENCY_MAP:**
  - Усе назвы агенцый прыведзены да адзінага стандарту (UPPERCASE).
  - Дададзена падтрымка кірылічнага напісання для OTTO ("отто для партнерів").
  - Абноўлены мапінг для Telegram: `rekruter freelancer` цяпер аўтаматычна пазначаецца як `SG`.
- **Уніфікацыя MANUAL:** Усталяваны адзіны фолбэк `MANUAL` для ўсіх выпадкаў, калі агенцыя не вызначана (замест разнабою з UNKNOWN).
- **Падрыхтоўка да UI-аптымізацыі:** Сфармулявана задача на ўкараненне ўмоўнага рэндерынгу (хаванне пустых палёў) і "жывога" лічыльніка Інбокса.

## [2026-04-25] — Аптымізацыя UI і кампактнасць Telegram

### ✅ Што зроблена:

- **Жывы лічыльнік Інбокса:** Укаранёна сістэма падзей (`CustomEvent`), якая дазваляе лічыльніку ў галоўным меню абнаўляцца імгненна пасля выдалення або апрацоўкі паведамленняў у Пясочніцы.
- **Кампактныя пасты ў Telegram:** Абноўлены `FORMAT_PROMPT` у `ai.service.js`. Цяпер AI аўтаматычна хавае назвы палёў (напр., "Давоз", "Жыллё", "Студэнты"), калі ў іх няма дадзеных. Посты сталі больш чытэльнымі і арганічнымі.
- **Фікс ESLint:** Выпраўлена памылка `cascading renders` у `Layout.jsx` праз асінхронны выклік пачатковай загрузкі статыстыкі.
- **Уніфікацыя MANUAL:** Поўнае выдаленне статусу `UNKNOWN` на карысць адзінага стандарту `MANUAL` для нераспазнаных агенцый.

### 🛠 Тэхнічныя змены:

- **Frontend:** `Layout.jsx` цяпер слухае падзею `inboxUpdated`.
- **Frontend:** `Inbox.jsx` і `Vacancies.jsx` генеруюць падзею `inboxUpdated` пасля паспяховых дзеянняў.
- **Backend:** Перапісана логіка фармату Telegram-поста для строгага захавання кампактнасці.

## [0.2.9] - 2026-04-27 — Наладка MacroDroid і ўключэнне аўтапарсінгу

### Дыягностыка і высновы

- **Зменныя MacroDroid:** Выяўлена, што `{not_text_big}` вяртае пусты радок для Viber і Telegram (Android не запаўняе поле BigText для гэтых дадаткаў). Адзіная рабочая зменная — `{notification}`, хаця яна абрэзае тэкст прыкладна на 200 сімвалах.
- **Прырода дублікатаў:** Пацверджана, што дублі генеруе не мэсенджар, а Android — MacroDroid трыгерыцца на кожнае абнаўленне апавяшчэння ў шторцы. Бэкэнд-дэдуплікацыя па хэшы справаляецца з гэтым на 100%.
- **Частковыя дублі:** Telegram спачатку адпраўляе кароткае апавяшчэнне (~200 сімв.), а праз 5-20 хвілін — поўнае. Гэта два розныя хэшы, таму абодва захоўваліся ў Пясочніцу як асобныя запісы.
- **Viber чат не лавіўся:** Апавяшчэнні з групавога чата не трапялі ў шторку Android, пакуль акаунт рэкрутэра не атрымаў правы адміністратара групы Viber.
- **Фільтрацыя ў MacroDroid:** Бясплатная версія не дазваляе дадаць умову параўнання зменнай у блоку "Умова Якщо" — катэгорыя "Змінна" адсутнічае. Фільтрацыя па вайтлісце пакінута цалкам на бэкэндзе.

### Added

- **Partial Deduplication Logic:** У `inbox.js` дададзена апрацоўка частковых дублікатаў. Калі прыходзіць поўнае паведамленне і за апошнія 5 хвілін з таго ж чата ўжо ёсць кароткае — існуючы запіс у Пясочніцы абнаўляецца да поўнага тэксту замест стварэння дубліката.

### Changed

- **MacroDroid — цела HTTP запыту:** Зменная тэксту зафіксавана як `{notification}` для абодвух макрасаў (Viber і Telegram). Усе зменныя выбіраюцца праз меню MacroDroid, а не ўпісваюцца ўручную.
- **Auto-pilot:** `AUTO_PROCESS_VACANCIES` зменена з `false` на `true`. Паведамленні з катэгорыяй `FULL_VACANCY` цяпер адразу ідуць на Groq і публікуюцца ў Telegram-канал. `UPDATE` і `RECRUITER_INFO` захоўваюцца ў Пясочніцу для ручной апрацоўкі.

### Fixed

- **Viber групавы чат:** Акаунт рэкрутэра прызначаны адміністратарам групы — апавяшчэнні цяпер трапляюць у шторку Android і лавяцца MacroDroid.

### System Status

- **MacroDroid:** Абодва макрасы (Viber і Telegram) працуюць стабільна. Фільтрацыя — цалкам на бэкэндзе.
- **Backend:** `AUTO_PROCESS_VACANCIES = true`. Чатырохузроўневая абарона: Whitelist → Дэдуплікацыя (24г + частковыя) → Regex → Gemini.
- **Inbox:** Захоўвае толькі `UPDATE` і `RECRUITER_INFO` для ручной апрацоўкі.

### Next Steps (Roadmap)

- [ ] **Маніторынг токенаў:** Праверыць расход Gemini + Groq пасля ўключэння аўтапілота.
- [ ] **Акно частковых дублікатаў:** Магчыма павялічыць з 5 да 20-30 хвілін, калі поўны тэкст з Telegram прылятае з вялікай затрымкай.
- [ ] **Тэставанне аўтапілота:** Праверыць якасць парсінгу Groq на рэальных вакансіях з вайтліставых чатаў.

## [2026-04-27] — Абарона ад дубляў і паляпшэнне класіфікацыі

- **Concurrent Protection:** Укаранёны `processingCache` у `inbox.js` для адсячэння адначасовых ідэнтычных запытаў (вырашана праблема дублявання вакансій пры адначасовых POST-запытах).
- **Truncation Guard:** Дададзена праверка `isTruncated`. Абрэзаныя паведамленні больш не праходзяць аўта-парсінг і заўсёды накіроўваюцца ў Пясочніцу для ручнога кантролю.
- **Gemini Logic:** Абноўлены промпт `gemini.service.js`. Спісы вакансій і кароткія паведамленні цяпер класіфікуюцца як `UPDATE`, што прадухіляе стварэнне некарэктных вакансій праз Groq.
- **Data Schema:** У мадэль `UnprocessedMessage` дададзены палі `rawText` (для ўкраінскага перакладу) і `isTruncated` (маркер паўнаты тэксту).

## [2026-04-27] — Сінхранізацыя мадэляў і ачыстка даных

- **Data Integrity:** Прыбрана значэнне ўзросту па змаўчанні (99). Цяпер `ageMax` захоўваецца як `null`, калі інфармацыя адсутнічае.
- **Model Sync:** У мадэль `Vacancy` дададзены палі `originalText` (арыгінал з MacroDroid) і `isTruncated` (маркер паўнаты даных).
- **AI Formatting:** Абноўлены `FORMAT_PROMPT` у `ai.service.js`. Цяпер блок з узростам цалкам хаваецца, калі ён не пазначаны або перавышае 65 гадоў.
- **Manual Agency:** Замацавана значэнне "Manual" як стандартнае для нераспазнаных агенцый.
- **Process Update:** Функцыя `processVacancyMessage` цяпер карэктна пракідвае арыгінальны тэкст і статус абрэзкі ў мадэль вакансіі.

## [2026-04-27] — Уніфікацыя патоку даных і абарона ад дубляў

- **Single Source of Truth:** Укаранёна поле `rawText` як асноўнае для ўсіх інтэрфейсаў (Пясочніца, Вакансіі, Telegram). Цяпер рэкрутэр працуе толькі з апрацаваным украінскім перакладам.
- **Concurrent Protection:** У `inbox.js` дададзены `processingCache` (Set), які блакуе адначасовыя ідэнтычныя запыты. Гэта цалкам вырашыла праблему стварэння 2-3 аднолькавых вакансій пры "гонцы запытаў".
- **Truncation Safety:** Уведзены маркер `isTruncated`. Калі паведамленне абрэзанае (ліміт Android), аўта-парсінг Groq забараняецца, а ў інтэрфейсе з'яўляецца папярэджанне ⚠️.
- **UI/UX Cleanup:**
  - Узрост па змаўчанні (99) выдалены. Цяпер поле `ageMax` захоўваецца як `null`.
  - У Telegram-посце і мадалцы вакансіі блок "Вік" цалкам хаваецца, калі дадзеныя адсутнічаюць або ўзрост перавышае 65 гадоў.
- **Gemini Refinement:** Абноўлены промпт класіфікацыі. Спісы вакансій і кароткія паведамленні цяпер строга маркіруюцца як `UPDATE`, што накіроўвае іх у Пясочніцу замест стварэння "пустых" вакансій.

### [0.3.0] - 2026-04-27 — Сэмантычная дэдуплікацыя і разумны вайтліст 23.38

#### Added

- **Semantic Deduplication (Gemini):** Укаранёна функцыя `analyzeAndCompareWithGemini`, якая параўноўвае новыя паведамленні з буферам за апошнія 12 гадзін. Сістэма цяпер аўтаматычна вызначае вердыкты: `DUPLICATE` (ігнараваць), `UPDATE` (у Пясочніцу) або `NEW` (на парсінг).
- **Aggressive Normalization:** Створана функцыя `superNormalize` у `messageFilters.js`. Яна пакідае ў назвах чатаў толькі літары і лічбы, што робіць вайтліст устойлівым да эмодзі, прабелаў і спецсімвалаў.

#### Changed

- **Inbox Pipeline:** Роўт `/push` цалкам перапрацаваны. Цяпер Groq запускаецца толькі для катэгорыі `FULL_VACANCY` з вердыктам `NEW`. Гэта цалкам вырашае праблему "размнажэння" вакансій (напр. VAC-0016...0019) пры дробных зменах тэксту.
- **Partial Duplicate Window:** Акно чакання поўнага тэксту (Telegram updates) павялічана з 5 да 60 хвілін.
- **Whitelist Logic:** Укаранёна двухбаковая праверка (Chat contains Key OR Key contains Chat) для максімальна дакладнага матчынгу агенцый.

#### Fixed

- **Works4you Matching:** Выпраўлена праблема ігнаравання чата "Works4you Вакансии в Польше" праз палепшаную нармалізацыю.
- **Token Optimization:** Скарочаны выдаткі токенаў Groq за кошт адсячэння сэмантычных дублікатаў на этапе Gemini.

## [0.3.1] - 2026-04-28 — Пакет стабілізацыі (Stabilization Phase)

### Fixed

- **Race Condition:** Укаранёна стратэгія "Save-First" у `inbox.js`. Паведамленні захоўваюцца ў базу адразу пасля Gemini, што блакуе дублікаты пры адначасовых запытах.
- **API Quota:** Пераход на мадэль `gemini-1.5-flash` з павялічанымі лімітамі запытаў.
- **Truncation Logic:** Абноўлена функцыя `isTruncated`. Цяпер паведамленні > 300 сімвалаў лічацца поўнымі без абавязковай кропкі ў канцы.
- **Atomic Codes:** Функцыя `generateVacancyCode` цяпер шукае максімальны нумар у базе, выключаючы памылку `E11000 duplicate key`.

### Added

- **Semantic Context:** Gemini цяпер параўноўвае новыя паведамленні не толькі з Пясочніцай, але і з апошнімі апублікаванымі вакансіямі.

## [0.3.2] - 2026-04-28 — Пакет стабілізацыі v2.0 (Production Ready)

### ✅ Backend & AI

- **Gemini v1 Migration:** Поўны пераход на стабільны API эндпоінт `v1`. Аптымізаваны спіс мадэляў (`1.5-flash`, `2.0-flash`).
- **Quota Protection (429):** Укаранёна логіка "Stop-on-429" у `gemini.service.js`. Пры вычарпанні квоты сістэма спыняе запыты і захоўвае паведамленне ў Пясочніцу без аўта-парсінгу.
- **Save-First Strategy:** У `inbox.js` зменена паслядоўнасць: паведамленне захоўваецца ў БД адразу пасля Gemini, што блакуе дублікаты пры адначасовых POST-запытах.
- **Atomic Vacancy Codes:** Функцыя `generateVacancyCode` перапісана на пошук максімальнага існуючага нумара (`sort: -1`), што выключае памылку `E11000 duplicate key`.
- **Age Policy Update:** Поўнае выдаленне дэфолтнага значэння `99`. Цяпер `ageMax` захоўваецца як `null`, калі дадзеныя адсутнічаюць.

### 🧹 Filtering & UI

- **Noise Reduction:** У `messageFilters.js` дададзены Regex-патэрны для адсеку сістэмнага шуму ("Ви маєте нові повідомлення", "Фотоповідомлення", "Ніден: закріплює").
- **Truncation Logic:** Абноўлены парогі `isTruncated`. Тэкст > 600 сімвалаў лічыцца поўным (BigText), < 300 сімвалаў без бяспечнага заканчэння — абрэзаным.
- **Compact Telegram Posts:** У `ai.service.js` абноўлены `FORMAT_PROMPT`. Блок "Вік" аўтаматычна хаваецца, калі значэнне `null`.
- **Whitelist Cleanup:** Выдалены нерэлевантны чат `BestWork`.

## [0.3.2] - 2026-04-28 — Пакет стабілізацыі v2.0 (Фінальныя праўкі)

### ✅ Backend & AI

- **Logging**: У `inbox.js` дададзены вывад тэксту паведамленняў, адхіленых Regex-фільтрам, для лепшай адладкі.
- **Age Policy**: Поўнае выкараненне значэння `99`. У `ai.service.js` укаранёна жорсткая ачыстка (cleanData) і абноўлена JSON-схема.
- **Formatting**: Палепшана інструкцыя `FORMAT_PROMPT` для пазбягання пустых пунктаў у спісе абавязкаў.
- **Truncation**: Абноўлена логіка `isTruncated` у `messageFilters.js` — цяпер яна больш адчувальная да паведамленняў без знакаў прыпынку ў канцы (прыкмета абрэзкі Android).

### 🧹 Filtering

- **Regex**: Палепшаны логі для адсочвання працы фільтраў шуму.
  [0.3.2] - Inbox Update: Added message length logging. Fixed category priority (truncated messages are now always 'vacancy'). Added smart fallback to identify vacancies when Gemini fails but text contains job keywords.

## [0.3.3] - 2026-04-28 — Пакет стабілізацыі v2.1 (Буфер і Groq Fallback)

### ✅ Backend & AI

- **Message Buffering**: Укаранёна сістэма назапашвання паведамленняў у `inbox.js`. Роўт `/push` цяпер толькі збірае даныя, а апрацоўка AI адбываецца раз у 2 хвіліны фонавым працэсам.
- **Stitching (Склейванне)**: Дададзена логіка абнаўлення існуючых паведамленняў у буферы. Калі на працягу 5 хвілін прыходзіць больш поўная версія тэксту (абнаўленне апавяшчэння), яна замяняе старую. Гэта вырашае праблему абрэзкі тэксту ў Telegram.
- **Groq Fallback**: У `gemini.service.js` дададзены фінальны этап ланцужка — класіфікацыя праз Groq (`llama-3.3-70b`). Цяпер, нават калі ўсе мадэлі Google выдадуць 429 памылку, паведамленне будзе апрацавана.
- **Smart Translation**: Створана функцыя `simpleTranslate` у `ai.service.js` для хуткага перакладу паведамленняў праз Groq, калі асноўныя мадэлі недаступныя.
- **Quota Optimization**: Укаранёна кэшаванне вынікаў Gemini на 5 хвілін і заменена логіка `break` на `continue` пры памылках 429 (RPM), што дазваляе выкарыстоўваць квоты розных мадэляў паслядоўна.

### 🧹 Filtering & Truncation

- **Truncation Logic**: Парог `isTruncated` падняты да 1200 сімвалаў. Логіка стала больш строгай: адсутнасць знакаў прыпынку ў канцы доўгага паведамлення цяпер заўсёды лічыцца абрэзкай.
- **Noise Reduction**: Дададзены новыя Regex-патэрны для адсеку сістэмнага шуму Viber ("Rakuten Viber", "відповідає: X місця").

## [0.3.4] - 2026-04-29 — Стабілізацыя буфера і Пясочніцы

### Added

- **Today Context**: Gemini цяпер атрымлівае ўсе паведамленні і вакансіі з пачатку бягучага дня (з 00:00:01) для максімальна дакладнай сэмантычнай дэдуплікацыі.
- **Date Filter**: У `messageFilters.js` дададзена функцыя `isOldMessage`, якая аўтаматычна ігнаруе паведамленні з датамі, старэйшымі за 2 дні ад сённяшняй.
- **Enhanced Logging**: У `inbox.js` укаранёны празрыстыя логі: цяпер відаць імя адпраўшчыка пры Regex-адмовах, ID запісу ў MongoDB і дэталёвыя AI-вердыкты (Category + Verdict).

### Fixed

- **Sandbox Visibility**: У роўце `GET /api/inbox` выдалены фільтр `processed: false`. Цяпер апрацаваныя паведамленні не знікаюць з фронту імгненна, што дазваляе рэкрутэру бачыць вынік працы AI.
- **AI Models Update**: У `gemini.service.js` выдалена састарэлая серыя 1.5. Усталяваны прыярытэт на актуальныя мадэлі красавіка 2026: `2.5-flash` і `2.0-flash`. Выпраўлены памылкі 404 праз пераход на `v1beta`.
- **Stitching & Throttling**: Акно абнаўлення паведамленняў (stitching) зафіксавана на 60 хвілінах. Уведзена штучная затрымка 2 секунды паміж запытамі да AI для захавання лімітаў RPM.
- **Truncation Threshold**: Парог `isTruncated` зніжаны з 1200 да 800 сімвалаў, што дазваляе большай колькасці інфарматыўных вакансій праходзіць на аўта-парсінг.

## [0.3.5] - 2026-04-30 — Стабілізацыя буфера і фінальная паліроўка AI

### ✅ Backend & AI (Стабілізацыя)

- **Deduplication Logic:** Выпраўлена памылка "бясконцага цыкла" (fallback loop) у `vacancies.js`. Цяпер сістэма правярае наяўнасць неапрацаванага дубліката па хэшы перад стварэннем новага запісу ў Пясочніцы.
- **AI Data Integrity:** Поўная перапіска функцыі `cleanData` у `ai.service.js`. Укаранёна ачыстка ад сістэмнага смецця AI ("99", "undefined", "не вказано") і выпраўлена памылка `Iterator value undefined`.
- **Compact Telegram Posts:** Узмацнены `FORMAT_PROMPT`. Дададзены жорсткія правілы на выдаленне пустых радкоў для палёў `👥 Набір` і `📄 Тип договору`, калі дадзеныя адсутнічаюць.
- **Inbox Refinement:**
  - Выпраўлена памылка прысваення катэгорыі: абразаныя паведамленні (`isTruncated`) цяпер атрымліваюць статус `vacancy` толькі пры пацверджанні ад AI.
  - Ліміт `isTruncated` усталяваны на 800 сімвалаў (верхні) і 50 сімвалаў (ніжні).
  - `isOldMessage` павялічаны да 3 дзён для захавання актуальнасці паведамленняў пасля выходных.

### 🧹 Filtering & Noise Reduction

- **Viber System Noise:** У `messageFilters.js` дададзены новыя рэгулярныя выразы для адсеку паведамленняў пра далучэнне карыстальнікаў да груп ("приєднався до .+/i").
- **Whitelist Accuracy:** Палепшана распазнаванне чатаў праз нармалізацыю назваў, што вырашыла праблемы з чатамі, якія змяшчаюць эмодзі.

### 🛠 Тэхнічныя змены

- **Logging:** Прэв'ю тэксту ў логах бэкенда (`logPreview`) зафіксавана на 80 сімвалах для зручнасці адладкі.
- **Buffer Processing:** Фонавая апрацоўка чаргі (`processPendingMessages`) настроена на інтэрвал 60 секунд з лімітам 10 паведамленняў за ітэрацыю.

## [0.3.6] - 2026-04-30 — Фінальная стабілізацыя фільтраў і бэкенда

### ✅ Frontend (Рэмонт фільтраў і UI)

- **Robust Filtering:** Поўная перапрацоўка `applyFilters` у `Vacancies.jsx`. Дададзена абарона ад `null` значэнняў, што выправіла памылку `Cannot read properties of undefined (reading 'includes')`.
- **Auto-Reset:** Укаранёны `useEffect` для аўтаматычнага прымянення фільтраў пры іх поўнай ачыстцы (больш не трэба націскаць кнопку пасля скіду).
- **Travel Group Logic:** Выпраўлена логіка "Хто едзе". Калі AI не пазначыў пол, вакансія аўтаматычна лічыцца даступнай для аднаго чалавека ("Адзін").
- **Nationality Logic:** Дададзены фолбэк на "Україна". Калі поле нацыянальнасці пустое, вакансія больш не знікае пры фільтрацыі.
- **International Locations:** У `dynamicData` і фільтрах укаранёны фармат `Горад (Краіна)` для вакансій па-за межамі Польшчы (напр. "Reinsdorf (Germany)").
- **Card UI:** У картку вакансіі дададзена адлюстраванне тыпу жытла (🏠 Платне/Безкоштовне) для хуткага азнаямлення.

### ⚙️ Backend (Аптымізацыя)

- **Deduplication Window:** Акно праверкі дублікатаў па хэшы павялічана з 1 да **12 гадзін**. Гэта дазваляе адсякаць паўторныя паведамленні на працягу ўсяго дня без звароту да AI.
- **Truncation Logging:** Дададзена празрыстае лагіраванне пры пропуску Groq-парсінгу для абразаных паведамленняў (`⏭️ Пропуск Groq: паведамленне абразанае`).
- **Fallback Loop Fix:** У `vacancies.js` узмацнена праверка на існуючыя неапрацаваныя паведамленні перад стварэннем fallback-запісу, што канчаткова спыніла "размнажэнне" паведамленняў пры памылках AI.

### 🤖 AI & Formatting

- **Strict Compactness:** Абноўлены `FORMAT_PROMPT` у `ai.service.js`. Блок `💸 Витрати та відповідальність` цяпер цалкам хаваецца, калі няма дадзеных, што выключыла з'яўленне фразы "(немає інформації)" у Telegram-постах.

## [0.3.7] - 2026-04-30 — Разумная дэдуплікацыя і давер да Тэлеграма

### Added

- **Smart Stitching (prefixHash):** У мадэль `UnprocessedMessage` дададзена поле `prefixHash` (першыя 150 сімвалаў). Гэта дазваляе сістэме адрозніваць розныя вакансіі ад адной агенцыі і склейваць толькі абнаўленні аднаго і таго ж паведамлення.
- **Composite Index:** Створаны індэкс `{ agencyName, prefixHash, createdAt }` для імгненнай праверкі дубляў у базе.

### Fixed

- **Truncation Bypass:** Функцыя `isTruncated` цяпер заўсёды вяртае `false` для крыніцы `telegram_userbot`, што дазваляе вакансіям з Тэлеграма адразу ісці на аўта-парсінг.
- **Safe Endings Regex:** Пашыраны спіс "бяспечных" заканчэнняў паведамленняў (дададзены літары і лічбы). Цяпер паведамленні, што заканчваюцца спасылкай, нумарам тэлефона або ключавым словам (CV, аванс), не лічацца абрэзанымі.
- **Groq Fallback Fix:** У `ai.service.js` выпраўлена функцыя `analyzeWithGroq`. Цяпер яна карэктна прымае параметр мадэлі, што дазваляе выкарыстоўваць хуткую `llama-3.1-8b` як фолбэк для Gemini.
- **Data Integrity:** Перапісана функцыя `cleanData` для больш надзейнай ачысткі AI-галюцынацый і прадухілення памылак пры апрацоўцы аб'ектаў.

### Changed

- **Inbox Pipeline:** Роўт `/push` пераведзены на параўнанне па `prefixHash`. Гэта вырашыла праблему "паглынання" новых вакансій старымі паведамленнямі ад той жа агенцыі.
- **Enhanced Logging:** У логі дададзена адлюстраванне крыніцы паведамлення `[source]` для лепшага маніторынгу патокаў Viber/Telegram.

## [0.3.8] - 2026-05-01 — Строгая класіфікацыя і гігіена Пясочніцы

### Added

- **Stricter Vacancy Rules:** Уведзены тэхнічны ліміт у 300 сімвалаў. Паведамленні, карацейшыя за гэты парог, аўтаматычна зніжаюцца да катэгорыі `update`, нават калі AI палічыў іх вакансіяй.
- **AI Alignment:** Промпты для Gemini і Groq (фолбэк) прыведзены да адзінага стандарту: для `FULL_VACANCY` цяпер абавязкова патрабуецца наяўнасць Пасады, Лакацыі і (Зарплаты або Апісання працы).

### Fixed

- **Import Error:** Выпраўлена памылка `ReferenceError: getPrefixHash is not defined` у роўце інбокса.
- **Sandbox Stats Sync:** Роўт `/stats` цяпер улічвае толькі неапрацаваныя паведамленні, што забяспечвае адпаведнасць лічыльніка ў меню рэальнаму спісу.

### Changed

- **Sandbox Hygiene:** Пясочніца (Inbox) цяпер адлюстроўвае толькі паведамленні са статусам `processed: false`.
- **Smart Auto-Done:** Паведамленне пазначаецца як апрацаванае (`processed: true`) і знікае з Пясочніцы толькі ў выпадку паспяховага аўта-парсінгу робатам, выяўлення дубліката або смецця. Кароткія апдэйты і інфа-радкі застаюцца для ручнога кантролю.

## [0.3.9] - 2026-05-01 — ID Discovery і прыярытэтная фільтрацыя па Chat ID

### Added

- **ID Discovery Mode:** У `userbot.js` дададзена функцыя `getDialogs`, якая пры старце сервера выводзіць у лог поўны спіс усіх чатаў (назва + ID), дзе прысутнічае акаўнт. Гэта дазваляе імгненна атрымаць дакладныя ID для вайтліста.
- **Chat ID Support:** У роўт `/push` і функцыю `getWhitelistedAgency` дададзена падтрымка `chatId`. Цяпер сістэма можа ідэнтыфікаваць агенцыю па ўнікальным ID чата, што на 100% абараняе ад памылак "Unknown" або змены назвы чата.

### Fixed

- **Logic Restoration:** Поўнасцю адноўлены ўсе спісы шуму (`SYSTEM_NOISE`, `RECRUITER_CHAT_NOISE`, `SOCIAL_NOISE`), функцыя праверкі старых паведамленняў `isOldMessage` і логіка затрымкі ў юзербоце. Усе фільтры працуюць у ранейшым рэжыме.

### Changed

- **Whitelist Priority:** Функцыя `getWhitelistedAgency` пераведзена на двухэтапную праверку: спачатку па ID (як самы надзейны метад для Telegram), потым па назве (як фолбэк для Viber).

## [0.4.0] - 2026-05-01 — Фіналізацыя вайтліста па Chat ID

### Added

- **Production Whitelist:** У `CHAT_AGENCY_MAP` дададзены рэальныя ID чатаў для SG, STAFF POWER, SOLANO і INTRASERVICE. Гэта забяспечвае 100% дакладнасць ідэнтыфікацыі агенцый у Telegram.

### Changed

- **Discovery Mode:** Блок аўтаматычнага вываду спісу чатаў у `userbot.js` закаментаваны для захавання чысціні логаў у працоўным рэжыме. Функцыянал захаваны для будучага выкарыстання.
- **Data Integrity:** Усе ID захоўваюцца як радкі (strings), што гарантуе карэктнае параўнанне з дадзенымі GramJS.

## [0.4.1] - 2026-05-01 — Стабілізацыя вайтліста і перадфільтрацыя шуму

### Fixed

- **Robust ID Matching:** Выпраўлена памылка ідэнтыфікацыі чатаў у Telegram. Функцыя `getWhitelistedAgency` цяпер выкарыстоўвае строгае параўнанне з ачысткай прабелаў (`trim`), што забяспечвае 100% трапленне для чатаў з вайтліста (напр. "Брыжук").
- **Pre-DB Filtering:** Узмацнена логіка адсеку смецця. Цяпер паведамленні, якія не прайшлі Regex-фільтры або вайтліст, наогул не трапляюць у базу дадзеных, што захоўвае Пясочніцу чыстай.

### Changed

- **Diagnostic Logging:** Дададзены вывад ID чата ў логі пры адмове па вайтлісце для палягчэння дыягностыкі новых крыніц.

## [0.4.2] - 2026-05-01 — Аптымізацыя лімітаў AI і чыстка логаў

### Added

- **Gemini Circuit Breaker:** Укаранёны механізм "замарозкі" мадэляў Gemini. Пры атрыманні памылкі `429` (Rate Limit) мадэль аўтаматычна ставіцца на паўзу да 07:00 раніцы наступнага дня. Гэта цалкам прыбрала спам памылкамі ў логах Render.
- **Smart Cooldown:** Функцыя `getMsUntilSevenAM` дынамічна вылічвае час адпачынку для кожнай мадэлі.

### Changed

- **Processing Interval:** Інтэрвал фонавай апрацоўкі паведамленняў у Пясочніцы павялічаны з 1 да **5 хвілін**. Гэта зніжае нагрузку на API і дазваляе больш эфектыўна выкарыстоўваць бясплатныя квоты.
- **Log Hygiene:** Поўныя JSON-аб'екты памылак Google выдалены з логаў, заменены на лаканічныя папярэджанні.

## [0.4.3] - 2026-05-02 — Аптымізацыя кампактнасці і ціхая апрацоўка

### Added

- **Silent Buffer Processing:** У `inbox.js` дададзена праверка колькасці паведамленняў перад запускам фонавага працэсу. Цяпер сістэма не спаміць у логі "ПАЧАТАК АПРАЦОЎКІ", калі чарга пустая.

### Fixed

- **Aggressive Data Cleaning:** Функцыя `cleanData` у `ai.service.js` цяпер больш жорстка выдаляе AI-смецце ("не вказано", "99", "undefined", "unknown"). Гэта забяспечвае чысціню даных у базе і карэктную працу фільтраў.
- **Telegram Compactness:** Перапісаны `FORMAT_PROMPT`. Цяпер цэлыя блокі (Пражыванне, Транспарт, Выдаткі) цалкам хаваюцца разам з загалоўкамі, калі ў іх няма карыснай інфармацыі. Посты ў Тэлеграм сталі выглядаць як натуральны тэкст, а не пустая анкета.
- **Salary Fallback:** У `parseVacancyWithAI` фолбэк для зарплаты зменены з тэкставага "не вказано" на `null`. Гэта выправіла памылку, калі ў Тэлеграм-посце выводзілася "Ставка: не вказано".

### Changed

- **Data Integrity:** Захавана поўная структура аб'екта ў базе (з `null` значэннямі) для бесперабойнай працы фронтэнд-фільтраў, пры гэтым забяспечана максімальная лаканічнасць пры вывадзе ў Тэлеграм.

## [0.4.4] - 2026-05-02 — Рамонт фільтраў і аптымізацыя выдаткаў AI

### Fixed

- **Frontend Filter Engine:** Поўная перапрацоўка функцыі `applyFilters` у `Vacancies.jsx`. Дададзена адсутная логіка для фільтраў: Транспарт, Дакументы, Асаблівасці (Чэк-ліст), Агенцыя і Брэнд. Цяпер фільтрацыя працуе строга па выбраных параметрах.
- **Dynamic Data Recovery:** Выпраўлена памылка знікнення спіса ваяводстваў. Цяпер `dynamicData` карэктна збірае ўнікальныя рэгіёны з масіва вакансій (улічваючы структуру v2.0).
- **AI Token Burner Fix:** У `inbox.js` зменена логіка выбару паведамленняў для аналізу. Цяпер паведамленні, якія ўжо былі апрацаваны AI (маюць запоўненае поле `rawText`), ігнаруюцца фонавым працэсам. Гэта спыніла бессэнсоўны паўторны аналіз "Абнаўленняў" кожныя 5 хвілін.
- **Accommodation Logic:** Выпраўлена інтэрпрэтацыя тыпаў жытла ў фільтрах. "Платнае" больш не трапляе ў катэгорыю "Без жытла".

### Added

- **Silent Mode:** Фонавы працэсар буфера цяпер працуе "ціха", калі ў базе няма новых паведамленняў для аналізу, што робіць логі сервера больш чытэльнымі.

## [0.4.5] - 2026-05-02 — Сінхранізацыя даных і пашырэнне геаграфіі

### Fixed

- **International Geography:** Укаранёна падтрымка вакансій па-за межамі Польшчы. Цяпер вакансіі з Германіі і іншых краін Еўропы аўтаматычна групуюцца ў рэгіён "Європа (інші країни)" і карэктна адлюстроўваюцца ў фільтрах.
- **Data Synchronization:** Абноўлены `masterData.js`. Назвы нацыянальнасцяў і дакументаў прыведзены ў поўную адпаведнасць з украінскамоўным выхадам AI (напр. "UA" -> "Україна", "Карта побыту" -> "Карта побуту"). Гэта выправіла памылку "нулявых" вынікаў у фільтрах.
- **Dynamic Nuances:** Фільтр "Асаблівасці (Чэк-ліст)" пераведзены на дынамічную мадэль. Цяпер ён паказвае толькі тыя тэгі, якія рэальна прысутнічаюць у вакансіях у базе (напр. "Запах гуми", "Холодны склад"), замест жорсткага спіса.
- **Accommodation Logic:** Удакладнена логіка фільтрацыі жытла. Цяпер сістэма дакладна адрознівае "Надаецца" (платнае/бясплатнае) ад "Уласнае" (власне).
- **ESLint Fix:** Выпраўлена памылка `no-undef` у `VacancyFilters.jsx` праз карэктную перадачу пропсаў з бацькоўскага кампанента.

### Changed

- **Filter Robustness:** Функцыя `applyFilters` цяпер больш устойлівая да пустых значэнняў і масіваў, што забяспечвае стабільную працу інтэрфейсу пры частковых даных.

## [0.4.6] - 2026-05-02 — Еўропа і катэгарызацыя нюансаў

### Added

- **Global Geography Support:** Уведзены стандартны рэгіён "Європа (інші країни)". Цяпер вакансіі з Германіі, Нідэрландаў і іншых краін аўтаматычна вызначаюцца парсерам і карэктна групуюцца ў фільтрах, нават калі дадзеныя ў базе былі частковымі.
- **Nuance Categorization:** Укаранёна сістэма катэгорый для нюансаў працы (Температурний режим, Запахи, Фізичне навантаження і інш.). AI цяпер фармуе тэгі ў фармаце "Катэгорыя (дэталь)", што дазволіла зрабіць фільтр на фронтэндзе чыстым і зручным.

### Fixed

- **Smart Filter Logic:** Функцыя `applyFilters` цяпер выкарыстоўвае частковае супадзенне для нюансаў (`startsWith`), што дазваляе фільтраваць па катэгорыі, бачачы пры гэтым поўныя дэталі ў картцы вакансіі.
- **Dynamic Data Recovery:** Палепшаны збор `dynamicData` для гарадоў і ваяводстваў. Цяпер назвы гарадоў для міжнародных вакансій аўтаматычна атрымліваюць прыстаўку краіны (напр. "Reinsdorf (Germany)"), што выключае блытаніну.
- **Master Data Sync:** Дададзена "Європа (інші країни)" у афіцыйны спіс ваяводстваў у `masterData.js`.

### Changed

- **AI Parser Instructions:** Абноўлены `SYSTEM_INSTRUCTION` для парсера. Цяпер ён абавязаны вызначаць краіну і прысвойваць еўрапейскім вакансіям адпаведны рэгіён, а таксама строга прытрымлівацца катэгорый у нюансах.
