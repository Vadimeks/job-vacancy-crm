# Project Activity Log

# 📋 PROJECT SUMMARY — Job Vacancy CRM (HR Dispatcher Bot)

## 🗂 СТРУКТУРА ПРАЕКТА (файлы, з якімі працавалі)

```
backend/
  routes/
    vacancies.js          — REST API маршруты, галоўная логіка апрацоўкі
  services/
    ai.service.js         — увесь AI: парсінг, фарматаванне, шаблоны, мердж
    telegram.service.js   — адпраўка ў TG, апавяшчэнні рэкрутэру
    matching.service.js   — матчынг кандыдатаў (НЕ закраналі)
  models/
    Vacancy.js            — Mongoose-схема v2.0
    Template.js           — мадэль шаблона (патрэбна абнаўленне пад v2.0)
  seeds/
    seedTemplates.js      — засеўка шаблонаў у базу (патрэбна абнаўленне)
    manpower.js           — 46 шаблонаў ✅
    ralen.js              — 15 шаблонаў ✅
    solano.js             — 3 шаблоны ✅

frontend/src/
  pages/
    Vacancies.jsx         — спіс вакансій + рэжым "З шаблона"
    Templates.jsx         — спіс шаблонаў + пошук
    Candidates.jsx        — НЕ закраналі
  components/
    VacancyViewModal.jsx  — прагляд вакансіі (перапісаны пад v2.0)
    EditVacancyModal.jsx  — рэдагаванне вакансіі (перапісаны пад v2.0)
    TemplateViewModal.jsx — прагляд шаблона (перапісаны пад v2.0)
    EditTemplateModal.jsx — рэдагаванне шаблона (перапісаны пад v2.0)
    VacancyFilters.jsx    — фільтры (абноўлены пад v2.0)
    AddTemplateModal.jsx  — НЕ праглядалі, магчыма старая структура ⚠️
  api.js                  — дадаў createVacancyFromTemplate
```

---

> Актуальны стан на: Красавік 2026
> Мова зносін з AI-агентам: **Беларуская** 🇧🇾
> Repo: `github.com/Vadimeks/job-vacancy-crm`

---

## 🧱 СТЭК

| Слой     | Тэхналогія                                   | Дэплой                  |
| -------- | -------------------------------------------- | ----------------------- |
| Backend  | Node.js + Express + MongoDB Atlas (Mongoose) | Railway                 |
| Frontend | React + Vite + Tailwind CSS                  | Vercel                  |
| AI       | Groq SDK (`llama-3.3-70b-versatile`)         | —                       |
| Bot      | Telegraf                                     | Telegram канал          |
| Userbot  | GramJS                                       | чытае чаты агенцый 24/7 |

**URLs:**

- Frontend: `https://job-vacancy-crm.vercel.app`
- Backend: `https://job-vacancy-crm-production.up.railway.app`

---

## 🔄 АРХІТЭКТУРА — ФЛОЎ АПРАЦОЎКІ ВАКАНСІІ

```
Тэкст з Telegram (GramJS userbot)
    ↓
POST /api/vacancies/auto
    ↓
isInformative() / isSimpleMessage()
    ├── Просты тэкст (<80 сімв. або няма ключавых палёў)
    │       → notifyRecruiterAboutShortMessage()
    │       → TG паведамленне рэкрутэру
    │         (без кнопак на localhost, з кнопкамі на продакшне)
    │       → вакансія НЕ ствараецца
    └── Інфарматыўны тэкст
            ↓
        parseVacancyWithAI() → структура v2.0
            ↓
        constructVacancyDisplayName()
        → назва для адмінкі: "АГЕНЦЫЯ — Суць працы — Горад"
            ↓
        formatTelegramPost() → Markdown
        (БЕЗ agencyName і templateName у посце!)
            ↓
        Захаванне ў MongoDB + адпраўка ў TG канал
            ↓
        Фонавы пошук: identifyTemplate()
            ├── Брэнд у тэксце → адразу ў AI (~3к токенаў, раней ~14к)
            ├── Знойдзены → запісаць ТОЛЬКІ templateId
            │   ⚠️ НЕ мяняць палі вакансіі!
            └── Не знойдзены → createTemplateFromVacancy() → новы шаблон
```

> ⚠️ **Крытычна важна:** Фонавы блок НЕ мяняе палі вакансіі — толькі запісвае `templateId`.
> Была праблема: VAC-0082 і VAC-0084 атрымлівалі дадзеныя з шаблона замест рэальнага тэксту — выпраўлена.

---

## 📐 СТРУКТУРА ДАДЗЕНЫХ v2.0 (Vacancy)

```js
{
  // Ідэнтыфікацыя
  agencyName,           // назва агенцыі
  templateName,         // назва шаблона/прадпрыемства
  vacancydescription,   // публічны загаловак (для кандыдатаў)
  vacancyCode,          // "VAC-0082"
  category,             // сфера работы
  keywords: [],
  contractType,
  status,               // active / archived / draft

  // Лакацыя
  location,             // горад (НЕ поўны адрас!) ← вядомая праблема
  locationDescription,
  checkInCity,
  voivodeship,

  // Зарплата
  salary: {
    baseNetto, studentNetto, hoursRange,
    payoutDates, bonusDetails, salaryNotes
  },

  // Графік
  schedule: {
    description, shiftsCount, hoursPerShift,
    workDaysWeek, breakDuration
  },

  // Жыллё
  accommodation: { type, forCouples, costRaw, details },

  // Транспарт
  transport: { provided, costRaw, details },

  // Патрабаванні
  requirements: {
    gender: [],         // масіў: ["М", "Ж"]
    ageMax,
    nationalities: [],
    standardDocs: [],
    polishLanguageLevel,
    physicalLoad
  },

  // Умовы
  conditions: {
    workwearFree,       // boolean
    foodType, foodDetails,
    specificNuances: [],
    specificConditionsDetails
  },

  // Выдаткі (блок у TG посце ТОЛЬКІ калі hasStartExpenses=true)
  startExpenses: { hasStartExpenses, details },

  // Адказнасць (блок у TG посце ТОЛЬКІ калі hasLiability=true)
  earlyTerminationLiability: { hasLiability, details },

  employerCompensations: { hasCompensations, details },

  // Для рэкрутэра (НЕ паказваецца кандыдатам)
  forRecruiter: {
    internalNotes,
    hideAgencyNameForCandidate,
    hideEnterpriseNameForCandidate
  },

  // Агульнае
  description, additionalNotes,
  arrivalDate, count,
  rawText, telegramPost
}
```

---

## ✅ ШТО БЫЛО ЗРОБЛЕНА (па ўсіх чатах)

### Backend

| Файл                  | Змены                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------ |
| `telegram.service.js` | `parse_mode`: HTML → Markdown                                                              |
| `telegram.service.js` | `notifyRecruiterAboutShortMessage`: `isLocalhost` праверка → розныя кнопкі                 |
| `ai.service.js`       | `FORMAT_PROMPT`: блок "Витрати" толькі пры `hasStartExpenses=true` або `hasLiability=true` |
| `ai.service.js`       | `identifyTemplate`: лакальны пошук выдалены, адразу ў AI. Токены: ~14к → ~3к               |
| `vacancies.js`        | Новы эндпоінт `POST /api/vacancies/from-template/:templateId`                              |
| `vacancies.js`        | `isSimpleMessage()` — фільтр нефарматыўных тэкстаў                                         |
| `vacancies.js`        | Выдалены аўта-мердж палёў з шаблонам у `processVacancyMessage`                             |
| `.env`                | Дадана `FRONTEND_URL=http://localhost:5173`                                                |

### Frontend

| Файл                    | Змены                                                                                                        |
| ----------------------- | ------------------------------------------------------------------------------------------------------------ |
| `VacancyViewModal.jsx`  | Поўны перапіс пад v2.0; кнопкі кандыдата закаменціраваны                                                     |
| `EditVacancyModal.jsx`  | Поўны перапіс пад v2.0; чэкбоксы для boolean палёў                                                           |
| `TemplateViewModal.jsx` | Поўны перапіс пад v2.0 + новы кампанент прагляду                                                             |
| `EditTemplateModal.jsx` | Поўны перапіс пад v2.0                                                                                       |
| `Vacancies.jsx`         | Картка: `v.title` → `v.templateName \|\| v.vacancydescription`; `applyFilters()` пад v2.0; рэжым "З шаблона" |
| `VacancyFilters.jsx`    | Мовы: нідэрл. → ням./англ.; давоз: "Ёсць/Няма"; нацыянальнасці: +Грузія, Казахстан, Азербайджан, Іншыя       |
| `Templates.jsx`         | Пошук па назве/фірме/горадзе/ключавых словах + кнопка 👁                                                     |
| `api.js`                | Дадаў `createVacancyFromTemplate`                                                                            |

### Шаблоны агенцый (seed-файлы)

| Файл          | Шаблонаў | Заўвага                |
| ------------- | -------- | ---------------------- |
| `manpower.js` | 46       | з 109, дублі аб'яднаны |
| `ralen.js`    | 15       | з 17                   |
| `solano.js`   | 3        | з 4                    |

**Агенцыі ў базе зараз:** PERSONNEL SERVICE (~50), EWL (~35), MANPOWER (46), BISAR, APOLO (~12), OTTO (~23), RALEN (15), SOLANO (3) = **~184–200+ шаблонаў**

---

## 🔴 БЯГУЧЫЯ ПРАБЛЕМЫ

### 1. Вакансіі перасталі апрацоўвацца ← ТЭРМІНОВА

**Сімптом:** У Telegram і базу нічога не прыходзіць.
**Дзе глядзець:** Лог Railway + `GROQ_API_KEY` у env.

### 2. `location` запісвае поўны адрас замест горада

**Сімптом:** `"Przemysłowa 12, 76-200 Głobino"` замест `"Słupsk"`
**Дзе выправіць:** `SYSTEM_INSTRUCTION` парсера ў `ai.service.js`

### 3. Паламаная вакансія `*[templateName]*` у TG канале

**Статус:** Крыніца не высветлена.

---

## 🟡 ТЭХДОЎГ (адкладзена)

### Backend

- [ ] `Template.js` — абнавіць мадэль пад v2.0
- [ ] `seedTemplates.js` — абнавіць пад v2.0
- [ ] AI промпты (`mergeWithTemplate`, `formatTelegramPost`, `parseVacancyWithAI`) — праверыць і абнавіць
- [ ] Мердж `from-template` — браць толькі ЗМЕНЕНЫЯ палі з новага тэксту (шаблон у базе не кранаць)
- [ ] `vacancydescription` фолбэк — `templateName + першы сказ description` (зараз можа вяртаць "Нова вакансія")

### Frontend

- [ ] `AddTemplateModal.jsx` — не праглядаўся, верагодна старая структура ⚠️
- [ ] Фільтр агенцый — цягнуць з базы дынамічна
- [ ] Фільтр "Еду" (Адзін/Пара/Сям'я) — логіка не вызначана
- [ ] Фільтр "Сфера" — уніфікаваць як AI запісвае `category`
- [ ] Выбар агенцыі пры рэдагаванні — dropdown замест free text
- [ ] `/templates?text=` і `/vacancies?updateText=` — апрацоўка query-параметраў з TG кнопак

### Кандыдаты (пазней)

- [ ] Кнопкі "Хачу працаваць" / "Дазнацца дэталі" — закаменціраваны ў `VacancyViewModal`
- [ ] Фронтэнд для кандыдатаў — без палёў `forRecruiter`
- [ ] `Candidates.jsx` і `matching.service.js` — не закраналіся

### Астатнія агенцыі (seed-файлы)

- [ ] PERSONNEL SERVICE, EWL, BISAR, APOLO, OTTO — генерацыя / рэфактарынг пад v2.0

---

## 🎯 ПРЫЯРЫТЭТЫ (з чаго пачынаць)

| #   | Задача                                      | Прыярытэт    |
| --- | ------------------------------------------- | ------------ |
| 1   | Дыягностыка: чаму вакансіі не апрацоўваюцца | 🔴 Тэрмінова |
| 2   | Выправіць `location` — горад замест адрасу  | 🔴           |
| 3   | Абнавіць `Template.js` пад v2.0             | 🟡           |
| 4   | Абнавіць `seedTemplates.js`                 | 🟡           |
| 5   | Абнавіць AI промпты                         | 🟡           |
| 6   | Праверыць `AddTemplateModal.jsx`            | 🟡           |
| 7   | Выправіць мердж `from-template`             | 🟡           |
| 8   | Фільтры: агенцыі з базы, сфера, еду         | 🟢           |
| 9   | Дадаць астатнія агенцыі ў seed-файлы        | 🟢           |
| 10  | Фронтэнд для кандыдатаў                     | 🟢 Пазней    |

---

## 🤝 ПРАВІЛЫ СУПРАЦЫ (для AI-агента — чытаць абавязкова)

1. **Мова:** Адказваць **толькі па-беларуску**
2. **Адзін крок за раз** — зрабілі → уладальнік пацвердзіў ("GO" або апісаў выніку) → наступны крок
3. **Заўсёды пытаць актуальны файл** перад зменамі — уладальнік мяняе код паміж сесіямі
4. **Не вынаходзіць кантэкст** — калі бачыш паляпшэнне, паведамі і чакай рашэння
5. **Не аптымізаваць самому** — мяняць строга тое, пра што дамовіліся
6. **Спачатку апісанне задачы + план** — потым код
7. **Папярэджваць** калі ідэя можа зламаць сістэму — прапанаваць альтэрнатыву
8. **Тлумачыць** кожны крок як для пачаткоўца
9. **Цэлы файл або лагічны блок** — не фрагменты; файлы да 300 радкоў цалкам
10. **Камітавацца** перад кожнай новай задачай

# Job Vacancy CRM - Guide

## Commands

- Dev: `npm run dev`
- Build: `npm run build`
- Python Tests: `D:/Projects/skills/venv/Scripts/python.exe ...`

## Tech Stack

- Frontend: React, Vite, Tailwind CSS
- Backend: Node.js, Express, MongoDB

## Testing Rules

- Use Playwright from the Skills repository.
- Always wait for `networkidle`.

## [14.04.2026] — Сесія: Пераход на архітэктуру v2.0

- **Зроблена:**
  - Распрацавана новая інструкцыя парсера (`vacancy_parser_v2.md`) з пашыранай схемай JS-аб'екта.
  - Сфармулявана новая логіка матчынгу: шаблон як даведнік, а не крыніца праўды.
  - Вызначаны прыярытэты па рамонце бэкенда.
  - Прынята рашэнне адмовіцца ад фільтрацыі "кароткіх паведамленняў" — парсім усё.

- **Бягучы стан:**
  - Вакансіі часова не даходзяць да базы/Telegram (патрэбна дыягностыка Railway).
  - Праблема з `location` (піша адрас замест горада).

- **Наступны крок:** Тэхнічны аўдыт сувязі GramJS -> Backend -> Groq.

## [15.04.2026] — Сесія: Аналітыка вакансій і фарміраванне Enums

### Выканана:

- **Аўтаматызацыя падрыхтоўкі даных**: Створаны Python-скрыпт `convert_docx_to_md.py` для пакетнай канвертацыі апісанняў вакансій з фармату `.docx` у `.md` (апрацавана 13 файлаў ад розных агенцый).
- **Глыбокі аналіз рынку**: Праз AI Studio (Gemini) праведзены поўны парсінг апрацаваных вакансій.
- **Фарміраванне даведнікаў (Enums)**:
  - Сфармаваны двухузроўневы спіс **сфер дзейнасці** (Логістыка, Харчовая, Аўта, Вытворчасць, Тэкстыль, Экалогія).
  - Складзена табліца **лакацый** (больш за 80 гарадоў з прывязкай да ваяводстваў).
  - Створаны рэестр **прадпрыемстваў і брэндаў** з прывязкай да гарадоў (LPP, Amazon, Valeo, Bosch і інш.).
- **Дакументацыя**: Створаны файл `frontend/instructions/vac-descr.md` (зводны тэкст) і абноўлены правілы структуравання даных для AI-агента.

### Наступныя крокі:

- Інтэграваць атрыманыя спісы ў кампанент `VacancyFilters.jsx` на фронтэндзе.
- Выкарыстоўваць дадзеныя лакацый і прадпрыемстваў для валідацыі ў `ai.service.js`.

## [15.04.2026] — Сесія: Паляпшэнне парсера v2.0 і ўніфікацыя катэгорый

- **Зроблена:**
  - Абноўлена `SYSTEM_INSTRUCTION` у `ai.service.js`: дададзены жорсткі спіс з 8 украінскамоўных катэгорый.
  - Укаранёны новы фармат `vacancydescription`: "Суть роботи (Підкатегорія) — Місто".
  - Уключана правіла `MAX DETAIL EXTRACTION`: парсер цяпер выцягвае дробныя дэталі (кошт харчавання, назвы аўтобусных маршрутаў, дэталізаваныя прэміі).
  - Паспяхова пратэставаная вакансія "Склад Gądki": парсер выцягнуў 100% інфармацыі, уключаючы графік руху транспарту і цэны ў сталоўцы.
- **Вынік:** Парсінг стаў глыбейшым, катэгорыі ўніфікаваныя пад будучыя фільтры фронтэнда.

## [2026-04-16] — Global Template & Database Refactoring

### Што было зроблена:

- **Уніфікацыя катэгорый:** Усе 279 шаблонаў пераведзены на новую сістэму з 8 строгіх катэгорый на ўкраінскай мове.
- **Стандартызацыя загалоўкаў:** - Пале `vacancydescription` зараз фармуецца па шаблоне: `Сутнасць (Падкатэгорыя) — Горад`.
  - Прыбраны назвы агенцтваў і лішні кантэкст з апісанняў.
- **Рэфактарынг назваў шаблонаў:** Поле `templateName` стала больш інфарматыўным для рэкрутэра (Брэнд + Горад + Сутнасць).
- **Стэрылізацыя базы дадзеных:**
  - Поўнасцю выдалены старыя вакансіі (калекцыя `Vacancies`).
  - Выдалены ўстарэлыя ўнікальныя індэксы MongoDB.
  - Калекцыя `Templates` перазапісана з нуля на аснове абноўленых `.js` файлаў.
- **Аўтаматызацыя:** Напісаны і паспяхова выкарыстаны скрыпты `patchTemplates.js` (для масавага рэдагавання файлаў) і `seedTemplates.js` (для чыстага імпарту ў БД).

### Вынік:

База шаблонаў утрымлівае **279** актуальных аб'ектаў, гатовых да працы з новымі фільтрамі на фронтэндзе.

## [2026-04-17] — State Synchronization & Testing Phase

### 📋 Актуальны стан праекта (Status Quo):

- **Database:** Стэрылізавана. Калекцыя `Vacancies` пустая, `Templates` утрымлівае 279 эталонных аб'ектаў.
- **Standards:** Усе шаблоны выкарыстоўваюць адзіную сістэму з 8 катэгорый і фармат загалоўкаў `Сутнасць (Падкатэгорыя) — Горад`.
- **Cleanup:** Старыя задачы па рэфактарынгу шаблонаў і выпраўленні дублікатаў лічацца **завершанымі**. Далейшыя працы будуць грунтавацца на гэтай чыстай базе.

### 🎯 Задачы на сёння:

1. Тэставанне скразнога шляху вакансіі: **Parsing -> Matching -> Saving**.
2. Падключэнне тэставай групы Viber праз Webhook.
3. Абнаўленне фронтэнд-фільтраў пад новыя катэгорыі.

## [2026-04-17] — New Architecture: Vacancy-Template Linking

### Змена логікі:

- **No Merging:** Адмова ад зліцця вакансіі з шаблонам пры парсінгу. Вакансія заўсёды захоўвае 100% свежага апісання.
- **Matching as Reference:** Шаблон цяпер выкарыстоўваецца як даведка (Reference). Калі супадзенне 100% — спасылка ў мадалцы. Калі <100%, але тая ж агенцыя/брэнд — блок "Падобныя вакансіі".
- **Self-Growing DB:** Калі матчынг не адбыўся — вакансія аўтаматычна становіцца новым шаблонам.
- **Age Policy:** Адмова ад значэння "60" па змаўчанні. Пераход на статус "Няма інфармацыі / Без абмежаванняў".

### Фронтэнд:

- Поўная лакалізацыя на ўкраінскую мову.
- Пераход на выпадаючыя спісы (dropdowns) для ўсіх шматварыянтных фільтраў.

## [2026-04-17] — Standards Consolidation & Recruitment Logic

### Дасягненні:

- **Master Data:** Сфармаваны эталонны спіс прадпрыемстваў, лакацый і дакументаў на аснове аналізу 279 шаблонаў.
- **Recruitment Tools:** Распрацаваны ўніфікаваны чэк-ліст кваліфікацыі кандыдата (украінская мова), які будзе інтэграваны ў карткі вакансій.
- **Architectural Pivot:** Зафіксавана новая логіка "Vacancy-Template Reference" замест Merging. Гэта гарантуе 100% дакладнасць апісання пры захаванні доступу да базы ведаў.
- **Age Policy Update:** Прынята рашэнне аб адыходзе ад статычнага значэння ўзросту (60) на карысць дынамічных статусаў ("Без обмежень", "Інформація відсутня").

### Тэхнічныя змены:

- Падрыхтаваны пераклады інтэрфейсу фільтраў на ўкраінскую мову.
- Сфармаваны спіс тэгаў для фільтра "Документи".

## [2026-04-17] — Backend Models Stabilization & AI Alignment

### ✅ Дасягненні (Backend):

- **Сінхранізацыя мадэляў:** Мадэлі `Vacancy.js`, `Template.js` і `Candidate.js` цалкам прыведзены да адзінага стандарту палёў.
- **Логіка Reference:** У мадэль `Vacancy.js` дададзена поле `relatedTemplates` (масіў ObjectIds). Гэта дазваляе звязваць вакансію з некалькімі падобнымі шаблонамі без зліцця тэкстаў (адпаведна PRD v2.0).
- **Age Policy (99):** У мадэлях і `ai.service.js` значэнне па змаўчанні для `ageMax` зменена з 60 на **99** (сістэмны маркер для "Без обмежень").
- **Кандыдацкая база:** - Мадэль `Candidate.js` пашырана палямі з чэк-ліста кваліфікацыі (зрок, тэмпературны рэжым, мануальныя навыкі).
  - Уніфікаваны блок `documents` у кандыдата для хуткага матчынгу з вакансіямі (`activeDocs` тэгі).
- **Data Integrity:** Поле `shiftsCount` замацавана як `Number` для захавання матэматычнай фільтрацыі, з логікай запісу максімальнай колькасці змен праз парсер.

### 🛠 Тэхнічныя праўкі:

- **ai.service.js:** Абноўлена функцыя `cleanData`, прыбрана прымусовая замена пустых значэнняў узросту на 60.
- **Indexes:** Падрыхтаваны поўнатэкставыя індэксы для шаблонаў для паляпшэння якасці пошуку AI-агентам.

## [2026-04-17] — Стандартызацыя даных v2.0 і паляпшэнне якасці парсінгу

### 🧠 Backend & AI Service

- **Мадэль Vacancy.js:** Дададзена поле `brand` (String). Цяпер назва прадпрыемства/завода захоўваецца асобна ад катэгорыі, што паляпшае пошук і аналітыку.
- **Абнаўленне AI Parser (ai.service.js):**
  - **Zero Loss Policy:** Уведзены жорсткія патрабаванні да захавання дэталяў (брэнды BMW/Tesla, этапы рэкрутацыі BHP/сумоўе).
  - **Bullet-point Formatting:** AI цяпер абавязаны выкарыстоўваць `;` для падзелу абавязкаў, што дазваляе фронтэнду выводзіць іх спісам.
  - **Geo-Standardization:** Аўтаматычны пераклад гарадоў на польскую мову (напр. "Варшава" -> "Warszawa") для бездакорнай працы фільтраў.
  - **Language Mapping:** AI мапіць апісальныя ўзроўні мовы на міжнародныя коды (A1, A2, B1, B2).
  - **Category Sync:** Укаранёны строгі спіс з 8 асноўных катэгорый на ўкраінскай мове.

### 🎨 Frontend & UI/UX

- **Рэдызайн VacancyViewModal.jsx:**
  - Структура цалкам перапрацавана пад логіку Telegram-поста.
  - Замена іконак на тэкставыя пазнакі ("Місто:", "Ставка:", "Набір:") для паляпшэння чытальнасці.
  - Дададзены візуальныя тэгі для катэгорыі і брэнда ў хэдэр мадалкі.
- **Новы EditVacancyModal.jsx:**
  - Створана форма рэдагавання з лагічным падзелам на секцыі (Аплата, Лакацыя, Жытло).
  - Рэалізавана функцыя глыбокага абнаўлення аб'ектаў (`handleChange` для ўкладзеных палёў).
- **Фільтрацыя (Vacancies.jsx):**
  - Функцыя `applyFilters` перапісана з пошуку падрадкоў на строгае параўнанне, што выправіла памылкі адлюстравання.
- **Master Data:** Створаны файл `constants/masterData.js`, які сінхранізуе значэнні фільтраў з выхадам AI на 100%.

### 🛠 Разбор тэхнічных памылак

- **Памылка 406 AUTH_KEY_DUPLICATED (GramJS):**
  - **Прычына:** Спроба адначасовага выкарыстання адной Telegram-сесіі ў розных месцах (Local vs Railway) або дубляванне працэсаў Node.js.
  - **Рашэнне:** Поўнае спыненне ўсіх працэсаў `node`, ачыстка файлаў сесіі і пры неабходнасці перавыпуск `STRING_SESSION`. Усталяваны прыярытэт: адна сесія — адзін актыўны IP.

# 📝 Справаздача: Аптымізацыя Telegram Userbot

**Дата:** 18 красавіка 2026 г.  
**Лог №:** 001

## 1. Выкананыя задачы (Changelog)

| Катэгорыя        | Дзеянне                                                                                      | Статус |
| :--------------- | :------------------------------------------------------------------------------------------- | :----- |
| **Аўтарызацыя**  | Вырашана памылка `406: AUTH_KEY_DUPLICATED` шляхам генерацыі новай сесіі.                    | ✅     |
| **Канфігурацыя** | Перанесены ўсе зменныя (`API_ID`, `API_HASH`) у `.env`.                                      | ✅     |
| **Стабільнасць** | Імплементаваны `Heartbeat` (кожныя 10 хв) праз `client.getMe()`.                             | ✅     |
| **Код**          | Выдалены дублікаты і выпраўлены парадак выканання (ініцыялізацыя -> аўтарызацыя -> сэрвісы). | ✅     |
| **Дэплой**       | Паспяховы рэдэплой на Render. Бот актыўны і падключаны да MongoDB.                           | ✅     |

---

## 2. Паточная архітэктура

- **Сэрвіс:** Node.js (Express) + GramJS.
- **Статус:** Анлайн (Render Free Tier).
- **Абарона ад сну:**
  1.  **Cron-job (Знешні):** HTTP-пінг кожныя 10 хвілін (забяспечвае актыўнасць працэсу).
  2.  **Heartbeat (Унутраны):** `setInterval` кожныя 10 хвілін (забяспечвае актыўнасць TCP-злучэння Telegram).

---

## 3. Інструкцыя па абслугоўванні (Troubleshooting)

Калі бот перастаў адказваць:

1.  **Крок 1 (Праверка):** Праверце логі на Render.
    - Калі бачыце `Deploying` — бот проста перазагружаецца, пачакайце.
    - Калі `429 Too Many Requests` — пачакайце, не чапайце код.
2.  **Крок 2 (Рэстарт):** Націсніце `Manual Deploy -> Clear Build Cache & Deploy` у Render Dashboard.
3.  **Крок 3 (Аўтарызацыя - крайняя мера):** Калі ў логах `Unauthorized` або `AUTH_KEY...` — запусціце `node generate_session.js` на сваім кампутары, атрымайце новы радок і абнавіце зменную `TELEGRAM_SESSION` у Render.

---

# Changelog

## [0.2.0] - 2026-04-18

### Fixed

- **Parser:** Resolved `baseNetto` assignment bug in `parseVacancyWithAI` where salary data was being lost or incorrectly mapped to a non-existent property.
- **Parser:** Fixed data loss issues by restoring full response structure using the spread operator (`...cleaned`) instead of rigid field mapping, ensuring all AI-generated fields are preserved.
- **Schema:** Added safe access checks (fallbacks like `|| {}`) for nested objects (`salary`, `schedule`, `accommodation`, etc.) to prevent crashes when the AI omits specific blocks.

### Refactored

- **Parser Logic:** Streamlined the `parseVacancyWithAI` function. Simplified the return structure to prioritize AI-provided data integrity while maintaining essential validation and formatting logic.
- **Salary Logic:** Implemented a robust fallback mechanism for `baseNetto` that automatically attempts to extract the rate from `salaryNotes` if the primary field is missing or marked as "не вказано".

## [0.2.1] - 2026-04-19

### Changed

- **Parser/Schema:** Enforced strict validation for the `requirements.polishLanguageLevel` field. The AI is now restricted to specific standardized codes ("Не вимагається", "A1", "A2", "B1", "B2", "C1").
- **Data Consistency:** Updated system prompt instructions to eliminate descriptive language (e.g., "рівень розуміння") in favor of fixed codes. This ensures that the data is ready for frontend filtering while maintaining a clear format for Telegram posts.

# 📝 Project Activity Log — Session: AI Parser & Formatter Refinement

> **Дата:** 19 красавіка 2026 г.
> **Аб'ект:** `backend/services/ai.service.js`
> **Вынік:** Пераход на стандарт парсінгу v2.0 (Update: Anti-Hallucination) і паляпшэнне Telegram-фарматавальніка.

---

## 🎯 Мэта сесіі

Выправіць памылкі інтэрпрэтацыі дадзеных AI-агентам (галюцынацыі), палепшыць геаграфічную дакладнасць і стварыць прафесійнае фарматаванне для Telegram-постаў.

---

## ✅ Тэхнічныя змены

### 1. AI Parser (`parseVacancyWithAI`)

- **Геаграфія (Strict Latin Rule):**
  - Уведзена жорсткае патрабаванне выкарыстоўваць толькі лацінскія сімвалы (A-Z) для польскіх гарадоў.
  - Забаронена дадаваць назву краіны "Polska" або "Poland" у поле `location`.
  - Дададзена кодавая страхоўка: `parsed.location.replace(/Polska,?\s*/gi, "")`.
- **Анты-галюцынацыя (Brand & Role Guard):**
  - Забаронена "прыдумляць" тып тавару (адзенне/абутак), калі гэтага няма ў тэксце. Пры адсутнасці дадзеных выкарыстоўваецца нейтральны загаловак: `Склад товарів (Логістика)`.
  - Забаронена ствараць вакансію "Водій", калі ў тэксце прысутнічае толькі расклад аўтобусаў.
- **Дагавор (Contract Integrity):**
  - Вернута логіка `contractType: null`, калі тып дагавора не згаданы ў арыгінале (AI больш не гадае).
- **Дубляванне (Data Hygiene):**
  - Уведзена правіла `STRICT NO-DUPLICATION` — інфармацыя, якая ўжо ёсць у структурных палях (зарплата, жыллё, давоз, адзенне), больш не трапляе ў `description` або `additionalNotes`.
- **Паўнавартаснасць:**
  - Удакладнена правіла для `additionalNotes`: сюды ідзе ўсё, што не ўлезла ў структуру (відэа, маршруты, этапы рэкрутацыі).

### 2. Telegram Formatter (`FORMAT_PROMPT`)

- **Выпраўленне структуры:** Выдалены інструкцыі па парсінгу, якія памылкова трапілі ў блок адлюстравання.
- **Блок зарплаты:** Дададзены асобныя пункты `• Бонуси` і `• Нотатки` для лепшай чытэльнасці.
- **Блок пражывання:** Уведзены падзел на `• Вартість` і `• Деталі`.
- **Умовы працы:** Палепшана адлюстраванне нюансаў харчавання і рабочага адзення.

---

## 📊 Вынікі тэстаў (Кейс Polkowice / CCC)

| Параметр         | VAC-0023 (Да выпраўленняў)             | VAC-0024 (Пасля выпраўленняў)            |
| :--------------- | :------------------------------------- | :--------------------------------------- |
| **Загаловак**    | Склад одягу (Логістика) — галюцынацыя  | **Склад товарів (Логістика)** — дакладна |
| **Лакацыя**      | Pольковіце (памылка транслітарацыі)    | **Polkowice** (чыстая лацінка)           |
| **Дагавор**      | Umowa zlecenie (прыдумана)             | **null** (адпавядае тэксту)              |
| **Фарматаванне** | Суцэльны тэкст у блоках                | **Структураваны спіс з булітамі**        |
| **Дублі**        | Паўторы пра давоз і адзенне ў нататках | **Чысты блок дадатковай інфармацыі**     |

---

## 🛠 Наступныя крокі

1.  **Message Stitching:** Распрацоўка механізму затрымкі (2-3 сек) для аб'яднання разбітых Telegram-паведамленняў у адзін кантэкст перад адпраўкай у AI.
2.  **Frontend Sync:** Праверка адлюстравання поля `contractType` у мадалках, калі яно прыходзіць як `null`.

---

**Log end.**

# Тэхнічны лог праекта: Інтэграцыя Viber

**Дата:** 20 красавіка 2026
**Статус:** У працэсе (Апошняя міля інтэграцыі Android-шлюза)

## 1. Кароткі агляд

Мэта праекта — наладзіць аўтаматычную перадачу ўваходных паведамленняў Viber у CRM-сістэму. Пасля няўдалых спроб прамога чытання лакальнай базы дадзеных (Viber PC/Windows), праект быў перанакіраваны на выкарыстанне Android-прылады ў якасці шлюза праз MacroDroid.

---

## 2. Храналагічны пратакол

| Этап       | Дзеянне                                          | Вынік                                                      |
| :--------- | :----------------------------------------------- | :--------------------------------------------------------- |
| **Этап 1** | Спроба прамога капіравання `viber.db`            | **Няўдача:** Файлы заблакаваныя, `file is not a database`. |
| **Этап 2** | Падключэнне праз `sql.Open` (DSN `_immutable=1`) | **Няўдача:** База зашыфравана (SQLCipher).                 |
| **Этап 3** | Дыягностыка структуры файлаў                     | **Вынік:** Паведамленні ў SQLCipher, доступ нерэнтабельны. |
| **Этап 4** | Пераход на Android + MacroDroid                  | **Поспех:** Апавяшчэнні паспяхова ловяцца сістэмай.        |
| **Этап 5** | Інтэграцыя з CRM (Бэкенд/Фронт)                  | **У працэсе:** Наладка фармату `x-www-form-urlencoded`.    |

---

## 3. Тэхнічная інструкцыя: Наладка перадачы дадзеных

Пасля аналізу праблем з парсінгам `JSON` у MacroDroid, мы пераходзім на больш стабільны фармат `application/x-www-form-urlencoded`.

### А. Наладка MacroDroid (HTTP Request)

## [20.04.2026] — Сесія: Інтэграцыя Viber і сістэма "Пясочніца"

### Выканана:

- **Інтэграцыя Viber (Мабільны мост)**:
  - Выяўлена шыфраванне лакальнай базы `viber.db` (SQLCipher), што зрабіла прамое чытанне немагчымым.
  - Рэалізаваны абыходны шлях праз **Android + MacroDroid**: тэлефон перахоплівае апавяшчэнні і адпраўляе іх на бэкенд праз HTTP POST.
  - Наладжана перадача даных у фармаце `application/x-www-form-urlencoded` для стабільнай апрацоўкі спецсімвалаў і эмодзі.

- **Бэкенд (Sandbox & API)**:
  - Створана мадэль `UnprocessedMessage` для захавання ўсіх уваходных паведамленняў "як ёсць".
  - Напісаны роўт `/api/inbox/push`, які здабывае тэкст паведамлення нават пры парушанай структуры запыту.
  - Укаранёна аўтаматычная класіфікацыя паведамленняў на **Вакансіі**, **Абнаўленні** і **Чат** па ключавых словах.

- **Фронтэнд (UI/UX)**:
  - Створана старонка **"Уваходныя"** для ручной апрацоўкі паведамленняў.
  - Рэалізавана логіка **"Стварыць вакансію"**: тэкст з Пясочніцы аўтаматычна пераносіцца ў форму AI-парсінга.
  - Дададзены лічыльнік непрачытаных паведамленняў у галоўнае меню (`Layout`).

### ⚠️ Бягучыя праблемы:

- **Дублікаты**: Пры паўторных апавяшчэннях у Android ствараюцца аднолькавыя запісы ў базе.
- **UI**: Вялікія карткі паведамленняў ствараюць занадта доўгую стужку (патрэбен Compact View).
- **Кіраванне**: Адсутнасць масавых аперацый (выдаліць усё / выбранае).

### Наступныя крокі:

- Укараніць дэдуплікацыю на бэкендзе (праверка па тэксце і часе < 10 хв).
- Стварыць `Bulk API` для масавага выдалення паведамленняў.
- Перавесці спіс уваходных у таблічны (кампактны) выгляд.

## [21.04.2026] — Сесія: Аптымізацыя Пясочніцы (Inbox) і Вайтліст чатаў

**Статус:** База ачышчана, наладжаны маппінг агенцый.

### Выканана:

- **Рэалізацыя Agency Mapping**: Створаны механізм ідэнтыфікацыі агенцый па назве чата. Цяпер паведамленні з Viber аўтаматычна атрымліваюць пазнаку `agencyName` (напр. "OTTO", "Global", "PERSONNEL SERVICE").
- **Строгая фільтрацыя (Whitelisting)**: Укаранёны спіс дазволеных чатаў. Паведамленні з іншых крыніц ігнаруюцца бэкендам (`status: not_whitelisted`), што эканоміць рэсурсы базы.
- **Глыбокі Noise-фільтр**: Напісаны Regex-патэрны для выдалення смецця:
  - Рэакцыі (эмодзі на паведамленні).
  - Сістэмныя паведамленні ("прыєднався до групи").
  - Пытанні пра кандыдатаў ("можу подати жінку").
  - Стандартныя фразы ("Дякую", "Доброго дня").
- **Масавыя аперацыі**: Дададзены API-эндпойнты:
  - `POST /api/inbox/cleanup` — для ачысткі базы ад назапашанага раней смецця.
  - `DELETE /api/inbox/bulk` — для масавага выдалення апрацаваных паведамленняў.
- **Абнаўленне мадэлі**: У `UnprocessedMessage` дададзена поле `agencyName` і палепшаны індэксы для дэдуплікацыі.

### ⚠️ Выяўленыя праблемы (патрабуюць вырашэння):

- **Telegram Crash**: Сэрвіс Telegram выдае памылку `RawMessage is not defined` пры спробе пераслаць паведамленне ў пясочніцу. Трэба праверыць імпарты ў `services/telegramListener.js` (або адпаведным файле).
- **Недакладнасць маппінгу**: Некаторыя чаты (напр. з эмодзі `🤝 Mrówki`) не распазнаюцца з-за розніцы ў прабелах або спецсімвалах.
- **Дэдуплікацыя**: Трэба павялічыць акно праверкі дублікатаў або зрабіць яго больш строгім па тэксце.

### План на наступную сесію:

1. Выправіць памылку `RawMessage is not defined` у Telegram-сервісе.
2. Аптымізаваць `CHAT_AGENCY_MAP`, выкарыстоўваючы больш простыя ключы для `includes()`.
3. Наладзіць фронтэнд (Inbox.jsx) для кампактнага адлюстравання паведамленняў з фільтрацыяй па агенцыях.

## [0.2.2] - 2026-04-21

### Added

- **Hard Filter System (`utils/messageFilters.js`)**: Створаны цэнтралізаваны сэрвіс фільтрацыі. Гэта "нулявы ўзровень" абароны, які выкарыстоўвае Regex для імгненнага адсячэння сістэмных паведамленняў (Viber/TG) і іншага шуму да таго, як паведамленне трапіць да AI.
- **Pipeline Integration**: У `routes/inbox.js` інтэграваны выклік `shouldIgnoreMessage()`. Цяпер сістэма прымусова правярае кожнае паведамленне на адпаведнасць патэрнам перад адпраўкай запыту ў Groq.
- **Regex-база**: Рэалізаваны пашыраны спіс `NOISE_PATTERNS` (на аснове `analyze.md`), які ўлічвае спецыфіку Viber-апавяшчэнняў і тэставых паведамленняў.
- **Centralized Config**: `CHAT_AGENCY_MAP` вынесены ў `messageFilters.js`, што выключае дубляванне логікі і спрашчае падтрымку спісу агенцый.

### Refactored

- **Routing Logic (`inbox.js`)**: Оптымізаваны шлях апрацоўкі паведамленняў. Сэрвіс імпартуе фільтры і адсякае смецце на ўваходзе, што значна эканоміць токены AI.
- **UI/UX (`Inbox.jsx`)**: Аптымізаваны кампанент прагляду ўваходных паведамленняў для больш хуткага апрацоўкі спісу.

### Fixed

- **Rate Limit Mitigation**: Дзякуючы ўкараненню Hard Filter, колькасць непатрэбных запытаў да AI скарочана (чакаецца зніжэнне спажывання токенаў на 70%+).

## [0.2.4] - 2026-04-23

### Added

- **API Interface:** Дададзены `aiUpdateVacancy` у `api.js` (PATCH /vacancies/:id/ai-update).
- **Error Handling (Backend):** - У `vacancies.js` дададзена "мяккая" апрацоўка памылак AI (429 Rate Limit), сістэма больш не выдае `500` пры перавышэнні лімітаў Groq.
  - Меры па захаванні цэласнасці дадзеных пры няўдалых спробах адпраўкі ў Telegram.

### Fixed

- **Deduplication:** У `backend/routes/inbox.js` інтэгравана лагіка `normalizeText` і `textHash`. Сістэма ігнаруе дублікаты (з улікам эмодзі і рэгістра) на працягу 1 гадзіны.
- **Frontend (Inbox):**
  - Выдалена катэгорыя "Chat" з фронтэнду (фільтрацыя ў `Inbox.jsx`).
  - Выпраўлена праца кнопкі "Абнавіць": дададзены `loading` state і карэктны сінхронны фетч дадзеных.
- **Frontend (Navigation):** - У `Vacancies.jsx` дададзены `useLocation` для захопу `initialText` з state. Мадалка стварэння вакансіі цяпер карэктна адкрываецца пасля пераходу з Інбокса.

### Logic

- **Filtering:** Уведзена жорсткая фільтрацыя шуму на ўзроўні роўтаў `inbox.js`.

## [0.2.5] - 2026-04-23

### Added

- **MacroDroid Bridge:** Рэалізацыя "No-Code" моста для перасылкі паведамленняў з Telegram і Viber на бэкенд праз HTTP POST.
- **Architecture Shift:** Пераход ад `GramJS` userbot да лакальнай аўтаматызацыі на прыладзе (MacroDroid) для забеспячэння стабільнасці.

### Changed

- **Deployment Strategy:** Юзербот (`userbot.js`) пераведзены ў пасіўны рэжым (дэпрэкейтыд), каб пазбегнуць рызыкі блакавання акаўнтаў праз API.
- **Data Flow:** Уніфікацыя ўваходных патокаў даных; бэкенд працягвае працаваць праз існуючыя эндпоінты `/push` (Viber) і `/push-userbot` (Telegram).

### Fixed

- **Risk Mitigation:** Вырашана праблема патэнцыйных банаў Telegram API праз адмову ад выкарыстання доўгажывучых userbot-сесій на карысць лакальнага чытання апавяшчэнняў.

## [0.2.6] - 2026-04-24

### Added

- **Groq Fallback:** Імплементавана логіка пераключэння мадэляў: пры памылцы 429 (ліміт `llama-3.3-70b`) сістэма аўтаматычна пераходзіць на хуткую `llama-3.1-8b`.
- **Frontend Filters:** Дададзены інтэрактыўныя фільтры ў `Vacancies.jsx` ("Давоз да працы", "Хто едзе").
- **UI Diagnostics:** У `VacancyViewModal` дададзены блок "Арыгінальны тэкст паведамлення" для зручнай праверкі працы AI.
- **Constants:** Файл `constants/masterData.js` папоўнены блокамі `TRANSPORT_OPTIONS` і `TRAVEL_GROUPS`.

### Fixed

- **Backend Stability:** Поўнае выдаленне `GramJS` і юзербота. Сервер ачышчаны ад састарэлага коду.
- **JSON Safety:** У `index.js` дададзены мідлвар для апрацоўкі `SyntaxError`. Бітыя JSON-пакеты больш не выклікаюць падзенне сервера.
- **Inbox UI:** Выдалена катэгорыя "Chat" з інтэрфейсу і статыстыкі. Рэалізавана функцыя масавага выдалення паведамленняў.
- **MacroDroid Integration:** Пераход макрасаў на фармат `application/x-www-form-urlencoded`, што канчаткова вырашыла праблему `Bad control character` пры перадачы тэксту.

### Logic

- **Manual Mode:** Уведзена глабальная канстанта `AUTO_PROCESS_VACANCIES = false`. Аўтаматычны парсінг адключаны; вакансіі збіраюцца ў Пясочніцы для ручнога кантролю.

## [0.2.7] - 2026-04-24

### Added

- **MacroDroid Integration:** Поўны пераход на фармат `application/x-www-form-urlencoded` для стабільнай перадачы тэксту без памылак JSON.
- **Groq Fallback:** Рэалізавана аўтаматычнае пераключэнне на мадэль `llama-3.1-8b` пры памылцы 429 (ліміт `70b`).
- **UI Enhancements:** У `VacancyViewModal` дададзены блок прагляду арыгінальнага тэксту паведамлення (Raw Text).
- **Debug:** Дададзены дэталёвыя логі для ўваходных MacroDroid запытаў.

### Fixed

- **Backend Architecture:** Поўнае выдаленне `GramJS` (юзербота), пераход на незалежную ад сесіі архітэктуру.
- **Stability:** Укаранёны `SyntaxError` мідлвар для прадухілення падзення сервера пры некарэктных сімвалах.
- **Inbox:** Створаны адзіны эндпоінт `/api/inbox/push` для ўніфікацыі ўсіх крыніц (Viber/Telegram).
- **Frontend:**
  - Выпраўлены канфлікт `useEffect` у `Vacancies.jsx`, які ламаў перадачу даных.
  - Абноўлена логіка фільтраў ("Давоз", "Хто едзе", "Агенцыя", "Брэнд").
  - Ачышчаны інтэрфейс: выдалена катэгорыя "Чат", пачынена кнопка масавага выдалення паведамленняў.

### Next Session (TODO)

- [ ] **Pending Messages:** Выпраўленне "вісячых" паведамленняў (перадача `messageId` для абнаўлення статусу `processed: true`).
- [ ] **Filters:** Фікс фільтра "Нацыянальнасць" (апрацоўка пустых масіваў/ключавых слоў).
- [ ] **Whitelist:** Наладка вайтліста для тэставых чатаў (напр. "Мур Іра").
- [ ] **Maintenance:** Чыстка MongoDB ад старых дублікатаў вакансій.

## [Snapshot] - Project File Structure (2026-04-25)

Актуальная структура праекта для хуткай навігацыі і кантролю файлавай сістэмы.

```text
.
./backend
./backend/analyze.md
./backend/chats.md
./backend/CLAUDE.md
./backend/data
./backend/data/patch
./backend/data/templates
./backend/generate_session.js
./backend/import-archive.js
./backend/index.js
./backend/models
./backend/models/Candidate.js
./backend/models/Template.js
./backend/models/UnprocessedMessage.js
./backend/models/Vacancy.js
./backend/package-lock.json
./backend/package.json
./backend/patchTemplates.js
./backend/result.json
./backend/routes
./backend/routes/apply.js
./backend/routes/candidates.js
./backend/routes/inbox.js
./backend/routes/templates.js
./backend/routes/vacancies.js
./backend/scripts
./backend/scripts/seedTemplates.js
./backend/services
./backend/services/ai.service.js
./backend/services/classifier.service.js
./backend/services/gemini.service.js
./backend/services/matching.service.js
./backend/services/telegram.service.js
./backend/swaggerConfig.js
./backend/userbot.js
./backend/utils
./backend/utils/messageFilters.js
./frontend
./frontend/CLAUDE.md
./frontend/diagnose_crm.py
./frontend/eslint.config.js
./frontend/index.html
./frontend/instructions
./frontend/instructions/job_categories_and_locations.md
./frontend/instructions/LOG.md
./frontend/instructions/PLAN.md
./frontend/instructions/PRD.md
./frontend/instructions/scripts
./frontend/instructions/SYSTEM_INSTRUCTIONS.md
./frontend/instructions/vac-descr.md
./frontend/instructions/vacancy_parser_v2.md
./frontend/package-lock.json
./frontend/package.json
./frontend/postcss.config.js
./frontend/public
./frontend/public/favicon.svg
./frontend/public/icons.svg
./frontend/README.md
./frontend/src
./frontend/src/App.css
./frontend/src/App.jsx
./frontend/src/assets
./frontend/src/components
./frontend/src/constants
./frontend/src/index.css
./frontend/src/main.jsx
./frontend/src/pages
./frontend/src/services
./frontend/tailwind.config.js
./frontend/vercel.json
./frontend/vite.config.js
./skills_tools
```

- **Structure-command**: find . -maxdepth 3 -not -path '/.' -not -path 'node_modules' -not -path 'dist' -not -path 'build'

## [0.2.8] - 2026-04-25

### Added

- **Whitelist (Layer 1 Filtering):** Імплементавана жорсткая фільтрацыя па давераных чатах (SG, STAFF POWER, Solano, OTTO і інш.) для адсеку 90% спаму.
- **Loop Protection:** Дададзена ігнараванне чата "NOVA WORK AGENCY" для прадухілення бясконцых цыклаў рэкурсіўнага парсінгу.
- **Gemini Fallback:** Наладжаны аўтаматычны ланцужок пераключэння мадэляў (Flash -> Flash-lite -> 2.0 -> 1.5). Сістэма захоўвае жывучасць пры памылках 503.

### Improved / Refactored

- **Architecture:** Поўны пераход на `MacroDroid` (адмова ад `GramJS`), што забяспечвае стабільнасць прыёму паведамленняў.
- **Deduplication:** Павялічана акно праверкі дублікатаў з 1 да 24 гадзін. `normalizeText` цяпер цалкам ачышчае тэкст ад эмодзі і сімвалаў, пакідаючы толькі літары і лічбы.
- **Inbox:** Уніфікацыя эндпоінта для Viber і Telegram (адзіны роўт для апрацоўкі), што спрасціла логіку падтрымкі.

### System Status

- **Backend:** Працуе на Render. `AUTO_PROCESS_VACANCIES = false` (рэжым "Пясочніца" для назірання за фільтрамі).
- **Filtering:** Працуе трохузроўневая абарона (Whitelist -> Regex -> AI-класіфікацыя).

### Next Steps (Roadmap)

- [ ] **Field Testing:** Маніторынг логаў Render і праверка траплянняў у вайтліст.
- [ ] **Data Cleanup:** Выпраўленне статусу `processed: true` пасля стварэння вакансіі (каб паведамленні знікалі з Пясочніцы).
- [ ] **UI/UX:** Аптымізацыя формы стварэння вакансіі (auto-fill з `translatedText` ад Gemini).
- [ ] **Auto-pilot:** Уключэнне `AUTO_PROCESS_VACANCIES = true` пасля паспяховага тэставання.
- [ ] **Frontend:** Даданне дынамічных фільтраў па агенцыях/брэндах.

## [2026-04-25] — Сінхранізацыя Інбокса і паляпшэнне фільтрацыі

### ✅ Што зроблена:

- **Сінхранізацыя Inbox-Vacancy:** Укаранёны механізм аўтаматычнай ачысткі Інбокса. Пры стварэнні вакансіі (аўта, з шаблона або ручной) зыходнае паведамленне і ўсе яго дублікаты па хэшы пазначаюцца як `processed: true`.
- **Разумная дэдуплікацыя:** Акно праверкі дубляў павялічана да **24 гадзін**. Функцыя нармалізацыі тэксту цяпер выдаляе ўсе сімвалы, акрамя літар і лічбаў, што выключае пропуск дубляў праз розныя эмодзі або прабелы.
- **Loop Protection:** Дададзены статус `IGNORE_SELF` для чата `NOVA WORK AGENCY`, каб бот не парсіў свае ўласныя пасты.
- **Фікс OTTO:** Выпраўлена памылка распазнавання чата OTTO (дададзена падтрымка кірылічных літар у назве).
- **Строгі AI:** Абноўлены промпт Gemini для адсячэння маркетынгавага шуму ("шмат вакансій", "дасылайце людзей") без канкрэтных дэталяў працы.

### 🛠 Тэхнічныя змены:

- **Backend:** У `vacancies.js` дададзена функцыя `markInboxMessageAsProcessed`.
- **Backend:** У `messageFilters.js` абноўлены `CHAT_AGENCY_MAP` (дададзены FREELANCE і кірылічны OTTO).
- **Frontend:** У `api.js` абноўлены метады стварэння вакансій для перадачы `messageId`.
- **Frontend:** У `Vacancies.jsx` інтэгравана перадача `sourceMessageId` праз state роўтара.

## [2026-04-25] — Стэрылізацыя базы і ўніфікацыя агенцый

### ✅ Што зроблена:

- **Чыстка БД:** Створаны і паспяхова запушчаны скрыпт `backend/scripts/clearVacancies.js`. База вакансій поўнасцю ачышчана (выдалена 64 аб'екты) для пачатку працы з чыстага ліста.
- **Стандартызацыя CHAT_AGENCY_MAP:**
  - Усе назвы агенцый прыведзены да адзінага стандарту (UPPERCASE).
  - Дададзена падтрымка кірылічнага напісання для OTTO ("отто для партнерів").
  - Абноўлены мапінг для Telegram: `rekruter freelancer` цяпер аўтаматычна пазначаецца як `SG`.
- **Уніфікацыя MANUAL:** Усталяваны адзіны фолбэк `MANUAL` для ўсіх выпадкаў, калі агенцыя не вызначана (замест разнабою з UNKNOWN).
- **Падрыхтоўка да UI-аптымізацыі:** Сфармулявана задача на ўкараненне ўмоўнага рэндерынгу (хаванне пустых палёў) і "жывога" лічыльніка Інбокса.

## [2026-04-25] — Аптымізацыя UI і кампактнасць Telegram

### ✅ Што зроблена:

- **Жывы лічыльнік Інбокса:** Укаранёна сістэма падзей (`CustomEvent`), якая дазваляе лічыльніку ў галоўным меню абнаўляцца імгненна пасля выдалення або апрацоўкі паведамленняў у Пясочніцы.
- **Кампактныя пасты ў Telegram:** Абноўлены `FORMAT_PROMPT` у `ai.service.js`. Цяпер AI аўтаматычна хавае назвы палёў (напр., "Давоз", "Жыллё", "Студэнты"), калі ў іх няма дадзеных. Посты сталі больш чытэльнымі і арганічнымі.
- **Фікс ESLint:** Выпраўлена памылка `cascading renders` у `Layout.jsx` праз асінхронны выклік пачатковай загрузкі статыстыкі.
- **Уніфікацыя MANUAL:** Поўнае выдаленне статусу `UNKNOWN` на карысць адзінага стандарту `MANUAL` для нераспазнаных агенцый.

### 🛠 Тэхнічныя змены:

- **Frontend:** `Layout.jsx` цяпер слухае падзею `inboxUpdated`.
- **Frontend:** `Inbox.jsx` і `Vacancies.jsx` генеруюць падзею `inboxUpdated` пасля паспяховых дзеянняў.
- **Backend:** Перапісана логіка фармату Telegram-поста для строгага захавання кампактнасці.

## [0.2.9] - 2026-04-27 — Наладка MacroDroid і ўключэнне аўтапарсінгу

### Дыягностыка і высновы

- **Зменныя MacroDroid:** Выяўлена, што `{not_text_big}` вяртае пусты радок для Viber і Telegram (Android не запаўняе поле BigText для гэтых дадаткаў). Адзіная рабочая зменная — `{notification}`, хаця яна абрэзае тэкст прыкладна на 200 сімвалах.
- **Прырода дублікатаў:** Пацверджана, што дублі генеруе не мэсенджар, а Android — MacroDroid трыгерыцца на кожнае абнаўленне апавяшчэння ў шторцы. Бэкэнд-дэдуплікацыя па хэшы справаляецца з гэтым на 100%.
- **Частковыя дублі:** Telegram спачатку адпраўляе кароткае апавяшчэнне (~200 сімв.), а праз 5-20 хвілін — поўнае. Гэта два розныя хэшы, таму абодва захоўваліся ў Пясочніцу як асобныя запісы.
- **Viber чат не лавіўся:** Апавяшчэнні з групавога чата не трапялі ў шторку Android, пакуль акаунт рэкрутэра не атрымаў правы адміністратара групы Viber.
- **Фільтрацыя ў MacroDroid:** Бясплатная версія не дазваляе дадаць умову параўнання зменнай у блоку "Умова Якщо" — катэгорыя "Змінна" адсутнічае. Фільтрацыя па вайтлісце пакінута цалкам на бэкэндзе.

### Added

- **Partial Deduplication Logic:** У `inbox.js` дададзена апрацоўка частковых дублікатаў. Калі прыходзіць поўнае паведамленне і за апошнія 5 хвілін з таго ж чата ўжо ёсць кароткае — існуючы запіс у Пясочніцы абнаўляецца да поўнага тэксту замест стварэння дубліката.

### Changed

- **MacroDroid — цела HTTP запыту:** Зменная тэксту зафіксавана як `{notification}` для абодвух макрасаў (Viber і Telegram). Усе зменныя выбіраюцца праз меню MacroDroid, а не ўпісваюцца ўручную.
- **Auto-pilot:** `AUTO_PROCESS_VACANCIES` зменена з `false` на `true`. Паведамленні з катэгорыяй `FULL_VACANCY` цяпер адразу ідуць на Groq і публікуюцца ў Telegram-канал. `UPDATE` і `RECRUITER_INFO` захоўваюцца ў Пясочніцу для ручной апрацоўкі.

### Fixed

- **Viber групавы чат:** Акаунт рэкрутэра прызначаны адміністратарам групы — апавяшчэнні цяпер трапляюць у шторку Android і лавяцца MacroDroid.

### System Status

- **MacroDroid:** Абодва макрасы (Viber і Telegram) працуюць стабільна. Фільтрацыя — цалкам на бэкэндзе.
- **Backend:** `AUTO_PROCESS_VACANCIES = true`. Чатырохузроўневая абарона: Whitelist → Дэдуплікацыя (24г + частковыя) → Regex → Gemini.
- **Inbox:** Захоўвае толькі `UPDATE` і `RECRUITER_INFO` для ручной апрацоўкі.

### Next Steps (Roadmap)

- [ ] **Маніторынг токенаў:** Праверыць расход Gemini + Groq пасля ўключэння аўтапілота.
- [ ] **Акно частковых дублікатаў:** Магчыма павялічыць з 5 да 20-30 хвілін, калі поўны тэкст з Telegram прылятае з вялікай затрымкай.
- [ ] **Тэставанне аўтапілота:** Праверыць якасць парсінгу Groq на рэальных вакансіях з вайтліставых чатаў.

## [2026-04-27] — Абарона ад дубляў і паляпшэнне класіфікацыі

- **Concurrent Protection:** Укаранёны `processingCache` у `inbox.js` для адсячэння адначасовых ідэнтычных запытаў (вырашана праблема дублявання вакансій пры адначасовых POST-запытах).
- **Truncation Guard:** Дададзена праверка `isTruncated`. Абрэзаныя паведамленні больш не праходзяць аўта-парсінг і заўсёды накіроўваюцца ў Пясочніцу для ручнога кантролю.
- **Gemini Logic:** Абноўлены промпт `gemini.service.js`. Спісы вакансій і кароткія паведамленні цяпер класіфікуюцца як `UPDATE`, што прадухіляе стварэнне некарэктных вакансій праз Groq.
- **Data Schema:** У мадэль `UnprocessedMessage` дададзены палі `rawText` (для ўкраінскага перакладу) і `isTruncated` (маркер паўнаты тэксту).

## [2026-04-27] — Сінхранізацыя мадэляў і ачыстка даных

- **Data Integrity:** Прыбрана значэнне ўзросту па змаўчанні (99). Цяпер `ageMax` захоўваецца як `null`, калі інфармацыя адсутнічае.
- **Model Sync:** У мадэль `Vacancy` дададзены палі `originalText` (арыгінал з MacroDroid) і `isTruncated` (маркер паўнаты даных).
- **AI Formatting:** Абноўлены `FORMAT_PROMPT` у `ai.service.js`. Цяпер блок з узростам цалкам хаваецца, калі ён не пазначаны або перавышае 65 гадоў.
- **Manual Agency:** Замацавана значэнне "Manual" як стандартнае для нераспазнаных агенцый.
- **Process Update:** Функцыя `processVacancyMessage` цяпер карэктна пракідвае арыгінальны тэкст і статус абрэзкі ў мадэль вакансіі.

## [2026-04-27] — Уніфікацыя патоку даных і абарона ад дубляў

- **Single Source of Truth:** Укаранёна поле `rawText` як асноўнае для ўсіх інтэрфейсаў (Пясочніца, Вакансіі, Telegram). Цяпер рэкрутэр працуе толькі з апрацаваным украінскім перакладам.
- **Concurrent Protection:** У `inbox.js` дададзены `processingCache` (Set), які блакуе адначасовыя ідэнтычныя запыты. Гэта цалкам вырашыла праблему стварэння 2-3 аднолькавых вакансій пры "гонцы запытаў".
- **Truncation Safety:** Уведзены маркер `isTruncated`. Калі паведамленне абрэзанае (ліміт Android), аўта-парсінг Groq забараняецца, а ў інтэрфейсе з'яўляецца папярэджанне ⚠️.
- **UI/UX Cleanup:**
  - Узрост па змаўчанні (99) выдалены. Цяпер поле `ageMax` захоўваецца як `null`.
  - У Telegram-посце і мадалцы вакансіі блок "Вік" цалкам хаваецца, калі дадзеныя адсутнічаюць або ўзрост перавышае 65 гадоў.
- **Gemini Refinement:** Абноўлены промпт класіфікацыі. Спісы вакансій і кароткія паведамленні цяпер строга маркіруюцца як `UPDATE`, што накіроўвае іх у Пясочніцу замест стварэння "пустых" вакансій.

### [0.3.0] - 2026-04-27 — Сэмантычная дэдуплікацыя і разумны вайтліст 23.38

#### Added

- **Semantic Deduplication (Gemini):** Укаранёна функцыя `analyzeAndCompareWithGemini`, якая параўноўвае новыя паведамленні з буферам за апошнія 12 гадзін. Сістэма цяпер аўтаматычна вызначае вердыкты: `DUPLICATE` (ігнараваць), `UPDATE` (у Пясочніцу) або `NEW` (на парсінг).
- **Aggressive Normalization:** Створана функцыя `superNormalize` у `messageFilters.js`. Яна пакідае ў назвах чатаў толькі літары і лічбы, што робіць вайтліст устойлівым да эмодзі, прабелаў і спецсімвалаў.

#### Changed

- **Inbox Pipeline:** Роўт `/push` цалкам перапрацаваны. Цяпер Groq запускаецца толькі для катэгорыі `FULL_VACANCY` з вердыктам `NEW`. Гэта цалкам вырашае праблему "размнажэння" вакансій (напр. VAC-0016...0019) пры дробных зменах тэксту.
- **Partial Duplicate Window:** Акно чакання поўнага тэксту (Telegram updates) павялічана з 5 да 60 хвілін.
- **Whitelist Logic:** Укаранёна двухбаковая праверка (Chat contains Key OR Key contains Chat) для максімальна дакладнага матчынгу агенцый.

#### Fixed

- **Works4you Matching:** Выпраўлена праблема ігнаравання чата "Works4you Вакансии в Польше" праз палепшаную нармалізацыю.
- **Token Optimization:** Скарочаны выдаткі токенаў Groq за кошт адсячэння сэмантычных дублікатаў на этапе Gemini.

## [0.3.1] - 2026-04-28 — Пакет стабілізацыі (Stabilization Phase)

### Fixed

- **Race Condition:** Укаранёна стратэгія "Save-First" у `inbox.js`. Паведамленні захоўваюцца ў базу адразу пасля Gemini, што блакуе дублікаты пры адначасовых запытах.
- **API Quota:** Пераход на мадэль `gemini-1.5-flash` з павялічанымі лімітамі запытаў.
- **Truncation Logic:** Абноўлена функцыя `isTruncated`. Цяпер паведамленні > 300 сімвалаў лічацца поўнымі без абавязковай кропкі ў канцы.
- **Atomic Codes:** Функцыя `generateVacancyCode` цяпер шукае максімальны нумар у базе, выключаючы памылку `E11000 duplicate key`.

### Added

- **Semantic Context:** Gemini цяпер параўноўвае новыя паведамленні не толькі з Пясочніцай, але і з апошнімі апублікаванымі вакансіямі.

## [0.3.2] - 2026-04-28 — Пакет стабілізацыі v2.0 (Production Ready)

### ✅ Backend & AI

- **Gemini v1 Migration:** Поўны пераход на стабільны API эндпоінт `v1`. Аптымізаваны спіс мадэляў (`1.5-flash`, `2.0-flash`).
- **Quota Protection (429):** Укаранёна логіка "Stop-on-429" у `gemini.service.js`. Пры вычарпанні квоты сістэма спыняе запыты і захоўвае паведамленне ў Пясочніцу без аўта-парсінгу.
- **Save-First Strategy:** У `inbox.js` зменена паслядоўнасць: паведамленне захоўваецца ў БД адразу пасля Gemini, што блакуе дублікаты пры адначасовых POST-запытах.
- **Atomic Vacancy Codes:** Функцыя `generateVacancyCode` перапісана на пошук максімальнага існуючага нумара (`sort: -1`), што выключае памылку `E11000 duplicate key`.
- **Age Policy Update:** Поўнае выдаленне дэфолтнага значэння `99`. Цяпер `ageMax` захоўваецца як `null`, калі дадзеныя адсутнічаюць.

### 🧹 Filtering & UI

- **Noise Reduction:** У `messageFilters.js` дададзены Regex-патэрны для адсеку сістэмнага шуму ("Ви маєте нові повідомлення", "Фотоповідомлення", "Ніден: закріплює").
- **Truncation Logic:** Абноўлены парогі `isTruncated`. Тэкст > 600 сімвалаў лічыцца поўным (BigText), < 300 сімвалаў без бяспечнага заканчэння — абрэзаным.
- **Compact Telegram Posts:** У `ai.service.js` абноўлены `FORMAT_PROMPT`. Блок "Вік" аўтаматычна хаваецца, калі значэнне `null`.
- **Whitelist Cleanup:** Выдалены нерэлевантны чат `BestWork`.

## [0.3.2] - 2026-04-28 — Пакет стабілізацыі v2.0 (Фінальныя праўкі)

### ✅ Backend & AI

- **Logging**: У `inbox.js` дададзены вывад тэксту паведамленняў, адхіленых Regex-фільтрам, для лепшай адладкі.
- **Age Policy**: Поўнае выкараненне значэння `99`. У `ai.service.js` укаранёна жорсткая ачыстка (cleanData) і абноўлена JSON-схема.
- **Formatting**: Палепшана інструкцыя `FORMAT_PROMPT` для пазбягання пустых пунктаў у спісе абавязкаў.
- **Truncation**: Абноўлена логіка `isTruncated` у `messageFilters.js` — цяпер яна больш адчувальная да паведамленняў без знакаў прыпынку ў канцы (прыкмета абрэзкі Android).

### 🧹 Filtering

- **Regex**: Палепшаны логі для адсочвання працы фільтраў шуму.
  [0.3.2] - Inbox Update: Added message length logging. Fixed category priority (truncated messages are now always 'vacancy'). Added smart fallback to identify vacancies when Gemini fails but text contains job keywords.

## [0.3.3] - 2026-04-28 — Пакет стабілізацыі v2.1 (Буфер і Groq Fallback)

### ✅ Backend & AI

- **Message Buffering**: Укаранёна сістэма назапашвання паведамленняў у `inbox.js`. Роўт `/push` цяпер толькі збірае даныя, а апрацоўка AI адбываецца раз у 2 хвіліны фонавым працэсам.
- **Stitching (Склейванне)**: Дададзена логіка абнаўлення існуючых паведамленняў у буферы. Калі на працягу 5 хвілін прыходзіць больш поўная версія тэксту (абнаўленне апавяшчэння), яна замяняе старую. Гэта вырашае праблему абрэзкі тэксту ў Telegram.
- **Groq Fallback**: У `gemini.service.js` дададзены фінальны этап ланцужка — класіфікацыя праз Groq (`llama-3.3-70b`). Цяпер, нават калі ўсе мадэлі Google выдадуць 429 памылку, паведамленне будзе апрацавана.
- **Smart Translation**: Створана функцыя `simpleTranslate` у `ai.service.js` для хуткага перакладу паведамленняў праз Groq, калі асноўныя мадэлі недаступныя.
- **Quota Optimization**: Укаранёна кэшаванне вынікаў Gemini на 5 хвілін і заменена логіка `break` на `continue` пры памылках 429 (RPM), што дазваляе выкарыстоўваць квоты розных мадэляў паслядоўна.

### 🧹 Filtering & Truncation

- **Truncation Logic**: Парог `isTruncated` падняты да 1200 сімвалаў. Логіка стала больш строгай: адсутнасць знакаў прыпынку ў канцы доўгага паведамлення цяпер заўсёды лічыцца абрэзкай.
- **Noise Reduction**: Дададзены новыя Regex-патэрны для адсеку сістэмнага шуму Viber ("Rakuten Viber", "відповідає: X місця").

## [0.3.4] - 2026-04-29 — Стабілізацыя буфера і Пясочніцы

### Added

- **Today Context**: Gemini цяпер атрымлівае ўсе паведамленні і вакансіі з пачатку бягучага дня (з 00:00:01) для максімальна дакладнай сэмантычнай дэдуплікацыі.
- **Date Filter**: У `messageFilters.js` дададзена функцыя `isOldMessage`, якая аўтаматычна ігнаруе паведамленні з датамі, старэйшымі за 2 дні ад сённяшняй.
- **Enhanced Logging**: У `inbox.js` укаранёны празрыстыя логі: цяпер відаць імя адпраўшчыка пры Regex-адмовах, ID запісу ў MongoDB і дэталёвыя AI-вердыкты (Category + Verdict).

### Fixed

- **Sandbox Visibility**: У роўце `GET /api/inbox` выдалены фільтр `processed: false`. Цяпер апрацаваныя паведамленні не знікаюць з фронту імгненна, што дазваляе рэкрутэру бачыць вынік працы AI.
- **AI Models Update**: У `gemini.service.js` выдалена састарэлая серыя 1.5. Усталяваны прыярытэт на актуальныя мадэлі красавіка 2026: `2.5-flash` і `2.0-flash`. Выпраўлены памылкі 404 праз пераход на `v1beta`.
- **Stitching & Throttling**: Акно абнаўлення паведамленняў (stitching) зафіксавана на 60 хвілінах. Уведзена штучная затрымка 2 секунды паміж запытамі да AI для захавання лімітаў RPM.
- **Truncation Threshold**: Парог `isTruncated` зніжаны з 1200 да 800 сімвалаў, што дазваляе большай колькасці інфарматыўных вакансій праходзіць на аўта-парсінг.

## [0.3.5] - 2026-04-30 — Стабілізацыя буфера і фінальная паліроўка AI

### ✅ Backend & AI (Стабілізацыя)

- **Deduplication Logic:** Выпраўлена памылка "бясконцага цыкла" (fallback loop) у `vacancies.js`. Цяпер сістэма правярае наяўнасць неапрацаванага дубліката па хэшы перад стварэннем новага запісу ў Пясочніцы.
- **AI Data Integrity:** Поўная перапіска функцыі `cleanData` у `ai.service.js`. Укаранёна ачыстка ад сістэмнага смецця AI ("99", "undefined", "не вказано") і выпраўлена памылка `Iterator value undefined`.
- **Compact Telegram Posts:** Узмацнены `FORMAT_PROMPT`. Дададзены жорсткія правілы на выдаленне пустых радкоў для палёў `👥 Набір` і `📄 Тип договору`, калі дадзеныя адсутнічаюць.
- **Inbox Refinement:**
  - Выпраўлена памылка прысваення катэгорыі: абразаныя паведамленні (`isTruncated`) цяпер атрымліваюць статус `vacancy` толькі пры пацверджанні ад AI.
  - Ліміт `isTruncated` усталяваны на 800 сімвалаў (верхні) і 50 сімвалаў (ніжні).
  - `isOldMessage` павялічаны да 3 дзён для захавання актуальнасці паведамленняў пасля выходных.

### 🧹 Filtering & Noise Reduction

- **Viber System Noise:** У `messageFilters.js` дададзены новыя рэгулярныя выразы для адсеку паведамленняў пра далучэнне карыстальнікаў да груп ("приєднався до .+/i").
- **Whitelist Accuracy:** Палепшана распазнаванне чатаў праз нармалізацыю назваў, што вырашыла праблемы з чатамі, якія змяшчаюць эмодзі.

### 🛠 Тэхнічныя змены

- **Logging:** Прэв'ю тэксту ў логах бэкенда (`logPreview`) зафіксавана на 80 сімвалах для зручнасці адладкі.
- **Buffer Processing:** Фонавая апрацоўка чаргі (`processPendingMessages`) настроена на інтэрвал 60 секунд з лімітам 10 паведамленняў за ітэрацыю.

## [0.3.6] - 2026-04-30 — Фінальная стабілізацыя фільтраў і бэкенда

### ✅ Frontend (Рэмонт фільтраў і UI)

- **Robust Filtering:** Поўная перапрацоўка `applyFilters` у `Vacancies.jsx`. Дададзена абарона ад `null` значэнняў, што выправіла памылку `Cannot read properties of undefined (reading 'includes')`.
- **Auto-Reset:** Укаранёны `useEffect` для аўтаматычнага прымянення фільтраў пры іх поўнай ачыстцы (больш не трэба націскаць кнопку пасля скіду).
- **Travel Group Logic:** Выпраўлена логіка "Хто едзе". Калі AI не пазначыў пол, вакансія аўтаматычна лічыцца даступнай для аднаго чалавека ("Адзін").
- **Nationality Logic:** Дададзены фолбэк на "Україна". Калі поле нацыянальнасці пустое, вакансія больш не знікае пры фільтрацыі.
- **International Locations:** У `dynamicData` і фільтрах укаранёны фармат `Горад (Краіна)` для вакансій па-за межамі Польшчы (напр. "Reinsdorf (Germany)").
- **Card UI:** У картку вакансіі дададзена адлюстраванне тыпу жытла (🏠 Платне/Безкоштовне) для хуткага азнаямлення.

### ⚙️ Backend (Аптымізацыя)

- **Deduplication Window:** Акно праверкі дублікатаў па хэшы павялічана з 1 да **12 гадзін**. Гэта дазваляе адсякаць паўторныя паведамленні на працягу ўсяго дня без звароту да AI.
- **Truncation Logging:** Дададзена празрыстае лагіраванне пры пропуску Groq-парсінгу для абразаных паведамленняў (`⏭️ Пропуск Groq: паведамленне абразанае`).
- **Fallback Loop Fix:** У `vacancies.js` узмацнена праверка на існуючыя неапрацаваныя паведамленні перад стварэннем fallback-запісу, што канчаткова спыніла "размнажэнне" паведамленняў пры памылках AI.

### 🤖 AI & Formatting

- **Strict Compactness:** Абноўлены `FORMAT_PROMPT` у `ai.service.js`. Блок `💸 Витрати та відповідальність` цяпер цалкам хаваецца, калі няма дадзеных, што выключыла з'яўленне фразы "(немає інформації)" у Telegram-постах.

## [0.3.7] - 2026-04-30 — Разумная дэдуплікацыя і давер да Тэлеграма

### Added

- **Smart Stitching (prefixHash):** У мадэль `UnprocessedMessage` дададзена поле `prefixHash` (першыя 150 сімвалаў). Гэта дазваляе сістэме адрозніваць розныя вакансіі ад адной агенцыі і склейваць толькі абнаўленні аднаго і таго ж паведамлення.
- **Composite Index:** Створаны індэкс `{ agencyName, prefixHash, createdAt }` для імгненнай праверкі дубляў у базе.

### Fixed

- **Truncation Bypass:** Функцыя `isTruncated` цяпер заўсёды вяртае `false` для крыніцы `telegram_userbot`, што дазваляе вакансіям з Тэлеграма адразу ісці на аўта-парсінг.
- **Safe Endings Regex:** Пашыраны спіс "бяспечных" заканчэнняў паведамленняў (дададзены літары і лічбы). Цяпер паведамленні, што заканчваюцца спасылкай, нумарам тэлефона або ключавым словам (CV, аванс), не лічацца абрэзанымі.
- **Groq Fallback Fix:** У `ai.service.js` выпраўлена функцыя `analyzeWithGroq`. Цяпер яна карэктна прымае параметр мадэлі, што дазваляе выкарыстоўваць хуткую `llama-3.1-8b` як фолбэк для Gemini.
- **Data Integrity:** Перапісана функцыя `cleanData` для больш надзейнай ачысткі AI-галюцынацый і прадухілення памылак пры апрацоўцы аб'ектаў.

### Changed

- **Inbox Pipeline:** Роўт `/push` пераведзены на параўнанне па `prefixHash`. Гэта вырашыла праблему "паглынання" новых вакансій старымі паведамленнямі ад той жа агенцыі.
- **Enhanced Logging:** У логі дададзена адлюстраванне крыніцы паведамлення `[source]` для лепшага маніторынгу патокаў Viber/Telegram.

## [0.3.8] - 2026-05-01 — Строгая класіфікацыя і гігіена Пясочніцы

### Added

- **Stricter Vacancy Rules:** Уведзены тэхнічны ліміт у 300 сімвалаў. Паведамленні, карацейшыя за гэты парог, аўтаматычна зніжаюцца да катэгорыі `update`, нават калі AI палічыў іх вакансіяй.
- **AI Alignment:** Промпты для Gemini і Groq (фолбэк) прыведзены да адзінага стандарту: для `FULL_VACANCY` цяпер абавязкова патрабуецца наяўнасць Пасады, Лакацыі і (Зарплаты або Апісання працы).

### Fixed

- **Import Error:** Выпраўлена памылка `ReferenceError: getPrefixHash is not defined` у роўце інбокса.
- **Sandbox Stats Sync:** Роўт `/stats` цяпер улічвае толькі неапрацаваныя паведамленні, што забяспечвае адпаведнасць лічыльніка ў меню рэальнаму спісу.

### Changed

- **Sandbox Hygiene:** Пясочніца (Inbox) цяпер адлюстроўвае толькі паведамленні са статусам `processed: false`.
- **Smart Auto-Done:** Паведамленне пазначаецца як апрацаванае (`processed: true`) і знікае з Пясочніцы толькі ў выпадку паспяховага аўта-парсінгу робатам, выяўлення дубліката або смецця. Кароткія апдэйты і інфа-радкі застаюцца для ручнога кантролю.

## [0.3.9] - 2026-05-01 — ID Discovery і прыярытэтная фільтрацыя па Chat ID

### Added

- **ID Discovery Mode:** У `userbot.js` дададзена функцыя `getDialogs`, якая пры старце сервера выводзіць у лог поўны спіс усіх чатаў (назва + ID), дзе прысутнічае акаўнт. Гэта дазваляе імгненна атрымаць дакладныя ID для вайтліста.
- **Chat ID Support:** У роўт `/push` і функцыю `getWhitelistedAgency` дададзена падтрымка `chatId`. Цяпер сістэма можа ідэнтыфікаваць агенцыю па ўнікальным ID чата, што на 100% абараняе ад памылак "Unknown" або змены назвы чата.

### Fixed

- **Logic Restoration:** Поўнасцю адноўлены ўсе спісы шуму (`SYSTEM_NOISE`, `RECRUITER_CHAT_NOISE`, `SOCIAL_NOISE`), функцыя праверкі старых паведамленняў `isOldMessage` і логіка затрымкі ў юзербоце. Усе фільтры працуюць у ранейшым рэжыме.

### Changed

- **Whitelist Priority:** Функцыя `getWhitelistedAgency` пераведзена на двухэтапную праверку: спачатку па ID (як самы надзейны метад для Telegram), потым па назве (як фолбэк для Viber).

## [0.4.0] - 2026-05-01 — Фіналізацыя вайтліста па Chat ID

### Added

- **Production Whitelist:** У `CHAT_AGENCY_MAP` дададзены рэальныя ID чатаў для SG, STAFF POWER, SOLANO і INTRASERVICE. Гэта забяспечвае 100% дакладнасць ідэнтыфікацыі агенцый у Telegram.

### Changed

- **Discovery Mode:** Блок аўтаматычнага вываду спісу чатаў у `userbot.js` закаментаваны для захавання чысціні логаў у працоўным рэжыме. Функцыянал захаваны для будучага выкарыстання.
- **Data Integrity:** Усе ID захоўваюцца як радкі (strings), што гарантуе карэктнае параўнанне з дадзенымі GramJS.

## [0.4.1] - 2026-05-01 — Стабілізацыя вайтліста і перадфільтрацыя шуму

### Fixed

- **Robust ID Matching:** Выпраўлена памылка ідэнтыфікацыі чатаў у Telegram. Функцыя `getWhitelistedAgency` цяпер выкарыстоўвае строгае параўнанне з ачысткай прабелаў (`trim`), што забяспечвае 100% трапленне для чатаў з вайтліста (напр. "Брыжук").
- **Pre-DB Filtering:** Узмацнена логіка адсеку смецця. Цяпер паведамленні, якія не прайшлі Regex-фільтры або вайтліст, наогул не трапляюць у базу дадзеных, што захоўвае Пясочніцу чыстай.

### Changed

- **Diagnostic Logging:** Дададзены вывад ID чата ў логі пры адмове па вайтлісце для палягчэння дыягностыкі новых крыніц.

## [0.4.2] - 2026-05-01 — Аптымізацыя лімітаў AI і чыстка логаў

### Added

- **Gemini Circuit Breaker:** Укаранёны механізм "замарозкі" мадэляў Gemini. Пры атрыманні памылкі `429` (Rate Limit) мадэль аўтаматычна ставіцца на паўзу да 07:00 раніцы наступнага дня. Гэта цалкам прыбрала спам памылкамі ў логах Render.
- **Smart Cooldown:** Функцыя `getMsUntilSevenAM` дынамічна вылічвае час адпачынку для кожнай мадэлі.

### Changed

- **Processing Interval:** Інтэрвал фонавай апрацоўкі паведамленняў у Пясочніцы павялічаны з 1 да **5 хвілін**. Гэта зніжае нагрузку на API і дазваляе больш эфектыўна выкарыстоўваць бясплатныя квоты.
- **Log Hygiene:** Поўныя JSON-аб'екты памылак Google выдалены з логаў, заменены на лаканічныя папярэджанні.

## [0.4.3] - 2026-05-02 — Аптымізацыя кампактнасці і ціхая апрацоўка

### Added

- **Silent Buffer Processing:** У `inbox.js` дададзена праверка колькасці паведамленняў перад запускам фонавага працэсу. Цяпер сістэма не спаміць у логі "ПАЧАТАК АПРАЦОЎКІ", калі чарга пустая.

### Fixed

- **Aggressive Data Cleaning:** Функцыя `cleanData` у `ai.service.js` цяпер больш жорстка выдаляе AI-смецце ("не вказано", "99", "undefined", "unknown"). Гэта забяспечвае чысціню даных у базе і карэктную працу фільтраў.
- **Telegram Compactness:** Перапісаны `FORMAT_PROMPT`. Цяпер цэлыя блокі (Пражыванне, Транспарт, Выдаткі) цалкам хаваюцца разам з загалоўкамі, калі ў іх няма карыснай інфармацыі. Посты ў Тэлеграм сталі выглядаць як натуральны тэкст, а не пустая анкета.
- **Salary Fallback:** У `parseVacancyWithAI` фолбэк для зарплаты зменены з тэкставага "не вказано" на `null`. Гэта выправіла памылку, калі ў Тэлеграм-посце выводзілася "Ставка: не вказано".

### Changed

- **Data Integrity:** Захавана поўная структура аб'екта ў базе (з `null` значэннямі) для бесперабойнай працы фронтэнд-фільтраў, пры гэтым забяспечана максімальная лаканічнасць пры вывадзе ў Тэлеграм.

## [0.4.4] - 2026-05-02 — Рамонт фільтраў і аптымізацыя выдаткаў AI

### Fixed

- **Frontend Filter Engine:** Поўная перапрацоўка функцыі `applyFilters` у `Vacancies.jsx`. Дададзена адсутная логіка для фільтраў: Транспарт, Дакументы, Асаблівасці (Чэк-ліст), Агенцыя і Брэнд. Цяпер фільтрацыя працуе строга па выбраных параметрах.
- **Dynamic Data Recovery:** Выпраўлена памылка знікнення спіса ваяводстваў. Цяпер `dynamicData` карэктна збірае ўнікальныя рэгіёны з масіва вакансій (улічваючы структуру v2.0).
- **AI Token Burner Fix:** У `inbox.js` зменена логіка выбару паведамленняў для аналізу. Цяпер паведамленні, якія ўжо былі апрацаваны AI (маюць запоўненае поле `rawText`), ігнаруюцца фонавым працэсам. Гэта спыніла бессэнсоўны паўторны аналіз "Абнаўленняў" кожныя 5 хвілін.
- **Accommodation Logic:** Выпраўлена інтэрпрэтацыя тыпаў жытла ў фільтрах. "Платнае" больш не трапляе ў катэгорыю "Без жытла".

### Added

- **Silent Mode:** Фонавы працэсар буфера цяпер працуе "ціха", калі ў базе няма новых паведамленняў для аналізу, што робіць логі сервера больш чытэльнымі.

## [0.4.5] - 2026-05-02 — Сінхранізацыя даных і пашырэнне геаграфіі

### Fixed

- **International Geography:** Укаранёна падтрымка вакансій па-за межамі Польшчы. Цяпер вакансіі з Германіі і іншых краін Еўропы аўтаматычна групуюцца ў рэгіён "Європа (інші країни)" і карэктна адлюстроўваюцца ў фільтрах.
- **Data Synchronization:** Абноўлены `masterData.js`. Назвы нацыянальнасцяў і дакументаў прыведзены ў поўную адпаведнасць з украінскамоўным выхадам AI (напр. "UA" -> "Україна", "Карта побыту" -> "Карта побуту"). Гэта выправіла памылку "нулявых" вынікаў у фільтрах.
- **Dynamic Nuances:** Фільтр "Асаблівасці (Чэк-ліст)" пераведзены на дынамічную мадэль. Цяпер ён паказвае толькі тыя тэгі, якія рэальна прысутнічаюць у вакансіях у базе (напр. "Запах гуми", "Холодны склад"), замест жорсткага спіса.
- **Accommodation Logic:** Удакладнена логіка фільтрацыі жытла. Цяпер сістэма дакладна адрознівае "Надаецца" (платнае/бясплатнае) ад "Уласнае" (власне).
- **ESLint Fix:** Выпраўлена памылка `no-undef` у `VacancyFilters.jsx` праз карэктную перадачу пропсаў з бацькоўскага кампанента.

### Changed

- **Filter Robustness:** Функцыя `applyFilters` цяпер больш устойлівая да пустых значэнняў і масіваў, што забяспечвае стабільную працу інтэрфейсу пры частковых даных.

## [0.4.6] - 2026-05-02 — Еўропа і катэгарызацыя нюансаў

### Added

- **Global Geography Support:** Уведзены стандартны рэгіён "Європа (інші країни)". Цяпер вакансіі з Германіі, Нідэрландаў і іншых краін аўтаматычна вызначаюцца парсерам і карэктна групуюцца ў фільтрах, нават калі дадзеныя ў базе былі частковымі.
- **Nuance Categorization:** Укаранёна сістэма катэгорый для нюансаў працы (Температурний режим, Запахи, Фізичне навантаження і інш.). AI цяпер фармуе тэгі ў фармаце "Катэгорыя (дэталь)", што дазволіла зрабіць фільтр на фронтэндзе чыстым і зручным.

### Fixed

- **Smart Filter Logic:** Функцыя `applyFilters` цяпер выкарыстоўвае частковае супадзенне для нюансаў (`startsWith`), што дазваляе фільтраваць па катэгорыі, бачачы пры гэтым поўныя дэталі ў картцы вакансіі.
- **Dynamic Data Recovery:** Палепшаны збор `dynamicData` для гарадоў і ваяводстваў. Цяпер назвы гарадоў для міжнародных вакансій аўтаматычна атрымліваюць прыстаўку краіны (напр. "Reinsdorf (Germany)"), што выключае блытаніну.
- **Master Data Sync:** Дададзена "Європа (інші країни)" у афіцыйны спіс ваяводстваў у `masterData.js`.

### Changed

- **AI Parser Instructions:** Абноўлены `SYSTEM_INSTRUCTION` для парсера. Цяпер ён абавязаны вызначаць краіну і прысвойваць еўрапейскім вакансіям адпаведны рэгіён, а таксама строга прытрымлівацца катэгорый у нюансах.

## [0.4.7] - 2026-05-02 — Сінхранізацыя тыпаў даных і аднаўленне формы рэдагавання

### Fixed

- **Validation Error (400 Bad Request):** Выпраўлена крытычная памылка пры захаванні вакансіі. Прычынай была неадпаведнасць тыпаў: палі `voivodeship` і `category` на фронтэндзе адпраўляліся як масівы, у то час як база даных чакала радок (String).
- **Data Type Alignment:**
  - Палі `voivodeship` і `category` пераведзены на адзінкавы выбар (SingleBtnGroup).
  - Поле `requirements.gender` цяпер карэктна апрацоўваецца як масіў, што адпавядае схеме `Vacancy.js`.
- **UI Restoration:** Поўнасцю адноўлены ўсе блокі формы рэдагавання, якія былі выпадкова выдалены ў папярэдняй ітэрацыі:
  - Выдаткі на старце і адказнасць (startExpenses, earlyTerminationLiability).
  - Кампенсацыі ад працадаўцы.
  - Пашыраныя патрабаванні (З дзецьмі, з жывёламі).
  - Нататкі для рэкрутэра.
- **Robust Data Prep:** Дададзена канвертацыя радкоў у масівы для палёў `keywords` і `specificNuances` непасрэдна перад адпраўкай на сервер, што гарантуе цэласнасць даных у MongoDB.

### Changed

- **Edit UI Components:** Уведзены падзел на `SingleBtnGroup` (для палёў з адным значэннем) і `MultiBtnGroup` (для масіваў), што зрабіла інтэрфейс рэдагавання больш інтуітыўным і тэхнічна правільным.

## [0.4.8] - 2026-05-03 — Паляпшэнне фільтрацыі шуму і дэдуплікацыі

### ✅ Backend (Фільтрацыя і стабільнасць)

- **Viber Noise Reduction:** У `messageFilters.js` дададзены новы рэджэкс патэрн для адсячэння апавяшчэнняў аб рэакцыях (`Реагує 💜 на...`). Гэта дазволіла цалкам прыбраць асабісты шум з Пясочніцы.
- **Whitelist Logic Fix:** Выпраўлена памылка ілжнага спрацоўвання вайтліста на кароткія назвы чатаў (напр. "То"). Выдалена зваротная праверка ўваходжання, якая памылкова матчыла кароткія назвы на доўгія ключы агенцый.
- **Extended Deduplication:** Акно праверкі дублікатаў па `prefixHash` у `inbox.js` павялічана з 12 да **48 гадзін**. Гэта забяспечвае надзейную абарону ад паўторных апавяшчэнняў MacroDroid, якія могуць прыходзіць пры абнаўленні шторкі Android праз доўгі час.

### 🛠 Тэхнічныя змены

- **messageFilters.js:** Абноўлена функцыя `getWhitelistedAgency` (строгае ўваходжанне ключа ў назву чата).
- **inbox.js:** Зменены інтэрвал `fortyEightHoursAgo` для пошуку існуючых паведамленняў у БД.

## [0.4.9] - 2026-05-03 — Аўта-архівацыя і стабілізацыя фільтраў

### ✅ Backend & AI

- **Regex Optimization:** У `messageFilters.js` дададзены якары `^` для сістэмных патэрнаў. Гэта выправіла памылковае выдаленне доўгіх вакансій, у сярэдзіне якіх сустракаліся ключавыя словы шуму.
- **Archive Policy:** Выдалена выдаленне паведамленняў праз `isOldMessage`. Цяпер старыя вакансіі праходзяць апрацоўку, але атрымліваюць статус `archive`.
- **AI Status Logic:** У `ai.service.js` укаранёна логіка: калі дата ў тэксце паведамлення старэйшая за май 2026, вакансіі прысвойваецца статус `archive`. Гэта сінхранізавана з фільтрамі на фронтэндзе.
- **Accommodation Fix:** У парсеры выпраўлена логіка "ўласнага жылля". Пры наяўнасці фразы "на власному житлі" тып аўтаматычна ставіцца `Не надається`, а дэфолтнае значэнне `Платне` выдалена.
- **Gemini Refinement:** Абноўлены промпт у `gemini.service.js` для больш дакладнай класіфікацыі `FULL_VACANCY`, што вырашыла праблему памылковага прысваення катэгорыі `UPDATE` для паведамленняў з MANUAL чатаў.

## [2026-05-03] — Рэфактарынг логікі жылля і адмена аўта-архівацыі

### ✅ Backend & AI

- **Абнаўленне AI-парсера (ai.service.js):**
  - Уведзены новыя тыпы пражывання: "Надається", "Надається (для пар)", "Не надається".
  - Поле `accommodation.forCouples` цяпер ставіцца ў `true` толькі пры наяўнасці наўпроставай інфармацыі аб жыллі для пар.
  - Дададзена падтрымка палёў `withChildren` і `withPets` (дзеці і жывёлы) у структуру пражывання.
  - Усе фінансавыя дэталі пражывання (кошт, даплаты) цяпер канцэнтруюцца ў полі `details`, поле `costRaw` больш не выкарыстоўваецца пры парсінгу.
- **Адмена аўта-архівацыі:** Выдалена логіка аўтаматычнага прысваення статусу `archived` па датах. Усе новыя вакансіі па змаўчанні атрымліваюць статус `active`.
- **Telegram Formatting:** Абноўлены `FORMAT_PROMPT` для больш кампактнага адлюстравання блока пражывання з улікам новых палёў (Дзеці/Жывёлы).
- **Геаграфія:** Замацавана правіла прысваення рэгіёна "Європа (інші країни)" для ўсіх вакансій па-за межамі Польшчы.

## [0.4.9-hotfix] - 2026-05-04 — Крытычны фікс буфера і фільтраў

### 🔴 Backend (Крытычны)

- **Buffer Processor Fix (`inbox.js`):** Выпраўлена крытычная памылка,
  з-за якой фонавы працэсар ніколі не знаходзіў паведамленні для
  аналізу. Прычына: схема `UnprocessedMessage` мае `rawText: { default: "" }`,
  таму поле існуе адразу пасля стварэння, і запыт
  `rawText: { $exists: false }` заўсёды вяртаў пусты масіў.
  Выпраўлена на `rawText: ""`. Цяпер AI-аналіз і Groq-парсінг вакансій
  працуюць штатна.

### 🟡 Backend (Фільтрацыя шуму)

- **Recruiter Chat Noise (`messageFilters.js`):** Дададзены новыя
  паттэрны ў `RECRUITER_CHAT_NOISE` для адсячэння чату рэкрутэраў
  паміж сабой: запыты аб датах выезду, наяўнасці месцаў, пацверджанні
  прыездаў і г.д. Гэтыя паведамленні прыходзілі ад белых агенцый і
  праходзілі ўсе папярэднія фільтры.
- **Viber System Noise (`messageFilters.js`):** Дададзены паттэрны ў
  `SYSTEM_NOISE` для сістэмных апавяшчэнняў Viber
  ("Отримання останніх повідомлень", "Повернутися у Viber" і інш.),
  якія раней дасягалі стадыі праверкі вайтліста.

### 🔴 Frontend (Крытычны)

- **Archive Status Mismatch (`masterData.js`):** Выпраўлена разбежнасць
  значэнняў статусу. Значэнне `"archive"` выпраўлена на `"archived"`,
  каб адпавядаць enum у схеме `Vacancy.js`. Фільтр "Архіў" раней не
  знаходзіў ніводнай вакансіі.

### 🟡 Frontend (Фільтры)

- **TravelGroup "alone" Filter (`Vacancies.jsx`):** Выпраўлена лагічная
  памылка фільтра "Адзін/Адна". Раней умова `isAlone` была `true` для
  амаль кожнай вакансіі (бо большасць мае `["Чоловіки", "Жінки"]` у
  gender). Цяпер: `isAlone = !isCouples && !isFamily`.
- **Accommodation "none" Filter (`Vacancies.jsx`):** Выпраўлена логіка
  фільтра "Без жылля". Раней вакансіі з пустым полем `accommodation.type`
  памылкова трапялі ў катэгорыю "Без жылля". Цяпер фільтр правільна
  лавіць толькі тыя, дзе тып утрымлівае "власн" або "не надаєт".

### 🟢 Frontend (Якасць кода)

- **aiUpdateVacancy + messageId (`Inbox.jsx`):** Выпраўлена перадача
  `messageId` у выклік `aiUpdateVacancy`. Раней `messageId` не
  перадаваўся, і пазначэнне паведамлення як апрацаванага выконвалася
  асобным ручным выклікам `markInboxProcessed`. Цяпер сервер сам
  пазначае паведамленне, а ручны выклік выдалены — гэта ліквідавала
  рызыку "завіслых" паведамленняў пры сервернай памылцы.

## [0.5.0] - 2026-05-04 — Мульці-вакансіі і Google Docs інтэграцыя

### Added

- **Multi-Vacancy Parser (`ai.service.js`):** Новая функцыя
  `parseMultipleVacancies`. Перад парсінгам AI падлічвае колькасць
  асобных вакансій у паведамленні. Калі больш за 1 — вяртае масіў
  аб'ектаў, кожны з поўнай структурай v2.0. Фолбэк на адзінарны
  парсінг калі падлік не ўдаўся.
- **Batch Processing (`vacancies.js`):** `processVacancyMessage` цяпер
  апрацоўвае масіў вакансій у цыкле. Кожная атрымлівае свой
  `vacancyCode`, свой Telegram-пост і захоўваецца незалежна.
  Паўза 1.5с паміж публікацыямі каб не спаміць канал.
- **Google Docs Enrichment (`ai.service.js`):** Новая функцыя
  `enrichTextWithDocs`. Аўтаматычна знаходзіць спасылкі на Google Docs
  у тэксце паведамлення, загружае змест праз публічны export API
  (`/export?format=txt`) і злучае з арыгінальным тэкстам перад
  парсінгам. Арыгінальны кароткі тэкст заходзіць першым (ён з'яўляецца
  актуалізацыяй для поўнага апісання).

### Fixed

- **Agency Detection (`vacancies.js`):** `finalAgency` цяпер выкарыстоўвае
  `vacancyData.agencyName` (з AI) як запасны варыянт — да фолбэку
  "Manual". Гэта выправіла памылку, калі ручнае стварэнне вакансіі
  з тэкстам "Агенцыя: APOLO" заўсёды давала "Manual".
- **Country Field (`ai.service.js`):** `country` у пост-апрацоўцы
  выпраўлена з `"Polska"` (жорсткі) на `cleaned.country || "Polska"`.
  Цяпер нямецкія і галандскія вакансіі захоўваюць правільную краіну.

## [0.5.1] - 2026-05-05 — Ажыўленне аўтаматыкі, мульці-парсінг і Google Docs

### Added

- **Падыход "Разбі і Парсі":** У `ai.service.js` дададзена функцыя `splitMultipleVacancies`. Яна выкарыстоўвае хуткую мадэль AI для разрэзкі доўгіх паведамленняў на асобныя лагічныя блокі (вакансіі).
- **Google Docs Enrichment:** Новая функцыя `enrichTextWithDocs` аўтаматычна знаходзіць спасылкі на дакументы Google, спампоўвае іх змест і дадае да арыгінальнага тэксту перад парсінгам.
- **Multi-parsing Coordinator:** Функцыя `parseMultipleVacancies` цяпер каардынуе ўвесь працэс: загрузка докаў -> разбіўка на часткі -> паслядоўны парсінг кожнай часткі асноўным AI-парсерам.

### Fixed

- **Background Processor Fix (`inbox.js`):** Выпраўлена крытычная памылка запыту ў БД. Робат ігнараваў паведамленні з-за няправільнай праверкі поля `rawText`. Цяпер фонавая апрацоўка працуе штатна.
- **Agency Detection Priority:** У `vacancies.js` выпраўлена логіка прысваення агенцыі. Цяпер назва, знойдзеная AI (`vacancyData.agencyName`), мае прыярытэт перад дэфолтным "Manual", калі паведамленне прыйшло не з вайтліста.
- **International Geography:** У `ai.service.js` прыбраны жорсткі перазапіс краіны на "Polska". Цяпер вакансіі з Германіі, Нідэрландаў і іншых краін захоўваюць сваю сапраўдную краіну.
- **Viber/Recruiter Noise:** У `messageFilters.js` дададзены новыя патэрны для адсячэння анкет кандыдатаў (ПІБ + пашпарт) і тэхнічнай перапіскі рэкрутэраў ("погодьте їх", "звʼяжіться з ним").

### Changed

- **Batch Processing:** Функцыя `processVacancyMessage` пераведзена на цыкл. Калі ў адным паведамленні знойдзена 3 вакансіі, сістэма створыць 3 асобныя карткі ў базе і адправіць 3 пасты ў Telegram з інтэрвалам у 1.5 секунды.

## [0.5.2] - 2026-05-05 — Дэбаг уваходнага трафіку і фікс прыярытэту агенцыі

### Added

- **Raw Traffic Logging:** У `inbox.js` дададзены лог `📡 RAW PUSH`. Цяпер кожнае паведамленне, якое прыходзіць ад юзербота або MacroDroid, фіксуецца ў логах Render яшчэ да пачатку фільтрацыі. Гэта дапаможа зразумець, чаму некаторыя чаты "маўчаць".
- **Immediate Startup:** Робат у `inbox.js` цяпер запускае апрацоўку паведамленняў адразу пры старце сервера, не чакаючы першых 5 хвілін інтэрвалу.

### Fixed

- **Manual Agency Override:** У `vacancies.js` маршрут `/auto` абноўлены для прыёму `agencyName` з фронтэнда. Гэта дазваляе пазбегнуць прымусовага прысваення "Manual", калі карыстальнік сам выбірае агенцыю ў форме або калі AI паспяхова яе вызначыў.

## [0.5.3] - 2026-05-06 — Фільтрацыя EN-шуму, MULTI_VACANCY, прэ-валідацыя

### ✅ Backend (Фільтрацыя)

- **EN Noise Filter (`messageFilters.js`):** Дадан масіў `RECRUITER_CHAT_NOISE_EN`
  з паттэрнамі для англамоўнай перапіскі рэкрутэраў (кароткія пытанні,
  пацверджанні, анкеты кандыдатаў, @мэнцыі). Зменшае шум з INTRASERVICE
  і іншых чатаў якія вядуць перапіску па-англійску.

### ✅ Backend (Класіфікацыя)

- **MULTI_VACANCY (`gemini.service.js`):** У промпт Gemini дадана новая
  катэгорыя `MULTI_VACANCY` для паведамленняў якія змяшчаюць 2+ асобных
  вакансій з уласнымі пасадай/лакацыяй/стаўкай (напр. SG-пакеты).
  Gemini вяртае `vacancyCount` для дыягностыкі.
- **Prompt Refinement (`gemini.service.js`):** Дакладнізаваны крытэрыі
  `FULL_VACANCY` — цяпер патрабуе ўсе тры элементы (пасада + лакацыя + стаўка)
  для АДНОЙ вакансіі. Кароткія спісы з лакацыямі ("Eurocash Lublin 2ч")
  і анкеты кандыдатаў выразна адносяцца да `UPDATE`.

### ✅ Backend (Апрацоўка)

- **`hasMinimalVacancyData` (`inbox.js`):** Новая функцыя прэ-валідацыі
  перад запускам Groq. Правярае наяўнасць стаўкі (zł/€) і лакацыі (📍).
  Калі дадзеных недастаткова — зніжае катэгорыю з `vacancy` да `update`
  і пазбягае стварэння пустых вакансій (тып VAC-0061).
- **MULTI_VACANCY routing (`inbox.js`):** Паведамленні з катэгорыяй
  `MULTI_VACANCY` аўтаматычна накіроўваюцца ў `processVacancyMessage`
  (сплітэр + Groq), абыходзячы `hasMinimalVacancyData` праверку,
  бо кожная з вакансій у пакеце мае поўную структуру.

### 🐛 Fixes

- **VAC-0061 root cause:** Паведамленні тыпу "Актуалізація вакансій:
  Eurocash — Lublin 2ч, Kraków 2ч..." больш не будуць класіфікавацца
  як `FULL_VACANCY` і ствараць пустыя карткі вакансій.

## [0.5.3-hotfix] - 2026-05-06 — Крытычны фікс цыкла і санітайзер Markdown

### 🔴 Backend (Крытычны)

- **`isAutoDone` declaration (`inbox.js`):** Выпраўлена адсутнасць аб'яўлення
  `let isAutoDone = false` унутры цыкла `for (const msg of pending)`.
  Раней пераменная не скідалася паміж ітэрацыямі, што магло прывесці
  да `ReferenceError` і паўнага спынення фонавага працэсара.

### 🟡 Backend (Якасць)

- **Markdown Sanitizer (`vacancies.js`):** Дадана функцыя
  `sanitizeTelegramMarkdown`. Выдаляе незакрытыя `**`, `_`, `` ` ``,
  `[` перад адпраўкай у Telegram. Вырашае памылку
  `❌ Памылка Markdown` якая выклікала фолбэк на plain text
  і страту фарматавання паста. Санітайзер ужываецца ў
  `processVacancyMessage` і ў маршруце `ai-update`.

## [0.5.4] - 2026-05-06 — Фікс cooldown Gemini і пашырэнне фільтраў

### 🔴 Backend (Крытычны)

- **Gemini Cooldown Fix (`gemini.service.js`):** Выпраўлена логіка
  `getMsUntilSevenAM`. Раней замарозка заўсёды ставілася на
  **заўтра 07:00**, нават калі бягучы час 06:59 або 08:00.
  Цяпер: калі 07:00 сёння яшчэ не наступіла — замарожвае да сёння,
  інакш да заўтра. Гэта выправіла сітуацыю калі пасля рэстарту
  ўсе Gemini-мадэлі адразу замарожваліся паўторна.

### 🟡 Backend (Фільтрацыя)

- **UA Recruiter Noise (`messageFilters.js`):** Дададзены ўкраінскія
  паттэрны для рэкрутэрскага шуму: кароткія просьбы ("Цю беремо теж?",
  "тільки цю жіночку додати", "також внесіть"), анкеты кандыдатаў
  (ПРІЗВИЩЕ ІМʼЯ + пашпарт + email), сацыяльныя прывітанні
  ("доброго ранку", "добрий день").
  Зменшае смецце з INTRASERVICE і аналагічных чатаў.

## [0.5.6] - 2026-05-06 — Фікс сплітэра, sourceBlock, лакацыі

### 🔴 Backend (Крытычны)

- **Splitter max_tokens (`ai.service.js`):** Дадан `max_tokens: 8000`
  у выклік Groq у `splitMultipleVacancies`. Раней Groq абрэзаў адказ
  па дэфолтным ліміце і вяртаў толькі 5 з 12 вакансій пакета SG.
  Таксама ўваход абмежаваны да 12000 сімвалаў каб не перавысіць
  кантэкстнае акно `llama-3.1-8b-instant`.

- **Per-vacancy sourceBlock (`ai.service.js` + `vacancies.js`):**
  `parseMultipleVacancies` цяпер вяртае `{data, sourceBlock}` для
  кожнай вакансіі. `processVacancyMessage` захоўвае ў `originalText`
  толькі блок тэксту канкрэтнай вакансіі. Выправіла сітуацыю
  калі ўсе 5 вакансій SG захоўвалі ў `originalText` увесь пакет з 12.

### 🟡 Backend (Якасць парсінгу)

- **International Location (`ai.service.js`):** У `SYSTEM_INSTRUCTION`
  дадана правіла 4: для ўсіх краін акрамя Польшчы горад пішацца
  лацінкай з краінай у дужках ("Corsica (France)"). Выправіла
  "Корсика" кірыліцай у VAC-0065 і адсутнасць краіны ў VAC-0063..0067.

- **Transport formatting (`ai.service.js`):** У `FORMAT_PROMPT`
  дадана правіла транспарту: "Власний" → "Довіз: немає",
  "надається" → "Довіз: надається". Выправіла незразумелы
  "Давіз: Власний" у TG-постах.
  ## [0.5.7] - 2026-05-06 — Фікс бясконцай петлі і лімітаў AI

### 🔴 Backend (Крытычны)

- **Processing Loop Fix (`inbox.js`):** Выпраўлена бясконцая петля
  рэапрацоўкі пры рэстарце сервера. Паведамленне цяпер адразу
  атрымлівае `rawText: "__processing__"` у пачатку апрацоўкі,
  да выкліку AI. Гэта выключае яго з чаргі `rawText: ""`
  і не дазваляе паўторна апрацоўваць пры наступным старце.

- **Retry Limit (`inbox.js`):** Дадана лічылка `retryCount`.
  Паведамленні якія 5 разоў запар не прайшлі AI-аналіз з-за
  лімітаў аўтаматычна пазначаюцца як апрацаваныя і хаваюцца.
  Гэта выправіла сітуацыю калі 7+ паведамленняў APOLO
  блакавалі чаргу і перашкаджалі апрацоўцы новых.

### 🟡 Backend (Фільтрацыя)

- **UA Noise Patterns (`messageFilters.js`):** Дададзены паттэрны
  для ўкраінскага рэкрутэрскага шуму які прайшоў праз папярэднія
  фільтры: "так так", "чекаю відповідь", "впишіть в систему",
  "буде кімната", "залежить хто перший" і інш.

## [0.5.9] - 2026-05-06 — Фікс сплітэра і аптымізацыя фільтраў

### 🔴 Backend (Крытычна)

- **Splitter Token Limit (`ai.service.js`):** Дададзены `max_tokens: 8000`
  у функцыю `splitMultipleVacancies`. Гэта выправіла праблему, калі
  пры апрацоўцы вялікіх пакетаў (напрыклад, SG на 12 вакансій)
  Groq абразаў JSON-адказ, што прыводзіла да страты часткі вакансій.

### 🟡 Backend (Фільтрацыя)

- **Recruiter Noise Regex (`messageFilters.js`):** Прыбрана жорсткая
  прывязка да пачатку радка (`^`) у масіве `RECRUITER_CHAT_NOISE`.
  Цяпер фільтр лепш адсякае шум у чатах (асабліва INTRASERVICE),
  нават калі паведамленне пачынаецца з эмодзі або згадкі імя.

### 🟢 Backend (Стабільнасць)

- **Full Text Processing (`inbox.js`):** Пацверджана выкарыстанне
  арыгінальнага тэксту паведамлення для парсінгу замест кароткага
  перакладу-самары, што вырашыла праблему пустых палёў у VAC-0071 (Zara Psary).

## [2025-05-06] Мадэрнізацыя архітэктуры апрацоўкі паведамленняў і парсінгу

### Backend / Services & Routes

- **inbox.js**:
  - Укаранёны фонавы працэсар чаргі (FIFO) з інтэрвалам 5 хвілін.
  - Дададзены статус `__processing__` для прадухілення паўторнай апрацоўкі паведамленняў пры збоях.
  - Інтэгравана перадача кантэксту (паведамленні і вакансіі за сёння) у Gemini для разумнай дэдуплікацыі.
- **gemini.service.js**:
  - Створаны шматслойны ланцужок фолбэкаў (Gemini 2.5 Flash -> 2.0 Flash -> Groq Llama 8b).
  - Рэалізавана логіка разумнай замарозкі квот з аўтаматычным размарожваннем а 07:00 раніцы.
  - Укаранёна Stage 1 класіфікацыя: вызначэнне тыпу паведамлення (FULL_VACANCY, MULTI_VACANCY, UPDATE, INFO, NOISE).
- **ai.service.js**:
  - Рэалізаваны "Split & Parse" механізм: аўтаматычнае разбіццё паведамленняў з некалькімі вакансіямі на асобныя блокі.
  - Дададзена функцыя `enrichTextWithDocs`: аўтаматычная загрузка і ўключэнне тэксту з Google Docs па спасылках у паведамленні.
  - Укаранёна іерархія мадэляў для парсінгу: SMART (Llama 70b) -> FAST (Llama 8b).
  - Абноўлены геаграфічныя правілы: строгая лацінка для польскіх гарадоў і англійская мова для іншых краін.
- **messageFilters.js**:
  - Значна пашыраны фільтры шуму: дададзены адсеў сістэмных паведамленняў ботаў (`**processing**`), англамоўных дыялогаў рэкрутэраў і агра-спецыфічных працоўных чатаў.
  - Створаны блок `CANDIDATE_FORM_NOISE` для ідэнтыфікацыі і ігнаравання анкет кандыдатаў (пашпарты, спісы імёнаў).
  - Выпраўлена логіка `getWhitelistedAgency` для бяспечнай працы з кароткімі назвамі чатаў.

### Database / Models

- **UnprocessedMessage.js**: Дададзены індэксы для аптымізацыі чаргі і `prefixHash` для хуткай дэдуплікацыі на этапе прыёму (push).
- **Vacancy.js**: Замацавана ўнікальнасць `vacancyCode` і структура палёў v2.0.

## [0.6.0] - 2026-05-06 — Канвеер v2.0: Stage 1 (Gemini) -> Stage 2 (Groq)

### Added

- **Two-Stage Pipeline**: Укаранёна падзеленая апрацоўка. Stage 1 (Gemini) адказвае за класіфікацыю, дэдуплікацыю і поўны пераклад на ўкраінскую мову. Stage 2 (Groq) выконвае толькі структураваны парсінг у JSON, выкарыстоўваючы ўжо гатовы пераклад.
- **aiAnalyzed Flag**: У мадэль `UnprocessedMessage` дададзены маркер `aiAnalyzed`. Гэта дазваляе адрозніваць новыя паведамленні ад тых, што ўжо прайшлі AI-аналіз, але патрабуюць ручной апрацоўкі (Update/Info).
- **Google Docs Stage 0**: Збагачэнне тэксту з Google Docs цяпер адбываецца да класіфікацыі, што дазваляе AI бачыць поўны змест вакансіі пры вызначэнні катэгорыі.

### Fixed

- **Double Translation**: Выключана паўторнае выкарыстанне токенаў для перакладу. Groq цяпер атрымлівае чысты ўкраінскі тэкст ад Gemini.
- **Inbox Visibility**: Выпраўлена дэфолтная катэгорыя паведамленняў з `chat` на `info`. Цяпер усе паведамленні з вайтліста адразу бачныя ў пясочніцы.
- **FIFO Logic**: Фонавы працэсар цяпер строга прытрымліваецца чаргі ад старых паведамленняў да новых.
- **EN Noise Patterns**: Дададзены новыя рэджэкс-фільтры для англамоўнага шуму ("Not written", "Cucumbers are stopped", "Waiting for response").

### Changed

- **Multi-Vacancy Handling**: Паведамленні з некалькімі вакансіямі цяпер аўтаматычна класіфікуюцца як `update` і накіроўваюцца ў пясочніцу для ручнога кантролю, што эканоміць ліміты Groq.

## [0.6.1] - 2026-05-06 — Хотфікс тыпу rawText і self-loop

### 🔴 Backend (Крытычны)

- **rawText Cast Error (`inbox.js`):** Выпраўлена памылка
  `Cast to string failed for value (type Object)`.
  Калі Groq-фолбэк вяртаў `translatedText` як аб'ект,
  код спрабаваў запісаць яго ў `String`-поле MongoDB.
  Дадана тыпавая праверка: калі `analysis.translatedText`
  не радок — выкарыстоўваецца арыгінальны `enrichedText`.

- **NOVA WORK AGENCY Self-Loop (`messageFilters.js`):**
  Дадан запіс `{ id: "-1002851211149", agency: "IGNORE_SELF" }`.
  Паведамленні з уласнага канала агенцыі больш не будуць
  трапляць у буфер.

## [0.6.2] - 2026-05-08 — Стабілізацыя канвеера і разумная фільтрацыя

### ✅ Backend & AI

- **Аптымізацыя інтэрвалаў:** Інтэрвал фонавай апрацоўкі павялічаны да **10 хвілін**. Гэта дазваляе MacroDroid сабраць поўны тэкст паведамленняў (stitching) і эканоміць сутачныя ліміты Gemini (RPD).
- **Зняцце лімітаў чаргі:** Выдалена абмежаванне `.limit(10)` у `inbox.js`. Цяпер робат апрацоўвае ўсе назапашаныя паведамленні за адзін цыкл.
- **Строгая класіфікацыя вакансій:** У `gemini.service.js` уведзена правіла: паведамленне лічыцца `FULL_VACANCY` толькі калі яго даўжыня **> 300 сімвалаў** або яно змяшчае спасылку на **Google Docs**. Кароткія паведамленні аўтаматычна ідуць у Пясочніцу як `UPDATE`.
- **Smart Deduplication:** AI цяпер ігнаруе розніцу ў эмодзі і стандартных прывітаннях пры параўнанні паведамленняў, што выключае стварэнне дублікатаў пры дробных праўках тэксту агенцыямі.
- **Павялічаны тэрмін захавання:** `expires` для неапрацаваных паведамленняў павялічаны да **72 гадзін** (3 дні).

### 🧹 Фільтрацыя шуму

- **Узмацненне Regex:**
  - Выпраўлены паттэрн для пашпартоў у `CANDIDATE_FORM_NOISE` (цяпер патрабуе наяўнасць лічбаў), што выратавала вакансіі ад памылковага выдалення.
  - Дададзены новыя маркеры рэкрутэрскага чату: "маю жінку на", "звичайний песель", "сильна перевірка", "дзвоню, виясняю".
- **Дакладны Stitching:** `prefixHash` павялічаны да **250 сімвалаў**, каб пазбегнуць ілжывага склейвання розных вакансій з аднолькавымі прывітаннямі.

## [0.6.3] - 2026-05-08 — Візуалізацыя статусу AI ў Пясочніцы

### ✅ Frontend

- **Індыкацыя стану апрацоўкі:** Укаранёны бэйджы "Не апрацавана" (⏳) і "Апрацавана" (✨) для кожнага паведамлення ў Inbox.
- **Разумнае адлюстраванне тэксту:** Рэалізавана пераключэнне кантэнту — да аналізу паказваецца арыгінальны тэкст, пасля аналізу — украінскі пераклад (`rawText`).
- **Маніторынг чаргі:** У сетку статыстыкі дададзена новая картка "Чакаюць AI", якая паказвае колькасць паведамленняў у чарзе на апрацоўку.
- **Абарона ад памылак:** Кнопкі стварэння і абнаўлення вакансій цяпер заблакаваныя для паведамленняў без AI-аналізу, каб пазбегнуць выкарыстання некласіфікаваных даных.
- **Паляпшэнне UX:** Неапрацаваныя паведамленні візуальна аддзелены праз паніжаную празрыстасць (`opacity-70`), што дазваляе рэкрутэру факусавацца на гатовых да працы вакансіях.

## [0.6.4] - 2026-05-08 — Стабілізацыя чаргі і паляпшэнне якасці перакладу

### ✅ Backend & AI

- **Рэанімацыя чаргі:** У `inbox.js` укаранёны механізм аўтаматычнага аднаўлення паведамленняў, якія "завіслі" ў статусе апрацоўкі больш чым на 5 хвілін.
- **Абарона ад пропускаў:** Дададзена логіка `break` пры памылках AI. Калі робат сустракае ліміт (429), ён спыняе цыкл, пакідаючы астатнія паведамленні ў чарзе да наступнага запуску, што гарантуе 100% апрацоўку без пропускаў.
- **Якасць перакладу:** У `gemini.service.js` узмацнена правіла мовы. Цяпер усе паведамленні (англ/рус) прымусова перакладаюцца на ўкраінскую, а арыгінальны тэкст дадаецца знізу пасля раздзяляльніка `--- ORIGINAL ---`.
- **Аптымізацыя лімітаў:** Порг аўтаматычнага стварэння вакансій зніжаны з 300 да **200 сімвалаў**.
- **Фікс памылкі 413 (Groq):** У `ai.service.js` уваходны тэкст для сплітэра абмежаваны да 5000 сімвалаў, каб пазбегнуць перавышэння лімітаў TPM (Tokens Per Minute) на бясплатным тарыфе.

### 🧹 Фільтрацыя

- **FIFO Прыярытэт:** Фонавы працэсар цяпер строга прытрымліваецца храналагічнага парадку, апрацоўваючы самыя старыя паведамленні першымі.

## [0.6.5] - 2026-05-08 — Глыбокая ачыстка Пясочніцы і ўзмацненне AI-перакладу

### ✅ Backend & AI

- **Аўта-ачыстка чату:** Паведамленні пра статус канкрэтных кандыдатаў ("прыехаў", "адмовіўся", "ператэлефаную") і кароткія рэплікі рэкрутэраў цяпер класіфікуюцца як `NOISE`.
- **Схаванне смецця:** У `inbox.js` паведамленні катэгорыі `NOISE` цяпер атрымліваюць статус `processed: true`, што дазваляе аўтаматычна прыбіраць іх з Пясочніцы.
- **Прымусовы пераклад:** У `gemini.service.js` замацавана жорсткае правіла: любы тэкст на англійскай ці рускай мовах павінен быць перакладзены на ўкраінскую. Арыгінал дадаецца знізу пасля `--- ORIGINAL ---`.
- **Даступнасць вакансій:** Порг для `FULL_VACANCY` зніжаны да **200 сімвалаў**, каб кароткія вакансіі (напрыклад, ад SG) апрацоўваліся аўтаматычна.

### 🧹 Фільтрацыя шуму

- **Пашырэнне Regex-базы:** У `messageFilters.js` дададзены новыя маркеры для адсячэння тэхнічнай перапіскі яшчэ да звароту да AI: "дякую, забрали", "refused to work", "will arrive", "call you back", "team won't take" і інш.

## [0.6.6] - 2026-05-08 — Паляпшэнне класіфікацыі доўгіх вакансій

### ✅ Backend & AI

- **Фікс класіфікацыі (Кейс "Сушы"):** У `gemini.service.js` узмацнены промпт. Цяпер паведамленні з падрабязным апісаннем (абавязкі, патрабаванні, умовы) прымусова класіфікуюцца як `FULL_VACANCY`, нават калі яны прыходзяць з MANUAL-чатаў. Гэта выправіла памылку, калі доўгія вакансіі памылкова лічыліся апдэйтамі.
- **Паляпшэнне дэтэкцыі лакацыі:** У `inbox.js` пашыраны Regex для функцыі `hasMinimalVacancyData`. Дададзены ключавыя словы "місто", "локація", "адреса", што дазваляе робату лепш распазнаваць вакансіі без сімвала 📍.
- **Аптымізацыя Splitter:** У `ai.service.js` ліміт уваходнага тэксту для сплітэра зафіксаваны на **4000 сімвалаў**. Гэта забяспечвае баланс паміж паўнатой даных і абаронай ад памылкі 413 (Request too large) на бясплатным тарыфе Groq.

### 🧹 Фільтрацыя

- **Дакладнасць валідацыі:** Палепшаная прэ-валідацыя даных дазваляе больш дакладна адрозніваць паўнавартасныя аб'явы ад кароткіх інфармацыйных паведамленняў.

## [0.6.7] - 2026-05-09 — Уніфікацыя даных і паляпшэнне AI-парсінгу

### ✅ Backend & AI

- **Інтэлектуальная ачыстка брэндаў:** У `ai.service.js` укаранёны `BRAND_BLACKLIST` і функцыя `validateBrand`. Цяпер сістэма аўтаматычна выдаляе агульныя словы ("склад", "ферма", "завод") з поля брэнда, пакідаючы толькі ўласныя назвы.
- **Уніфікацыя агенцый:** Дададзена функцыя `normalizeAgency`, якая прыводзіць назвы агенцый да адзінага стандарту (UPPERCASE + мапінг) пры парсінгу і мерджы шаблонаў.
- **Паляпшэнне геаграфіі:** Укаранёна логіка `displayLocation`. Для вакансій па-за межамі Польшчы краіна аўтаматычна дадаецца ў дужках да горада ў загалоўку і лакацыі.
- **Строгія правілы парсінгу:** Абноўлены `SYSTEM_INSTRUCTION` для AI. Дададзены жорсткія патрабаванні па мапінгу дакументаў ("Біометрія", "PESEL UKR") і катэгарызацыі нюансаў працы.

### 🧹 Гігіена даных

- **Выдаленне дублікатаў:** Ачышчаны код функцыі `parseVacancyWithAI`, выдалены паўторныя блокі апрацоўкі загалоўкаў і зарплаты.
- **Экспарт утыліт:** Функцыі нармалізацыі даступныя для выкарыстання ў іншых маршрутах (напр. пры ручным рэдагаванні).

## [2.1.0] — 2026-05-09

### Backend / AI

- `ai.service.js`: додано `KNOWN_AGENCIES` список та функцію `normalizeAgency()`
  — агенцыі прыводзяцца да UPPER і маппінгу незалежна ад мовы ўводу
- `ai.service.js`: додано `BRAND_BLACKLIST` та функцію `validateBrand()`
  — агульныя словы ("ферма", "склад", "цех" і інш.) больш не трапляюць у поле `brand`
- `ai.service.js`: абноўлены `SYSTEM_INSTRUCTION` — дакладныя правілы для
  `brand`, `agencyName`, `standardDocs`, `specificNuances` з катэгорыямі
- `ai.service.js`: `mergeWithTemplate` цяпер таксама нармалізуе `agencyName`
- `ai.service.js`: экспартаваны `normalizeAgency` і `validateBrand`
  для выкарыстання ў іншых сэрвісах

### Frontend — EditVacancyModal

- Поле `agencyName` → `<select>` dropdown з эталонным спісам агенцый
- Поле `contractType` → кнопкі (Umowa zlecenie / Umowa o pracę / Іншае)
  з тэкставым полем для custom значэння
- Поле `count` → кнопкі (1 чалавек / Пара / Сям'я)
- Поле `polishLanguageLevel` → кнопкі замест тэкставага поля
- Поле `accommodation.type` → кнопкі (Надається / Надається для пар / Не надається)
  з аўтасінхранізацыяй `forCouples`
- Поле `foodType` → кнопкі (Власне / Обіди / Субсидоване)
- Выдалены дубліруючы чэкбокс `forCouples` (зараз кіруецца кнопкай тыпу жытла)

### Frontend — VacancyViewModal

- Уведзены кампаненты `SectionTitle`, `Row`, `Note` для двух памераў тэксту:
  загалоўкі секцый — буйней, апісальныя нататкі — дробны курсіў
- Брэнд (`brand`) перанесены ў шапку мадала побач з катэгорыяй
- Лакацыя цяпер заўжды адлюстроўвае краіну ў дужках для не-польскіх вакансій
- Абавязкі (`description`) дзеляцца строга па `;` (не па `.`)
- `physicalLoad` вылучаны курсівам з іконкай
- Дадатковыя дакументы (`additionalDocsDetails`) адлюстроўваюцца як `Note`
- Раздзел "Витрати" паказваецца толькі калі ёсць хаця б адно значэнне

### Frontend — Vacancies (картка ў спісе)

- Брэнд-тэг перанесены ў верхні радок побач з катэгорыяй
- Лакацыя заўжды з назвай краіны для замежных вакансій
- Зарплата хаваецца калі поле пустое (замест пустога месца)

## [2.2.0] — 2026-05-09

### Backend (Крытычны фікс)

- `index.js`: падключаны маршруты `/api/templates`, `/api/candidates`, `/api/apply`
  — гэта была галоўная прычына таго, чаму старонкі Templates і Candidates
  не грузіліся (500/404 на ўсіх запытах)

### Backend — candidates.js (маршрут)

- Выдалена старая логіка `match-vacancies` v1.0 (выкарыстоўвала `vacancy.sphere`,
  `vacancy.accommodation.available`, `requirements.docs` — палі якіх няма ў v2.0)
- Перапісана пад v2.0: `vacancy.category`, `accommodation.type`,
  `requirements.standardDocs`, `requirements.polishLanguageLevel`,
  `schedule.shiftsCount`, `accommodation.forCouples`

### Backend — apply.js

- Выпраўлена `vacancy.title` → `vacancy.vacancydescription || vacancy.templateName`
- Фармат паведамлення прыведзены да HTML (адпавядае `parse_mode: "HTML"`
  у `notifyRecruiter`)

### Backend — Template.js (схема)

- Дадана поле `brand: { type: String, default: "" }` — сінхранізацыя з Vacancy.js
- `requirements.ageMax`: `default: 99` → `default: null` — сінхранізацыя з Vacancy.js

### Backend — ai.service.js

- Дадана `"STAFF POWER"` у масіў `KNOWN_AGENCIES`

## [2.3.0] — 2026-05-09

### Backend / AI-Parser Improvements

- `ai.service.js`: радыкальна абноўлены `SPLIT_PROMPT`
  — уведзена "правіла адной вакансіі": AI забаронена рэзаць доўгія тэксты на часткі, калі яны апісваюць адну пазіцыю. Гэта вырашыла праблему страты лакацыі і дэталяў транспарту.
- `ai.service.js`: палепшана логіка вызначэння геаграфіі ў `SYSTEM_INSTRUCTION`
  — дададзены жорсткія маркеры для падзелу `location` (месца працы) і `checkInCity` (месца афармлення/офіс) на аснове ключавых слоў "оформлення", "приїзд", "місто".
- `ai.service.js`: аптымізавана функцыя `parseVacancyWithAI`
  — выпраўлена памылка дублявання аб'яўлення зменнай `parsed`.
  — укаранёна дынамічнае фарміраванне `displayLocation` для замежных вакансій: цяпер краіна аўтаматычна дадаецца ў дужках у загаловак.
- `ai.service.js`: удакладнены спіс `standardDocs`
  — дададзены пункт "Довідка резидента" для карэктнага распазнавання і фільтрацыі.
- `ai.service.js`: палепшаны `FORMAT_PROMPT` для Telegram
  — апісанне даезду (`transportDetails`) цяпер выводзіцца разгорнута побач са статусам "надається", што робіць пасты больш інфарматыўнымі.

## [2.4.0] — 2026-05-09

### Backend / AI & Routing (Мадэрнізацыя "Tier 1")

- `ai.service.js`: Укаранёны адзіны "бранябойны" рухавік `executeAIRequest` з ланцужком з 6 мадэляў (Gemini 2.0 Flash/Lite, 2.5 Flash/Lite і Groq 70b/8b).
- `ai.service.js`: Сплітар (разбіўка на некалькі вакансій) перанесены непасрэдна ў асноўны промпт парсера для захавання 100% кантэксту.
- `ai.service.js`: Укаранёна аўтаматычная класіфікацыя `parsingResultType` (FULL_VACANCY vs UPDATE) з улікам ліміту ў 250 сімвалаў.
- `ai.service.js`: Усталявана разумная паўза ў 1 гадзіну пры вычарпанні лімітаў усіх AI-правайдэраў (замест замарозкі да раніцы).
- `vacancies.js`: Функцыя `processVacancyMessage` цяпер працуе як інтэлектуальны дыспетчар: паўнавартасныя вакансіі ідуць у базу і Telegram, а кароткія апдэйты аўтаматычна накіроўваюцца ў Пясочніцу (`UnprocessedMessage`).
- `gemini.service.js`: Цалкам пераведзены на выкарыстанне адзінага AI-рухавіка, што спрасціла код і павялічыла надзейнасць Stage 1.

## [2.4.1] — 2026-05-10

### Backend / AI Stability & Logic

- `ai.service.js`: Аптымізаваны фармат запыту ў `executeAIRequest` (выкарыстанне масіва частак і версіі `v1beta`) для забеспячэння сумяшчальнасці з Enterprise/Tier 1 ключамі Google.
- `gemini.service.js`: Узмацнены правілы класіфікацыі ў `SYSTEM_PROMPT`. Уведзена жорсткае патрабаванне маркіраваць доўгія паведамленні (>300 сімв.) з дэталямі працы як `FULL_VACANCY`, каб пазбегнуць іх памылковага скіду ў Пясочніцу.
- `gemini.service.js`: Выдалены небяспечны фолбэк "па змаўчанні ў UPDATE" у блоку `catch`. Цяпер пры тэхнічных збоях (напр. `Error fetching`) сістэма вяртае `null`, што пакідае паведамленне ў буферы для паўторнай спробы і захоўвае яго зыходную якасць.

## [2.5.0] — 2026-05-10 — Архітэктура "Канвеер v2.0": Сплітар і Буфер

### ✅ Backend & AI (Мадэрнізацыя)

- **Двухэтапная апрацоўка (Pipeline):** Укаранёна падзеленая логіка апрацоўкі вакансій:
  - **Stage 1 (Gemini):** Класіфікацыя, поўны пераклад на ўкраінскую і інтэлектуальны спліцінг (разбіўка мульці-вакансій на масіў фрагментаў).
  - **Stage 2 (Groq/Gemini):** Ювелірны парсінг кожнага фрагмента ў структуру JSON v2.0.
- **15-хвілінны буфер (Scheduler):** Дададзены фонавы працэсар, які кожныя 15 хвілін правярае Пясочніцу і аўтаматычна апрацоўвае новыя паведамленні праз поўны AI-канвеер.
- **Stage 0 (Enrichment):** Функцыя загрузкі тэксту з Google Docs перанесена ў самы пачатак ланцужка, што дазволіла AI бачыць поўны кантэнт пры класіфікацыі.
- **Эканомія токенаў:** Уведзена "Жалезнае правіла 250 сімвалаў" — кароткія паведамленні без спасылак аўтаматычна ідуць у Пясочніцу як UPDATE без выкліку AI.
- **Zero-Output Rule:** Абноўлены `FORMAT_PROMPT` для Telegram. Цяпер пустыя палі, эмодзі і загалоўкі секцый цалкам хаваюцца, калі ў іх няма дадзеных.
- **Гігіена базы:** Кожная вакансія цяпер захоўвае ў `rawText` толькі свой рэлевантны фрагмент тэксту замест усяго агульнага паведамлення.

### 🛠 Тэхнічныя праўкі

- У `vacancies.js` рэалізаваны падзел на ручны парсінг (напрамую Stage 2) і аўтаматычны (Stage 1 + Stage 2).
- Захаваны маркер `isTruncated` для візуалізацыі абрэзаных паведамленняў на фронтэндзе.
- Уніфікаваны матор `executeAIRequest` цяпер выкарыстоўваецца ва ўсіх AI-сэрвісах.

## [2025-05-10] — Стабілізацыя канвеера апрацоўкі паведамленняў

### Выпраўлена:

- **Валідацыя UnprocessedMessage**: Дададзена `lowercase: true` для поля `category`. Цяпер катэгорыі ад AI (напр. `UPDATE`) аўтаматычна праходзяць валідацыю `enum`.
- **Завісанне канвеера (inbox.js)**: У цыкле апрацоўкі паведамленняў `break` заменены на `continue`. Цяпер памылка ў адным паведамленні не спыняе ўсю чаргу.
- **Памылка імпарту (inbox.js)**: Выпраўлены выклік `enrichTextWithDocs`, функцыя цяпер правільна імпартуецца з `gemini.service.js`.
- **Памылка валідацыі (vacancies.js)**: Поле `senderInfo` заменена на `sender` пры стварэнні sandbox-элемента, што вырашыла праблему `Path sender is required`.
- **Канфлікт планіроўшчыкаў**: Выдалены дублюючы `setInterval` з `vacancies.js`. Цяпер за апрацоўку чаргі адказвае толькі адзін планіроўшчык у `inbox.js`.

### Палепшана:

- **Фільтрацыя шуму**: У `messageFilters.js` дададзены новыя патэрны для адсейвання кароткіх чатавых паведамленняў ("наберу завтра", "дякую всім", "зателефоную").
- **Захаванне логікі**: У `vacancies.js` вернуты арыгінальныя каментары-страхоўкі і строгая ачыстка Telegram Markdown.

## [2025-05-10] — Пераход на карпаратыўны SDK Google AI (Agent Platform)

### Дададзена:

- **Аўтэнтыфікацыя ADC**: Сістэма пераведзена з API-ключаў на Application Default Credentials (ADC). На Render загружаны `adc.json` для бяспечнай аўтарызацыі праз карпаратыўны акаўнт.
- **Новы SDK**: Усталявана бібліятэка `@google/genai` для працы з Google Agent Platform (Vertex AI).

### Выпраўлена:

- **Памылка аўтарызацыі**: Вырашана праблема `API Keys are Disallowed` шляхам выкарыстання Service Account і ADC.
- **Памылка URL**: Выпраўлена памылка фармавання URL (`/mod`), якая ўзнікала ў старым SDK.
- **Захаванне катэгорый**: У `gemini.service.js` выдалены "шкодны" fallback, які ператвараў вакансіі ў "Інфа" пры памылках AI. Цяпер пры памылцы паведамленне застаецца неапрацаваным для паўторнай спробы.
- **Аптымізацыя мадэляў**: У `AI_CHAIN` прыярытэт аддадзены `flash-lite` версіям для максімальнай эканоміі токенаў. Выдалена слабая мадэль Groq, якая выклікала памылку 413 (Request too large).

### Тэхнічныя змены:

- Абноўлена ініцыялізацыя кліента `GoogleGenAI` з параметрам `vertexai: true`.
- Абноўлена логіка атрымання тэксту ў `executeAIRequest` пад новы фармат адказу SDK (`response.text`).

# Лаг зменаў

## 📂 ai.service.js

- Дададзена абмежаванне ўваходу (`substring(0, 6000)`) для абароны ад памылкі 413.
- Дададзена праверка cooldown праз `chainFrozenUntil`.
- Абноўлены блок **Gemini**:
  - Пераход на новы SDK `@google/genai` з ADC.
  - Правільны доступ да выніку праз `response.text`.
  - Дададзена падтрымка `responseMimeType` для JSON/plain.
- Абноўлены блок **Groq**:
  - Выклік праз `groq.chat.completions.create`.
  - Правільны фармат `messages` (system + user).
- Catch‑блок цяпер карэктна працягвае да наступнай мадэлі і вяртае `null`, калі ўсе мадэлі не адказалі.

---

## 📂 gemini.service.js

- Catch‑блок у enrichTextWithDocs і analyzeAndCompareWithGemini пакідае паведамленні “неапрацаванымі” пры памылках.
- Інтэграцыя з абноўленым `executeAIRequest`.
- Кэш і аўта‑класыфікацыя кароткіх паведамленняў (<250 сімвалаў) засталіся без змен.

---

## 📂 FORMAT_PROMPT

- Дададзены пустыя радкі паміж блокамі (💰, 🛠, 📋, 🕒, 📄, 🏠, 🚌, 💸, 🌡, 📝).
- 🏠 Accommodation:
  - Прыбрана дубляванне загалоўкаў.
  - Паказваецца толькі “🏠 Проживання” + радкі з дэталямі, калі ёсць даныя.
- 🚌 Transport:
  - Прыбрана дубляванне загалоўкаў.
  - Паказваецца толькі “🚌 Довіз: немає” або “🚌 Довіз: надається” + дэталі.
  - “Власний” больш не выкарыстоўваецца.
- 📍 Location:
  - Строга аддзяляецца “📍 Місто” (месца працы) ад “• Оформлення” (месца афармлення).
- 📝 Додаткова:
  - Арганізаваны трансфер з Украіны заўсёды пераносіцца сюды, а не ў Transport.

---

## 📂 SYSTEM_INSTRUCTION

- Дададзены блок **STRICT LOCATION VALIDATION**:
  - location = actual place of work.
  - checkInCity = registration/arrival city.
  - Калі ў сырым тэксце лакацыя напісана з памылкай (напрыклад, "Elena Gura"), аўтаматычна замяняць на правільную польскую назву ("Zielona Góra").
  - Завсёды выкарыстоўваць польскую арфаграфію (Latin alphabet).
  - Калі лакацыя не пазначана дакладна, пакідаць як ёсць, але дадаваць у additionalNotes для ручной праверкі.

---

# 📝 Вынік

- **ai.service.js** — абноўлены пад новы SDK, fallback працуе правільна.
- **gemini.service.js** — catch‑блок і кэш нармальна працуюць, інтэграцыя з новым `executeAIRequest`.
- **FORMAT_PROMPT** — больш кампактны, без дублявання, з падзеламі паміж блокамі.
- **SYSTEM_INSTRUCTION** — дададзена строгая праверка лакацыі, каб пазбегнуць памылак тыпу “Елена Гура”.
- **Дададзены слоўнік перакладу** (TRANSLATION_MAP) для аўтаматычнага мапінгу кірылічных назваў агенцый у лацінку (напр. "Прогрес" -> "PROGRES").
- **Выпраўлена лагічная памылка ў executeAIRequest**: прыбраны недасягальны return null, цяпер ланцужок правільна замарожваецца пры адмове ўсіх мадэляў.
- **У SYSTEM_INSTRUCTION** дададзена строгае правіла перакладу назваў агенцый для AI.

## [2025-05-10] — Пераклад інтэрфейсу і сінхранізацыя з v2.0

### Пераклад (Украінская мова):

- **Галоўная старонка**: Перакладзены Hero-блок, статыстыка і карткі свежых вакансій.
- **Пясочніца (Inbox)**: Перакладзены статусы апрацоўкі AI ("Оброблено", "Не оброблено"), катэгорыі і кнопкі кіравання.
- **Вакансіі і Шаблоны**: Поўны пераклад загалоўкаў, фільтраў і формаў дадання/рэдагавання.
- **Навігацыя**: Меню Layout перакладзена на ўкраінскую мову.

### Тэхнічныя праўкі:

- **Сінхранізацыя палёў**: Усюды, дзе выкарыстоўваўся загаловак вакансіі, поле `title` замінена на `vacancydescription` для адпаведнасці схеме бэкенда v2.0.
- **Уніфікацыя агенцый**: У `EditVacancyModal.jsx` ручны спіс агенцый замінены на імпарт эталоннага масіва `AGENCIES` з `masterData.js`.
- **Выпраўленне адлюстравання**: У картках вакансій дададзена апрацоўка масіва гендараў (`requirements.gender.join`) для карэктнага вываду.

### 12.05.2026 — Нармалізацыя дадзеных і пашыраныя фільтры

**Backend:**

- **AI Service**:
  - Укаранёна функцыя `normalizeLocation` для ачысткі гарадоў ад дубляў краін і кірыліцы.
  - Абноўлена логіка `processSingle`: цяпер загалоўкі вакансій фармуюцца без паўтораў лакацыі.
  - У `SYSTEM_INSTRUCTION` дададзена 10 строгіх катэгорый для нюансаў працы (`specificNuances`).
  - Абноўлены `FORMAT_PROMPT`: дададзены "Кампактны рэжым" для кароткіх вакансій і жорсткія правілы па вывадзе транспарту.
- **Gemini Service**:
  - Абноўлены `SYSTEM_PROMPT` сплітэра: дададзены правілы для апрацоўкі шматмоўных паведамленняў і аб'яднаных пазіцый у адным цэху.
  - Дададзена падрабязнае лагаванне загрузкі Google Docs.
- **Vacancies Route**:
  - Створаны маршрут `GET /filters-data` для аддачы чыстых спісаў гарадоў, брэндаў і нюансаў на фронт.
  - Дададзена функцыя `cleanTelegramPost` для выдалення пустых радкоў і лэйблаў перад адпраўкай у ТГ.
  - Створаны сістэмны маршрут `POST /system/cleanup-locations` для масавай нармалізацыі лакацый у існуючай базе.

### 12.05.2026 — Аналіз нюансаў працы і план развіцця UI/UX

**Вынікі аналізу дадзеных:**

- **Класіфікацыя нюансаў**: На аснове аналізу 200+ шаблонаў вылучана 10 крытычных катэгорый для фільтрацыі:
  1. Тэмпературны рэжым (холад/спёка).
  2. Фізічна-цяжкая праца (вага >15кг).
  3. Санітарныя абмежаванні (манікюр, віі, біжутэрыя).
  4. Пахі і алергены (гума, фарба, пыл).
  5. Шум (праца ў навушніках/берушах).
  6. Характар працы (100% стоячы/сядзячая).
  7. Спецыфічныя навыкі (нож, пнеўмаінструмент, чытанне чарцяжоў).
  8. Нормы (акорд, высокі тэмп).
  9. Тэсты пры вступе (мануальныя, матэматыка).
  10. Іншае (аўта, праверка зроку і г.д.).
- **UI/UX Аўдыт**: Выяўлены праблемы са скролам панэлі фільтраў, накладаннем выпадаючых спісаў і адсутнасцю адаптыўнасці для мабільных прылад.

**План далейшых дзеянняў (Roadmap):**

1. **Frontend: Інтэграцыя новых фільтраў (Крок 6.1)**
   - Падключыць кампанент фільтраў да новага эндпойнта `/api/vacancies/filters-data`.
   - Укараніць фільтры па: Тыпе дагавору, Гадзінах працы (8 / 10-12), Зменах (Дзень / Мікс) і Нюансах (10 катэгорый).
   - Замяніць простыя спісы на Checkbox-Dropdown для мультывыбару (горад, брэнд, агенцыя).

2. **Frontend: Рэдызайн інтэрфейсу (Крок 6.2)**
   - Зрабіць панэль фільтраў фіксаванай (Sticky Sidebar), каб скроліўся толькі спіс вакансій.
   - Павялічыць шчыльнасць палёў (Compact UI), каб усе асноўныя фільтры змяшчаліся на адным экране.
   - Выправіць паводзіны Dropdown-меню: яны павінны адкрывацца ўверх, калі знаходзяцца ўнізе экрана.

3. **Лакалізацыя і Мабільнасць (Крок 6.3)**
   - Поўны пераклад інтэрфейсу фронтэнда з беларускай на ўкраінскую мову.
   - Распрацоўка мабільнай версіі: схаваная панэль фільтраў (Drawer/Modal) і адаптыўныя карткі вакансій.

## [12.05.2026] — Укараненне Layer 2 Filtering і фікс лакацый

### ✅ Backend

- **messageFilters.js**: Дададзены агрэсіўны масіў `POST_AI_NOISE_UA` (30+ патэрнаў) для адсеку рэкрутэрскага шуму пасля AI-перакладу.
- **inbox.js**: Укаранёна логіка аўта-архівацыі (`processed: true`, `category: "chat"`) для паведамленняў, якія прайшлі Stage 1, але прызнаны шумам. Гэта дазволіць трымаць Пясочніцу чыстай.
- **ai.service.js**: Удакладнена логіка `finalTitle` у `processSingle`. Цяпер праверка на дублікат горада ў загалоўку ідзе па чыстай назве (`cleanLoc`), што выключае паўторы тыпу "Rottendorf — Rottendorf (Germany)".
- **ai.service.js**: Палепшана функцыя `normalizeLocation` для больш жорсткай ачысткі дубляў краін у дужках.

### 🎯 Вынік

- Пясочніца аўтаматычна чысціцца ад дыялогаў тыпу "Маю жінку", "Якая стаўка", "Впишіть у табліцу".
- Вакансіі ў базе і Telegram атрымалі чыстыя загалоўкі без геаграфічных дубляў.

### ✅ Backend (ai.service.js)

- **Валюта**: Дададзена аўтаматычная страхоўка валюты ў `processSingle`. Калі AI вяртае лічбу без сімвала, сістэма падстаўляе zł/€ у залежнасці ад краіны.
- **Telegram**: Абноўлены `FORMAT_PROMPT`. Цяпер у пастах адлюстроўваюцца гадзіны ў месяц (`hoursRange`) і патрабаванне па вопыце працы.
- **Транспарт**: Укаранёна поўная ачыстка дэталяў транспарту пры значэнні "Власний", каб пазбегнуць пустых або нелагічных радкоў у ТГ.
- Уведзена эталонная нармалізацыя для 19 агенцый (пераклад з RU/UA + UPPERCASE).
- Дададзена аўтаматычная канвертацыя ўкраінскіх назваў ваяводстваў у польскія эталоны.
- Укаранёны строгі фільтр нюансаў: цяпер захоўваюцца толькі 10 зацверджаных катэгорый.
- Палепшана дэдуплікацыя ў Пясочніцы: хэш цяпер ігнаруе даты і час адпраўкі паведамленняў.
- Абноўлены маршрут `cleanup-locations` для поўнай стандартызацыі ўсіх існуючых вакансій у базе.

### 12.05.2026 — Аптымізацыя AI-канвеера і выпраўленне адлюстравання перакладу

**Backend:**

- **inbox.js**:
  - Выпраўлены баг з адлюстраваннем расійскай мовы ў Пясочніцы: цяпер у поле `rawText` захоўваецца вынік перакладу ад Gemini (`translatedFragments`).
  - Прыбраны двайны выклік `enrichTextWithDocs` (Google Docs цяпер загружаецца адзін раз унутры `gemini.service`).
  - Укаранёна пафрагментная апрацоўка вакансій: калі паведамленне змяшчае некалькі прапаноў, кожная адпраўляецца на парсінг асобна.
- **vacancies.js**:
  - Прыбраны залішні выклік Gemini ў функцыі `processVacancyMessage`. Цяпер яна працуе як чысты Stage 2 парсер, выкарыстоўваючы ўжо гатовы пераклад з Inbox.
- **messageFilters.js**:
  - Абноўлена функцыя `getPrefixHash`: цяпер яна выдаляе эмодзі, што забяспечвае стабільную дэдуплікацыю паведамленняў (вырашана праблема дубляў ад адной агенцыі пры змене афармлення).
  - Пашыраны спіс `POST_AI_NOISE_UA` новымі ўкраінскімі стоп-фразамі для больш якаснай фільтрацыі рэкрутэрскага шуму пасля AI-перакладу.
- **ai.service.js**:
  - Абноўлена функцыя `normalizeNuances`: дададзена ігнараванне рэгістра пры праверцы катэгорый і аўтаматычнае групаванне нераспазнаных нюансаў у катэгорыю "Інше", каб пазбегнуць страты дадзеных.

## [12.05.2026] — Глыбокая нармалізацыя дадзеных і "Ядзерны перазапуск" базы

### ✅ Backend & AI (Рэвалюцыя якасці)

- **Строгая нармалізацыя**: Укаранёны функцыі `normalizeLocation`, `normalizeAgency`, `validateBrand` і `normalizeNuances`. Цяпер дадзеныя прыводзяцца да эталона (лацінка для гарадоў, UPPERCASE для агенцый, 10 катэгорый для нюансаў) яшчэ да захавання ў БД.
- **Аптымізацыя AI-канвеера**:
  - **inbox.js**: Выпраўлены баг з адлюстраваннем расійскай мовы ў Пясочніцы — цяпер у поле `rawText` заўсёды захоўваецца ўкраінскі пераклад фрагментаў ад Gemini.
  - **vacancies.js**: Прыбраны залішні выклік Gemini ў функцыі `processVacancyMessage`. Цяпер яна працуе як чысты Stage 2 парсер, выкарыстоўваючы гатовы пераклад з Inbox.
  - **gemini.service.js**: Прыбраны двайны выклік `enrichTextWithDocs` (Google Docs загружаецца адзін раз).
- **Дэдуплікацыя 2.0**: Абноўлена функцыя `getPrefixHash` — цяпер яна выдаляе эмодзі, дату і час. Гэта забяспечвае стабільную дэдуплікацыю паведамленняў ад адной агенцыі (напр. APOLO), нават калі яны мяняюць афармленне.
- **Фільтрацыя шуму**: Пашыраны спіс `POST_AI_NOISE_UA` новымі ўкраінскімі стоп-фразамі для адсеку рэкрутэрскага шуму пасля AI-перакладу.

### 🧹 Database (Fresh Start)

- **Nuclear Reset**: Выканана поўная ачыстка калекцый `Vacancies` і `UnprocessedMessages` для пачатку працы з чыстага ліста.
- **Seed Templates**: База шаблонаў перазапісана з нуля праз `seedTemplates.js`. У базу паспяхова загружана **279 эталонных шаблонаў** з актуальнымі правіламі нармалізацыі.

### 🎯 Вынік

- Сістэма цалкам гатовая да прыёму "чыстых" дадзеных.
- Пясочніца стала 100% украінскамоўнай.
- Выключаны дублікаты вакансій і геаграфічны "вінегрэт" у загалоўках.

### 🛠 Наступныя крокі

- Маніторынг якасці новых вакансій, якія паступяць у базу пасля перазапуску.
- **Frontend**: Пераход да Крока 6 (Sticky Filters, Checkbox-Dropdowns для мультывыбару, мабільная адаптацыя).

## [13.05.2026] — План мадэрнізацыі v2.1: "Clean UI & Safe Data"

### 🛡️ Backend & AI (Гігіена і Прыватнасць)

- **Абарона ад маркетынгавых дубляў (SG Filter)**: Укараненне ў `messageFilters.js` логікі дэтэкцыі кароткіх "бонусных" анонсаў. Калі паведамленне пачынаецца з лічбаў + "зл за кандидата" і яго даўжыня < 400 сімвалаў — прысвойваем катэгорыю `UPDATE`. Гэта дазволіць такім паведамленням ісці ў Пясочніцу, не ствараючы дублікатаў вакансій.
- **Пашырэнне Regex-шуму (UA)**: Даданне ўкраінскіх стоп-фразаў для фільтрацыі рэкрутэрскіх чатаў: `зарплата прийшла`, `чекає на карту`, `децизію`, `минутку уточню`, `хто першы`, `питають і зникають`.
- **Ізаляцыя прыватных даных (Recruiter Vault)**: Абнаўленне `SYSTEM_INSTRUCTION`. Любыя згадкі пра бонусы рэкрутэрам (грошы "за галаву") і ўнутраную колькасць месцаў ("трэба 1 чалавек") запісваюцца **строга** ў `forRecruiter.internalNotes`. У публічныя палі `additionalNotes` або `description` гэтая інфа трапляць не павінна.
- **Гендарная і колькасная дакладнасць**: Пераход ад агульных тэгаў да канкрэтыкі. AI павінен запісваць патрабаванні тыпу "2 пары + 1 жанчына". Калі асобна мужчын не бяруць — гэта павінна быць адлюстравана ў полі `sex`, каб адсекчы нерэлевантныя водгукі.
- **Структураванне нюансаў для тэгаў**: Змена фармату `specificNuances`. Цяпер гэта масіў аб'ектаў `{ category, text }` (напр. `{ category: "TEMPERATURE", text: "склад +20°C" }`). Гэта неабходна для будучай фільтрацыі і "воблака тэгаў" на фронце.

### 📢 Telegram & Formatting (Ідэальны Пост)

- **Чыстка "Нюансаў"**: Поўнае выдаленне службовых назваў катэгорый. Замест _"Характер праці (робота зі сканером)"_ выводзім толькі _"• робота зі сканером"_. Вывад праз кулі (•) або праз коску.
- **Аптымізацыя моўнага блока**: Аб'яднанне моўных патрабаванняў у адзін радок: _"Мова: A1 (Знання польської мови — бажане)"_.
- **Дэдуплікацыя графіка**: Аўтаматычнае выдаленне пункта "Перерва", калі гэтая інфармацыя ўжо ёсць у апісанні зменаў.
- **Zero Technical Noise**: Забарона на вывад у Telegram тэхнічных пазнак AI (_"інфармацыя не поўная"_, _"тэкст абарваны"_).
- **Апрацоўка "Абрэзаных" вакансій**: Вакансіі з `isTruncated: true` публікуюцца ў ТГ у максімальна чыстым выглядзе, а асноўная праца па іх дапаўненні пераносіцца ў адмінку.

### 💻 Frontend (UI/UX Evolution v2.0)

- **Sticky Filters & Sidebar**: Замацаванне панэлі фільтраў пры скроле для пастаяннага доступу.
- **Multi-select Dropdowns**: Укараненне чэкбоксаў для мультывыбару ў палях "Горад", "Агенцыя" і "Катэгорыя".
- **Хмара Тэгаў (Tag Cloud)**:
  - Адлюстраванне нюансаў на картцы як каляровых інтэрактыўных тэгаў.
  - Магчымасць хуткай фільтрацыі па кліку на тэг.
- **Badge "⚠️ АБРЭЗАНА"**: Візуальная пазнака і жоўтая рамка на картках вакансій, якія патрабуюць ручной праверкі рэкрутэрам.
- **Mobile Drawer**: Перанос сістэмы фільтраў у ніжнюю панэль для мабільных прылад.

### 🎯 Вынік

- Тэлеграм-канал становіцца чыстай вітрынай без унутранай "кухні".
- Усе бонусы і колькасныя абмежаванні ізаляваны ва ўнутраных нататках.
- Фронтэнд атрымлівае прафесійную сістэму фільтрацыі і візуальную дыягностыку праблем.

### 🛠 Наступныя крокі

- **Stage 1**: Абнаўленне `messageFilters.js` (Regex-шум і SG-бонус фільтр).
- **Stage 2**: Мадэрнізацыя `ai.service.js` (Промпты для ізаляцыі бонусаў і фарматавання ТГ).
- **Stage 3**: Frontend — Sticky UI, Multi-select і Tag Cloud.

## [2026-05-13] — План мадэрнізацыі v2.1: "Clean UI & Safe Data"

### ✅ Backend & AI (Гігіена і Прыватнасць)

- **Мадэлі (Vacancy/Template):**
  - Поле `physicalLoad` пераведзена ў `Boolean`.
  - Поле `gender` пашырана значэннем "Сім'ї".
  - Дададзена поле `genderDescription` для канкрэтыкі ("2 пари + 1 жінка").
  - Структура `specificNuances` зменена на масіў аб'ектаў `{ category, text }`.
- **AI Service (ai.service.js):**
  - **SYSTEM_INSTRUCTION:** Уведзены жорсткія правілы прыватнасці. Бонусы рэкрутэраў і ўнутраныя лічбы па набору цяпер ідуць ТОЛЬКІ ў `forRecruiter.internalNotes`.
  - **FORMAT_PROMPT:** Поўнасцю ачышчаны Telegram-пост. Прыбраны назвы катэгорый нюансаў, тэхнічны шум ("інфармацыя не поўная") і дублікаты графіка. Моўны блок аб'яднаны ў адзін радок.
  - **normalizeLocation:** Выпраўлены баг дублявання краіны ў дужках (напр. "France (France)").
- **Filters (messageFilters.js):**
  - Дададзены новыя ўкраінскія патэрны шуму для Пясочніцы.
  - Укаранёна функцыя `isMarketingBonus` для дэтэкцыі кароткіх рэкламных анонсаў SG.

### 🎯 Вынік

- Сістэма гатовая да аўтаматычнага адсеку маркетынгавых дубляў.
- Telegram-канал стаў прафесійнай вітрынай без унутранай рэкрутэрскай інфармацыі.
- База даных атрымала структуру, неабходную для прасунутай фільтрацыі і воблака тэгаў.

## [2026-05-13] — План мадэрнізацыі v2.1: "Clean UI & Safe Data" (Completed)

### ✅ Backend & AI

- **ai.service.js:** Поўная ізаляцыя бонусаў рэкрутэра. Нюансы цяпер — гэта аб'екты `{category, text}`.
- **Models:** `physicalLoad` пераведзены ў `Boolean`, дададзена `genderDescription`, пашыраны спіс гендараў ("Сім'ї").

### ✅ Frontend (UI/UX)

- **EditVacancyModal.jsx:**
  - Дададзена поле для рэдагавання `genderDescription`.
  - `physicalLoad` заменены на чэкбокс.
  - Выпраўлена захаванне нюансаў (канвертацыя тэксту ў аб'екты).
- **VacancyViewModal.jsx:** Ачышчаны вывад нюансаў (толькі тэкст без катэгорый).
- **Vacancies.jsx:** Выпраўлены крытычныя памылкі `e.includes` праз падтрымку нюансаў-аб'ектаў у фільтрах і статыстыцы.

### [14.05.2026] — Unified Modernization Plan v2.1

**Backend & AI:**

- **Pipeline Fix:** Removed 250-char translation limit; AI classification now overrides Layer 2 noise filters.
- **Privacy Shield:** Strictly isolated recruiter bonuses and internal counts into `forRecruiter.internalNotes`.
- **Marketing De-duplication:** Implemented `isMarketingBonus` filter to prevent duplicate vacancies from short promo posts.
- **Data Normalization:** Fixed double-country naming in locations; structured nuances as `{category, text}` objects.
- **TG Formatting:** Cleaned nuances from category labels; merged language requirements; removed redundant schedule fields and technical AI noise.

**Frontend (Roadmap):**

- **Advanced Filtering:** Sticky sidebar with multi-select (checkbox) dropdowns.
- **Visual Diagnostics:** Added "⚠️ TRUNCATED" badge for incomplete vacancies.
- **Interactive UI:** Nuance-based Tag Cloud for quick filtering and better data visualization.
  [14.05.2026] - gemini.service.js: removed 250-char limit for translation; added shared info rule to splitting prompt while maintaining strict vacancy criteria.

### [14.05.2026] — Pipeline & Filtering Update

- **inbox.js**: Дададзена аднаўленне "завіслых" паведамленняў (`__processing__`), укаранёна фільтрацыя Layer 2 з імунітэтам для поўных вакансій і інтэгравана дэтэкцыя маркетынгавых бонусаў SG.
- **messageFilters.js**: Пашыраны спіс `POST_AI_NOISE_UA` новымі ўкраінскімі стоп-фразамі для лепшай гігіены Пясочніцы.

### [14.05.2026] — Backend: Atomic Counters & Logic Unification

- **Counter.js**: Створана новая мадэль для атамарных лічыльнікаў у MongoDB.
- **vacancies.js**:
  - Укаранёна функцыя `generateVacancyCode` праз `findOneAndUpdate` ($inc), што на 100% гарантуе ўнікальнасць VAC-кодаў без race conditions.
  - Рэфактарынг `processVacancyMessage`: логіка захавання спрошчана, прыбраны лішнія retry-цыклы.
  - Маршрут `/auto` пераведзены на выкарыстанне цэнтралізаванай функцыі працэсінгу.
  - Выпраўлены збор фільтраў у `/filters-data` для падтрымкі нюансаў у фармаце аб'ектаў `{category, text}`.
  - Скрыпт `/system/cleanup-locations` абноўлены пад стандарты v2.1 (нармалізацыя нюансаў і загалоўкаў).

### [14.05.2026] — AI Service: Privacy & Formatting Refinement

- **ai.service.js**:
  - Укаранёны **Privacy Shield**: бонусы рэкрутэраў і ўнутраныя лічбы набору цяпер строга ізалююцца ў `internalNotes`.
  - Структура `specificNuances` пераведзена на масіў аб'ектаў `{category, text}` для дакладнай катэгарызацыі.
  - Аптымізаваны `FORMAT_PROMPT`: прыбраны назвы катэгорый нюансаў і палепшаны вывад моўнага блока для Тэлеграма (кампактны рэжым).
  - Дададзена аўтаматычная чыстка `description` ад пустых радкоў і фолбэк на `MANUAL` для невядомых агенцый у функцыі `normalizeAgency`.

### [15.05.2026] — Frontend: UI/UX Evolution v2.1

- **VacancyViewModal.jsx**:
  - Укаранёны каляровыя тэгі для нюансаў (чырвоныя для крытычных катэгорый).
  - Дададзена адлюстраванне канкрэтыкі па набору (`genderDescription`) і лагічны вывад фізічнай нагрузкі.
- **Vacancies.jsx**:
  - Дададзены візуальны badge "⚠️ ОБРІЗАНО" для вакансій з непаўным тэкстам.
  - Укаранёна "Хмара тэгаў" (Tag Cloud) прама на картках вакансій для хуткага азнаямлення.
  - Пераведзена фільтрацыя нюансаў на строгае супадзенне катэгорый.
- **EditVacancyModal.jsx**:
  - Рэалізавана разумнае захаванне нюансаў: пры рэдагаванні тэксту катэгорыі (напр. "Шум") цяпер захоўваюцца.
  - Дададзена поле для рэдагавання `genderDescription` і выпраўлена праца чэкбокса фізічнай нагрузкі.

### [15.05.2026] — Мадэрнізацыя бэкенд-канвеера v2.2

**Мадэль дадзеных (Vacancy.js):**

- Поле `ageMax` пераведзена з `Number` у `String` для падтрымкі дыяпазонаў узросту (напр. "18-55", "да 60 років").
- Дададзена поле `parsingResultType` для захавання вердыкту AI (FULL_VACANCY, UPDATE, TRUNCATED).

**Stage 1: Класіфікацыя і ўзбагачэнне (gemini.service.js):**

- **Google Drive Folders:** Укаранёна падтрымка чытання папак Google Drive. Сістэма аўтаматычна збірае тэкст з усіх Google Docs унутры папкі.
- **Жорсткая класіфікацыя:** Уведзены ліміт у 400 сімвалаў для `FULL_VACANCY`. Паведамленні, карацейшыя за гэты парог або без апісання працэсаў, аўтаматычна атрымліваюць статус `UPDATE`.
- **Шматмоўны мержынг:** AI цяпер прыярытэтна выкарыстоўвае ўкраінскую версію тэксту як аснову, дадаючы ў яе ўнікальныя дэталі з рускіх ці польскіх блокаў.
- **Катэгорыя TRUNCATED:** Дададзена афіцыйная дэтэкцыя абарваных паведамленняў на этапе класіфікацыі.

**Stage 2: Парсінг і прыватнасць (ai.service.js):**

- **Privacy Shield:** Узмацнена ізаляцыя бонусаў рэкрутэраў і ўнутраных лічбаў набору. Яны трапляюць толькі ў `internalNotes` і не выходзяць у Тэлеграм.
- **Чысціня палёў:** AI забаронена пісаць узрост і нацыі ў поле гендара. Узрост ідзе ў `ageMax`, нацыянальнасці — у масіў `nationalities`.
- **Аб'ектны Тэлеграм:** Функцыя `formatTelegramPost` пераведзена на працу з гатовым JSON-аб'ектам з базы. Гэта гарантуе, што ў пост трапляюць толькі адобраныя палі.
- **Compact Mode:** Уведзены аўтаматычны кампактны выгляд (адзін абзац) для паведамленняў, агульны памер якіх менш за 700 сімвалаў.

**Логіка маршрутаў (inbox.js & vacancies.js):**

- **Сінхранізацыя:** Вердыкт AI з першай фазы праходзіць праз увесь ланцужок і захоўваецца ў базу.
- **Бяспечная паслядоўнасць:** Укаранёна схема: **Захаванне ў БД -> Фарматаванне паста па палях базы -> Адпраўка ў Тэлеграм**.
- **Аўтапарсінг:** Забаронена аўтаматычная апрацоўка (Stage 2) для паведамленняў тыпу `UPDATE` і `TRUNCATED`.

### [15.05.2026] — Поўная інтэграцыя Stage 0 і бяспечнага рэндэрынгу v2.2

**Stage 0: Узбагачэнне тэксту (gemini.service.js & vacancies.js):**

- **Google Drive Folders:** Дададзена поўная падтрымка чытання папак Drive. Сістэма ітэруе па файлах і збірае тэкст з усіх Google Docs унутры.
- **Усюдыіснасць Stage 0:** Выклік `enrichTextWithDocs` інтэграваны ва ўсе шляхі стварэння вакансій: аўтаматычны канвеер, мануальнае стварэнне з Пясочніцы і стварэнне праз шаблоны.
- **Захаванне кантэксту:** У базе дадзеных у полі `rawText` цяпер захоўваецца поўны тэкст (арыгінальнае паведамленне + змесціва спампаваных файлаў).

**Stage 2: Бяспечны рэндэрынг Тэлеграма (ai.service.js & vacancies.js):**

- **Object-based Rendering:** Функцыя `formatTelegramPost` цяпер працуе не з сырым тэкстам, а з гатовым JSON-аб'ектам з базы.
- **Privacy Shield:** Дзякуючы схеме "Захаванне -> Рэндэрынг", бонусы рэкрутэраў і ўнутраныя нататкі фізічна не могуць патрапіць у Тэлеграм, бо яны ізаляваны ў `internalNotes`.
- **Smart Formatting:** `FORMAT_PROMPT` навучаны выбіраць паміж `COMPACT` (адзін абзац) і `FULL` (структураваны спіс) рэжымамі на аснове агульнага памеру дадзеных (парог 700 сімвалаў).

**Логіка маршрутаў (vacancies.js):**

- **Выпраўленне Scope:** Выпраўлена памылка бачнасці `enrichedText` у маршруце стварэння з шаблона.
- **Сінхранізацыя вердыкту:** Маршрут `/from-template` цяпер прымае і захоўвае `parsingResultType`.
- **Валідацыя экспартаў:** Адноўлены правільны экспарт `router` у файле вакансій, што забяспечвае стабільнасць API.

**Мадэль дадзеных (Vacancy.js):**

- Тып поля `ageMax` канчаткова замацаваны як `String` для гнуткага запісу патрабаванняў да ўзросту.

## [16.05.2026] — Frontend Evolution v2.2-UI: Поўная ўкраінізацыя і структураваныя дадзеныя

### ✅ Канстанты і Фільтры (masterData.js, filters.js)

- **Поўная ўкраінізацыя**: Усе лэйблы (статусы, мовы, катэгорыі) пераведзены на ўкраінскую мову.
- **Аптымізацыя структуры**: Выдалена састарэлае поле `travelGroup`. Масіў `GENDERS` пашыраны значэннем "Сім'ї".
- **Сінхранізацыя**: Фільтры ў `filters.js` прыведзены ў адпаведнасць з новай схемай `gender`.

### ✅ Галоўны экран (Vacancies.jsx, VacancyFilters.jsx)

- **Інфармацыйны "Пашпарт"**: Верхні радок карткі вакансіі рэканструяваны. Цяпер там адлюстроўваюцца: ID, Статус, Агенцыя, Брэнд, Катэгорыя, Лакацыя (Горад + Ваяводства).
- **Timestamp**: Дададзена дата стварэння вакансіі (`createdAt`) у фармаце `uk-UA`.
- **Хмара тэгаў (Tag Cloud)**: Укаранёны візуальны блок групаваных тэгаў (Жытло, Давоз, Мова, Нацыі, Дакументы).
- **Катэгарызацыя нюансаў**: У кароткім выглядзе выводзяцца толькі катэгорыі нюансаў з іконкамі (напр. 🌡️ Тэмпературны рэжым) замест доўгага тэксту.
- **Украінізацыя фільтраў**: Левая панэль фільтраў цалкам пераведзена на ўкраінскую мову, блок "Хто едзе" пераключаны на выкарыстанне `gender`.

### ✅ Мадальныя вокны (VacancyViewModal.jsx, EditVacancyModal.jsx)

- **View Modal**:
  - Поўная ўкраінізацыя інтэрфейсу.
  - Падзел блока "Набор" на агульны гендар і дэталізаванае апісанне (`genderDescription`).
  - Падтрымка `ageMax` як радка і вывад масіва нацыянальнасцяў.
  - Нюансы адлюстроўваюцца як падрабязныя баджы "Катэгорыя: Тэкст".
- **Edit Modal (у працэсе)**:
  - Поле `ageMax` пераведзена ў тып `text` для падтрымкі дыяпазонаў (напр. "18-55").
  - Пачата маштабная ўкраінізацыя лэйблаў (завершаны блокі: Шапка, Сістэмныя палі, Лакацыя, Аплата).

### 🎯 Вынік

Фронтэнд цяпер цалкам разумее структуру дадзеных бэкенда v2.2. Рэкрутэр бачыць максімум карыснай інфармацыі адразу ў спісе вакансій, што паскарае падбор.

### [18.05.2026] — Frontend: Повна українізація та "Оборонний" рендеринг v2.2

**Глобальні зміни:**

- **Локалізація**: Усі інтерфейси (модальні вікна, фільтри, статуси) повністю перекладені на українську мову. Видалено всі залишки білоруської мови та HR-суржику (напр., "Підходящі" -> "Відповідні").
- **Стабільність**: Упроваджено "Оборонне програмування" — безпечна ініціалізація масивів (`nationalities`, `docs`, `nuances`) через фолбеки `|| []`, що запобігає краху інтерфейсу ("White Screen of Death") при некоректних даних від AI.

**Компоненти:**

- **Vacancies.jsx**:
  - Реконструйовано "Паспорт" вакансії (ID, Статус, Агенція, Бренд, Категорія, Локація в один рядок).
  - Впроваджено "Розумну локацію": назва країни більше не дублюється, якщо вона вже є в назві міста.
  - Хмара тегів перенесена під опис вакансії та групується за унікальними категоріями (без дублів).
  - Набір (гендер) відображається лише компактними маркерами (Ч, Ж, Парі, Сім'ї).
- **VacancyViewModal.jsx**:
  - Реалізовано інтелектуальну дедуплікацію нюансів: якщо текст нюансу вже є в основному описі, він автоматично ховається.
  - Покращено візуалізацію витрат, бонусів та умов проживання.
- **EditVacancyModal.jsx**:
  - Поле `ageMax` переведено в тип `text` для підтримки діапазонів (напр., "18-55").
  - Додано поле `genderDescription` для деталізації набору.
  - Усі кнопки вибору (Single/MultiBtnGroup) тепер працюють на українських константах.
- **VacancyFilters.jsx**:
  - Впроваджено мапінг технічних ключів нюансів (`temperature`, `noise`) у зрозумілі назви з іконками з `masterData.js`.
- **VacancyMatchModal.jsx**:
  - Повна українізація статусів кандидатів та інтерфейсу підбору.

**Константи (masterData.js):**

- Оновлено `CHECKLIST_ITEMS` з іконками та уточненими назвами.
- Розширено `GENDERS` (додано "Сім'ї") та актуалізовано список `AGENCIES`.

### [18.05.2026] — Frontend: Аптимізація ієрархії даних у картці вакансії v2.2.1

**Зміни в інтерфейсі (Vacancies.jsx):**

- **Реструктуризація "Панелі швидких фактів" (Рядок 2)**: Усі базові умови побуту (Набір, Житло, Довіз, Мова) зібрані в один логічний блок. Це дозволяє рекрутеру миттєво оцінити "фундамент" вакансії.
- **Інтеграція тега "Пари"**: Позначка 👫 тепер відображається безпосередньо поруч зі статусом житла (`Житло надається + 👫`). Це усунуло дублювання інформації та звільнило місце в хмарі тегів.
- **Оптимізація "Хмари тегів"**: Тепер блок тегів фокусується виключно на специфічних вимогах:
  - **Національності** (🌍) та **Документи** (📄).
  - **Нюанси** (🌡️, 🏋️ тощо) — додано логіку приховування порожніх тегів. Якщо текст нюансу був видалений як дублікат основного опису, сам тег категорії більше не відображається.
- **Інформаційна точність**: Збережено маркер `*` для гендера, який сигналізує про наявність важливих уточнень у повному описі (наприклад, про чоловіків зі своїм житлом).
- **Візуальна гігієна**: Картка стала більш структурованою, з чітким поділом на "базовий комфорт" та "специфічні вимоги".

### 19.05.2026 — Тэхнічная гігіена AI і Privacy Shield

**Backend (ai.service.js):**

- Выдалена неіснуючая мадэль `gemini-3-flash-preview` з ланцужка апрацоўкі, што прыбярэ памылкі 404 у логах.
- Уніфікаваны катэгорыі нюансаў (перавод на ўкраінскі стандарт), што спыняе страту даных у "Інше".
- Укаранёны **Technical Privacy Shield**: прыватныя палі (`forRecruiter`, `rawText`, `textHash`) цяпер фізічна выдаляюцца з аб'екта перад адпраўкай у AI для фармавання Тэлеграм-поста.
- Дададзена правіла апрацоўкі "бітых" сімвалаў у зарплаце (фікс страты даных як у VAC-0055).
- Выпраўлена лагічная супярэчнасць у патрабаваннях да мовы (фікс для выпадкаў тыпу VAC-0047).
- Дададзена жорсткая праверка выніку фармавання посту: зараз RAW JSON фізічна не зможа патрапіць у канал (фікс VAC-0060).

### 19.05.2026 — Аптымізацыя апрацоўкі і Markdown

**Backend (vacancies.js):**

- Функцыя `processVacancyMessage` стала больш эканомнай: яна больш не выклікае Google Drive паўторна, калі тэкст ужо быў узбагачаны ў Inbox (эканомія часу і рэсурсаў).
- Палепшаны санітайзер Markdown: выдалены сімвалы (`_`, `[`, `]`, `(`, `)`), якія часцей за ўсё выклікалі памылкі пры адпраўцы ў Telegram (фікс "Markdown Error").
- Маршрут стварэння з шаблона цяпер таксама падтрымлівае разумнае ўзбагачэнне тэксту.

### 19.05.2026 — Рэформа мадэлі даных і аптымізацыя канвеера

**Backend (Models):**

- **Vacancy.js & Template.js**: Пераведзены на гібрыдную структуру даных. Зарплата (`baseNetto`, `studentNetto`) і Узрост (`age.min`, `age.max`) цяпер захоўваюцца як лічбы (`Number`) для будучай фільтрацыі.
- Дададзены палі `rawSalaryDisplay` і `age.rawText` для захавання арыгінальнага прыгожага вываду ў Telegram.
- Сінхранізавана структура нюансаў у шаблонах (цяпер гэта масіў аб'ектаў, а не радкоў).

**Backend (Inbox Pipeline):**

- Укаранёны **"Імунітэт па даўжыні"**: паведамленні < 350 сімвалаў ад вядомых агенцый аўтаматычна ідуць у `UPDATE`, эканомячы запыты да Stage 1 AI.
- Уведзены **"Кантроль Сплітэра"**: ліміт на 5 фрагментаў з аднаго паведамлення для абароны ад стварэння дублікатаў-пустышак.
- Палепшана дэдуплікацыя: функцыя хэшавання цяпер ігнаруе службовае слова "Menu" (фікс спаму ад MacroDroid/Viber).
- Дададзены новыя рэгекс-фільтры для адсеку рэкрутэрскага шуму ("чи візьмуть на Псари", "усіх на псари").

**AI Service (ai.service.js):**

- Абноўлены `SYSTEM_INSTRUCTION`: AI навучаны аддаваць прыярытэт "гарачым" запытам у пачатку тэксту над шаблоннымі хвастамі.
- Дададзена правіла захавання "бітых" сімвалаў у зарплаце (фікс VAC-0055).
- `FORMAT_PROMPT` пераведзены на выкарыстанне тэкставых палёў для стабільнага адлюстравання дыяпазонаў.

### 19.05.2026 — Ліквідацыя дэзінфармацыі і міграцыя даных

**Backend:**

- **ai.service.js**: Выпраўлена функцыя `normalizeLocation`. Цяпер яна агрэсіўна выдаляе дублікаты краін у дужках, забяспечваючы чысціню гарадоў у базе і фільтрах.
- **vacancies.js**: Абноўлены скрыпт Cleanup. Дададзена логіка **аўтаматычнай міграцыі**: старыя тэкставыя палі зарплаты і ўзросту цяпер канвертуюцца ў лічбавыя палі новай мадэлі (v2.2).
- **vacancies.js**: Маршрут `/filters-data` больш не дублюе назву краіны пры зборы спіса гарадоў.

**Frontend:**

- **Vacancies.jsx**: Укаранёны "Абарончы рэндэрынг" для жылля. Калі даных няма, сістэма піша "Житло не вказано" замест ілжывага "Житло надається".
- **Vacancies.jsx**: Выпраўлена логіка фільтрацыі па гендары. Цяпер фільтр правільна працуе з масівам `requirements.gender`.
- **Vacancies.jsx**: Адлюстраванне зарплаты пераведзена на поле `rawSalaryDisplay`, што дазваляе бачыць прыгожыя дыяпазоны ("25-30 зл"), захоўваючы лічбы для фільтрацыі "пад капотам".

**Вынік:**

- Пясочніца і спіс вакансій ачышчаны ад геаграфічнага "вінегрэту".
- Фільтры пачалі бачыць вакансіі, створаныя да рэформы мадэлі.
- Рэкрутэры бачаць праўдзівы статус жылля.

### 19.05.2026 — Рэформа парсінгу і фарматавання (Backend)

**AI Service & Gemini:**

- **Мульты-лакацыі**: Укаранёна правіла `MULTI-CITY RULE`. Калі адна вакансія мае спіс гарадоў (напр. Biedronka), яны больш не спліцяцца на розныя фрагменты, а запісваюцца ў адно поле праз коску.
- **Фарматаванне тэксту**: AI навучаны выкарыстоўваць падвойныя пераносы радкоў (`\n\n`) і маркіраваныя спісы (`•`). Поле `description` цяпер фармуецца як вертыкальны спіс, а не суцэльны радок.
- **Тэлеграм-гігіена**: Аптымізаваны `FORMAT_PROMPT`. Дададзена правіла ігнаравання дубляў у блоку зарплаты (калі дэталі паўтараюць асноўную стаўку).
- **Катэгарызацыя**: Вакансіі тыпу "Догляд" (Caregiver) цяпер аўтаматычна накіроўваюцца ў катэгорыю "Різне".

**Маршруты (vacancies.js):**

- **Разумныя фільтры**: Маршрут `/filters-data` цяпер разбівае спісы гарадоў праз коску на асобныя пункты. Гэта дазваляе фільтраваць вакансіі па кожным горадзе асобна.
- **Абарона ад пустышак**: У маршрут ручнога стварэння дададзена валідацыя апісання (мінімум 30 сімвалаў). Гэта прадухіляе стварэнне вакансій без карыснай інфармацыі.
- **Нармалізацыя**: Функцыя `normalizeLocation` цяпер агрэсіўна чысціць дублікаты гарадоў і краін, забяспечваючы чысціню базы.

### 19.05.2026 — Паляпшэнне візуалізацыі і сінхранізацыя (Frontend)

**Vacancies.jsx & VacancyViewModal.jsx:**

- **Фікс "Маналіту"**: Укаранёна ўласцівасць `whitespace-pre-wrap` для блокаў апісання. Цяпер маркіраваныя спісы і абзацы, сфармаваныя AI, адлюстроўваюцца карэктна (вертыкальна), а не зліваюцца ў адзін радок.
- **Сінхранізацыя катэгорый**: Мапінг іконак і лэйблаў нюансаў абноўлены пад новыя ўкраінскія назвы з бэкенда. Гэта вярнула каляровую індыкацыю "тэрміновасці" для тэмпературных рэжымаў і фізічных нагрузак.
- **Абарончы рэндэрынг**: Дададзены праверкі `Array.isArray` для поля гендара і `optional chaining` для лакацый. Гэта прадухіляе "белы экран" пры адсутнасці некаторых даных у старых вакансіях.
- **Разумныя тэгі**: Хмара тэгаў на картцы вакансіі цяпер выкарыстоўвае ўніфікаваныя назвы катэгорый наўпрост з базы.

### 20.05.2026 — Фінальная паліроўка інтэрфейсу (Frontend)

**Vacancies.jsx & VacancyViewModal.jsx:**

- **Фікс чытальнасці**: Укаранёна ўласцівасць `whitespace-pre-wrap` для ўсіх тэкставых блокаў (апісанне, графік, жыллё, дадатковыя нататкі). Цяпер пераносы радкоў і спісы ад AI адлюстроўваюцца карэктна ва ўсіх частках праграмы.
- **Аптымізацыя картак**: Выдалены дублюючы блок зарплаты ў спісе вакансій. Цяпер на картцы адлюстроўваецца толькі кампактная стаўка, што робіць спіс больш акуратным.
- **Кампактны опіс**: Дададзена поле "Кароткі опіс" з булітамі прама на картку вакансіі (з абмежаваннем у 2 радкі), каб рэкрутэр мог хутка зразумець суць працы, не адкрываючы мадалку.
- **Разумныя лакацыі**: Выпраўлена логіка збору гарадоў у `dynamicData`, што канчаткова ліквідавала дубляванне краіны ў фільтрах.

### 20.05.2026 — Фінальная сінхранізацыя інтэрфейсу (Frontend)

**Vacancies.jsx & VacancyViewModal.jsx:**

- **Ліквідацыя дубляў**: Выпраўлена логіка апрацоўкі лакацый. Цяпер назва краіны не дублюецца ў фільтрах і на картках (напр. замест "America (Netherlands) (Netherlands)" адлюстроўваецца чыстае "America (Netherlands)").
- **Аптымізацыя вёрсткі**:
  - Выдалены папярэдні прагляд апісання з картак вакансій для большай кампактнасці спіса.
  - Блок зарплаты перанесены ўнутар агульнага шэрагу з іконкамі і прыціснуты да правага краю (`ml-auto`), што выправіла памылку з "выпаданнем" цэлага радка.
- **Сінхранізацыя нюансаў**: Мапінг іконак і логіка падсветкі "крытычных" умоў (тэмпература, нагрузка) цалкам пераведзены на новыя ўкраінскія назвы катэгорый з бэкенда.
- **Чытальнасць**: Укаранёна ўласцівасць `whitespace-pre-wrap` для блокаў графіка і жылля ў мадальным акне, што дазволіла адлюстроўваць падрабязныя спісы ад AI ў зручным выглядзе.
  Enhanced formatText helper with regex to split text by semicolons and periods (only when followed by a capital letter).
  This ensures legacy "monolith" text blocks are automatically converted into readable bulleted lists without breaking decimals or abbreviations.
